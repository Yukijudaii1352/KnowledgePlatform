/**
 * harness-data.js — 由 pipeline/build.py 于 2026-06-15 09:55:47 自动生成。
 * 源文件：content/agent/harness.md
 * ⚠️  请勿手动修改；如需更新，修改源文档后重新编译。
 */
window.PAGE_CONFIG = {
  "meta": {
    "domain": "agent",
    "topic_id": "harness",
    "topic_name": "Harness",
    "page_title": "Agent Harness Engineering 技术演进",
    "page_subtitle": "2026-06-15 版",
    "page_desc": "从 WebArena、AgentBench、SWE-bench 的可复现实验环境，到 Codex、Claude Code、GenericAgent、OpenClaw 的执行 harness，再到 Meta-Harness、AHE、Harness-Bench、RHO 代表的自动优化与诊断范式，系统梳理 Agent 评测 harness、任务环境、自动回归与运行时工程主线。",
    "page_icon": "🧰",
    "hero_pills": [
      "🏷️ Benchmark · Sandbox · Runtime · Regression",
      "Codex · Claude Code · OpenClaw · Harness Optimization"
    ],
    "count_pill": "{count} 个算法",
    "image_base": "",
    "overview_from_doc": true,
    "latest_overview_from_doc": true
  },
  "overview": [
    {
      "title": "",
      "body_html": "<h1>智体Harness工程：综述（上）</h1>\n<ul>\n<li>来源平台: <strong>zhihu</strong>（知乎专栏文章）</li>\n<li>原文链接: <a href=\"https://zhuanlan.zhihu.com/p/2041527501567027027\">https://zhuanlan.zhihu.com/p/2041527501567027027</a></li>\n<li>作者: 黄浴</li>\n</ul>\n<hr />\n<p>智体Harness工程：综述（上）</p>\n<h1>智体Harness工程：综述（上）</h1>\n<p>作者: 黄浴, 赞: 15</p>\n<p>26年5月来自CMU, Yale大学, JHU, 东北大学（NEU）, Tulane大学, 阿拉巴马大学（UAB）, 俄亥俄州立（OSU）, Virginia Tech 和Amazon公司的论文“Agent Harness Engineering: A Survey”。</p>\n<p>大语言模型（LLM）智体在生产环境中的快速部署揭示一个反复出现的规律：任务执行的可靠性与其说取决于底层的语言模型本身，不如说更多地取决于包裹该模型的“基础设施层”——即智体Harness（Agent Harness）。本综述基于实践经验，对智体Harness工程进行系统性的梳理，并围绕三个核心论点展开。首先，智体harness是一个独立的系统层，其工程质量在很大程度上决定实际应用中的系统可靠性；通过三个阶段的工程演进（从“提示工程”到“上下文管理”再到“harness工程”）来阐述这一观点，并在此基础上进行跨层级的综合分析，涵盖“成本-质量-速度”三难困境、“能力与控制”的权衡取舍，以及“harness耦合”等关键问题；此外，还提出一系列开放性研究议题，旨在填补当前研究领域的空白并解决生产环境中的痛点。其次，提出 ETCLOVG 这一七层分类体系（分别代表：执行环境、工具接口、上下文管理、生命周期/编排、可观测性、验证、治理）；该体系在既有的六组件框架基础上进行扩展，将“可观测性”和“治理”提升为独立的架构考量要素。第三，将 170 多个开源项目映射至上述分类体系中，旨在揭示当前生态系统的发展模式、覆盖范围的空白点以及正在涌现的设计原则；与此同时，还提炼一系列源自 OpenAI、Anthropic 和 LangChain 等机构生产部署实践的工程原则，旨在弥合从业者经验知识与学术研究术语体系之间的鸿沟。</p>\n<p>如图4所示智体Harness工程分类体系的详细内容：</p>\n<p><img alt=\"\" src=\"https://picx.zhimg.com/v2-bb995bb960b7a9392e1763b6569f1e2f_1440w.jpg\" /></p>\n<hr />\n<h2>约束瓶颈：驾驭（Harness）机制重于模型本身</h2>\n<p>针对基于大语言模型（LLM）的智体（Agent）进行的学术研究，在很大程度上一直聚焦于模型本身。研究议程的核心在于探究模型的能力边界：它能否执行多步规划、能否可靠地调用工具、能否检索并压缩相关记忆，抑或能否与其他智体协同工作。这种研究范式背后潜藏着一个隐性假设：智体的能力主要取决于模型的能力；换言之，只要拥有足够强大的模型并辅以足够精良的提示词（Prompt），便能产出足够可靠的行为表现。</p>\n<p>然而，近期涌现的实证证据对这一假设提出了挑战——即单纯依靠更先进的模型，并不能自动带来更可靠的智体表现。近期三项研究成果确立了这一规律。Bölük (2026a) 仅修改“编辑工具”的格式及其周边的工具驾驭机制（Tool Harness），而未对模型本身做任何改动；结果显示，在针对15个不同模型进行的编程基准测试中，其性能提升幅度最高竟达10倍之巨。Trivedy (2026) 在 Terminal-Bench 2.0 基准测试中，将一个固定的 GPT-5.2-Codex 智体性能从 52.8% 提升至 66.5%；这一提升完全归功于对系统提示词的重构、中间件层面的上下文注入，以及引入“自我验证钩子”等基础设施层面的调整——在未触及模型本身的情况下，实现了高达 13.7 个百分点的性能跃升。<strong>Meta-Harness</strong> (Lee et al., 2026) 项目通过自动化驾驭机制优化手段，在 Terminal-Bench-2 测试中取得了 76.4% 的成绩；在同样未修改模型权重的前提下，这一成绩超越了所有由人工精心调校的优化方案。在上述每一个案例中，作为变量的均是“执行驾驭机制”（即负责管理上下文构建、工具交互、任务编排、反馈机制及执行约束的基础设施层）；而作为对照组的模型本身则始终保持固定不变。值得注意的是，这些仅凭优化驾驭机制所取得的性能提升，均显著超越在同一基准测试中被视为“模型重大进步”的典型提升幅度（通常仅为 2 至 4 个百分点）。这一规律绝非偶然：真正决定最终成效的，并非模型本身，而是其背后的驾驭（harness）机制。</p>\n<p>这一规律称之为“约束瓶颈论”（Binding-Constraint Thesis，Bölük, 2026b）：针对那些在同类前沿模型之间进行横向对比的长周期任务评估而言，基准测试结果的差异性，可能在很大程度上既取决于模型本身，也同样取决于其所搭载的执行驾驭机制。</p>\n<p>如图3 形象地展示这一历史性的范式转变：早期的智体系统往往将核心能力高度集中于单一的模型循环（Model Loop）之中；而近期的系统则日益揭示出这样一个事实——智体的可靠性问题，本质上是一个横跨各层级的基础设施层面的系统性问题。</p>\n<p><img alt=\"\" src=\"https://pic1.zhimg.com/v2-7e24c07854993338b15d1d455b5df086_1440w.jpg\" /></p>\n<h2>智体系统的演进</h2>\n<p>从早期的“思维链提示”（Chain-of-Thought Prompting）到自主智体，这一发展轨迹可以被理解为从业者必须管理的“工程界面”在逐步扩展的过程。</p>\n<p>ReAct 时代（2022–2023）。Yao（2023）确立“观察-思考-行动”（Observe-Think-Act）循环作为智体系统的基础原语。早期的系统仅依靠极简的基础设施运行：一个 <code>while</code> 循环、一个提示模板以及一个简易的工具分发表。AutoGPT 和 BabyAGI 通过将语言模型的调用封装在任务队列、记忆模块和工具分发机制之中，展现实现完全自主运行的雄心；与此同时，它们也将“执行失控”、“上下文溢出”、“状态丢失”以及“未受监控的副作用”等故障模式，从单纯的“提示工程问题”转化为显性的“基础设施问题”（Significant Gravitas, 2023; Nakajima, 2023）。</p>\n<p>工具集成与多智体协作（2023–2024）。Gorilla、ToolLLM 和 Toolformer 等项目证实，工具的使用能力是可以被模型通过学习或引导而获得的，而非必须硬编码进固定的 API 封装器中（Patil et al., 2024b; Qin et al., 2024; Schick et al., 2023）。CAMEL、ChatDev、MetaGPT 和 Mixture-of-Agents 等项目引入丰富多样的多智体协作模式，其形式涵盖从“角色扮演式对话”到“模拟软件开发组织”乃至“分层智体聚合”等多种类型（Li et al., 2023a; Qian et al., 2023a; Hong et al., 2023; Wang et al., 2024）。随着 SWE-bench、AgentBench、WebArena 和 GAIA 等基准测试平台的问世，智体系统的评估基础设施日趋成熟（Jimenez et al., 2024; Liu et al., 2023a; Zhou et al., 2024; Mialon et al., 2023）；与此同时，以 Anthropic 提出的 MCP 协议和 Google 提出的 A2A 协议为开端，智体领域的协议标准化工作也正式启动（Anthropic, 2024c; Surapaneni et al., 2025）。</p>\n<p>“驾驭（harness）与管理”的转折期（2025–2026）。截至2025年，业界已积累了足够的部署经验，足以确凿地证明：制约智体（Agent）可靠性的关键瓶颈在于基础设施质量，而非模型质量。2026年初发生的三个独立进展进一步印证这一趋势的转变：OpenAI 正式将“驾驭工程”（Harness Engineering）确立为一门独立学科；斯坦福大学与麻省理工学院联合推出的 Meta-Harness 项目证实，自动化的 Harness 优化方案其成效优于人工工程调优；此外，LangChain 的 DeepAgents 项目仅通过对 Harness 层进行调整，便在 Terminal-Bench 2.0 基准测试中将性能得分从 52.8% 提升至 66.5%——这相当于实现了 13.7 个百分点的绝对增长，以及约 26% 的相对提升（OpenAI, 2026a; Lee et al., 2026; Trivedy, 2026）。</p>\n<h2>三个工程阶段</h2>\n<p>2022年至2026年这一时期，该领域所选择的工程实践呈现出一种连贯的、分三个阶段演进的脉络，如图 1所示。</p>\n<p><img alt=\"\" src=\"https://pic3.zhimg.com/v2-2a6988a2995b714b0438d850d45c7592_1440w.jpg\" /></p>\n<p><strong>提示工程</strong>（2022–2024）。这一阶段的主要着力点在于输入提示文本（prompt text）。从业者通过精心设计更优质的指令、少样本示例（few-shot examples）以及推理模板来进行优化。当时的工程范畴较为狭窄：仅限于优化针对单次模型调用的单一文本输入。</p>\n<p><strong>上下文工程</strong>（2025）。随着智体（agents）的运行周期变得更长，其面临的“瓶颈约束”随之发生了转移——从最初的“输入内容是什么？”，转变为“在每一步操作中，模型应当看到哪些信息？”这一阶段的核心在于上下文管理：即在每一轮交互中应注入何种信息、如何检索并压缩记忆数据、如何依据相关性对工具执行结果进行排序，以及如何应对上下文窗口（context window）达到容量上限的问题。工程范畴由此前的单一输入，扩展至对流入上下文窗口的多个信息流进行统筹管理（Anthropic Applied AI Team, 2025）。</p>\n<p><strong>驾驭工程</strong>（2026）。随着模型能力的日益增强，足以胜任长期运行的任务，系统的可靠性也愈发依赖于其外部的“基础设施驾驭层”（harness wrapper）。该驾驭层负责维护系统状态、协调工具调用、注入反馈信号、强制执行约束条件，并对任务进度进行核验。这一观察结果与“瓶颈约束”理论的观点不谋而合：即具备长周期执行能力的智能体，其优异表现并非仅凭模型自身所能达成，而是源于模型与外部驾驭层（harness）协同耦合所构成的整体系统（Bölük, 2026b）。因此，“驾驭工程”所探讨的核心问题在于：为了确保智体系统的可靠运行，必须在模型外围构建怎样的治理机制、约束条件、反馈回路以及执行控制策略？在所构建的分类体系中，这一阶段将ETCLOVG框架下的全部七个层级视为一个不可分割的有机整体（OpenAI, 2026a; Böckeler, 2026; LangChain, 2026b）。</p>\n<p>这三个阶段之间存在着层层包含的关系：驾驭（harness）工程涵盖上下文（context）工程，而上下文工程又涵盖提示（prompt）工程。此外，这三个阶段在时间与概念上均存在相互重叠之处，并非以泾渭分明的界限彼此割裂、依次更替。时至今日，提示工程依然是驾驭工程实践中不可或缺的活跃组成部分；与此同时，上下文工程也在持续演进与成熟，并与驾驭层级相关议题并行发展。因此，这一阶段的划分，更恰当的解读应是：工程领域内“边际工程投入”重心的转移，而非旧有工程范式的彻底废弃与更迭。</p>\n<h2>ETCLOVG 七层分类体系</h2>\n<p>针对智体驾驭工程（agent harness engineering）提出一套七层分类体系。简称为 ETCLOVG，该缩写分别代表：执行（Execution）、工具（Tooling）、上下文（Context）、生命周期（Lifecycle）、可观测性（Observability）、验证（Verification）以及治理（Governance）。前图 4 提供该体系的精简可视化概览。如图 2 所示：</p>\n<p><img alt=\"\" src=\"https://pica.zhimg.com/v2-4fb10df89f462b90ea2b9966a62e486e_1440w.jpg\" /></p>\n<p>前四层描述驾驭的核心结构。执行层（E）决定智体代码的运行环境，以及对其进行约束的沙箱限制；工具层（T）规定外部能力的描述、发现及调用方式；上下文层（C）控制模型在短期、会话级及持久化时间跨度内所能感知的信息范围；而生命周期层（L）则组织负责读取与写入上述状态的控制流——其涵盖范围从单智体循环，延伸至多智体协作及“从问题（Issue）到拉取请求（Pull Request）”的完整工作流。余下的三层则描述围绕该核心结构构建的控制平面。可观测性层（O）负责捕获追踪日志、成本数据、故障信息及可靠性信号；验证层（V）将任务执行情况与追踪日志转化为评估指标、故障归因分析及回归测试反馈；而治理层（G）则通过权限管理、身份认证、策略约束、安全加固、审计追踪及人工监督等机制来规范智能体的行为。</p>\n<p>该分类体系的两项设计抉择使其独树一帜。首先，将“可观测性”（O）提升为一个独立的层次，而非将其仅仅视为生命周期钩子（lifecycle hooks）所产生的附带效应。在生产级系统中，可观测性拥有其专属的工具生态体系（例如 Langfuse、Arize Phoenix、OpenLLMetry）以及独特的工程实践（例如 OpenTelemetry 埋点检测、成本归因分析、异常检测等），因此理应作为独立层次进行专门探讨。其次，将“治理”（G）确立为一个“一级”层次，旨在全面涵盖安全与合规领域的各类关切；该层次进一步细分为三个子层级：模型级（涵盖防护栏机制、内容过滤等）、系统级（涵盖网关、智体、权限模型等）以及组织级（涵盖审计追踪、合规管理、人工介入监督等）。</p>\n<p>状态管理（State management）自然应归属于“生命周期与编排”（L）层级之中，并与负责读取及写入该状态的执行流紧密相伴；生命周期钩子与策略强制执行应归属于“治理”（G）范畴，在此处，它们能与其他约束机制保持协调一致。</p>\n<p>构建该语料库，旨在对公开文档中记载的“智体-驾驭”（agent-harness）相关制品进行系统性映射；在此过程中，借鉴系统综述的研究规范，明确披露数据来源渠道、检索策略及筛选流程（Page et al., 2021）。如图5所示，候选对象主要通过四类渠道进行收集：既往的综述论文与基准测试论文；基于GitHub平台、针对项目名称、描述、README文档、主题标签、星标数、更新时间及归档状态等字段进行的可复现式检索；经过人工整理的项目列表与软件包注册库；以及各类公司工程博客或发布说明中涉及驾驭级机制的内容（Meng et al., 2026; Jimenez et al., 2024; Liu et al., 2023a; Zhou et al., 2024; GitHub, 2026; OpenAI, 2026a; Anthropic, 2025d; LangChain, 2026b）。典型的检索关键词组合涵盖“agent harness”（智体驾驭）、“coding agent”（编程智体）、“LLM agent sandbox”（大语言模型智体沙箱）、“MCP server”、“agent observability”（智体可观测性）、“agent memory”（智体记忆）、“agent evaluation”（智体评估）以及“agent governance”（智体治理）等术语。对于每一个最终入选的候选对象，均记录其项目名称、URL链接、制品类型、来源类型、可用性状态、可识别的发布年份、可获取的GitHub元数据，以及用于后续ETCLOVG编码分析的公开佐证材料。</p>\n<p><img alt=\"\" src=\"https://pic4.zhimg.com/v2-633f163844cf8f4a2054b263dc726a69_1440w.jpg\" /></p>\n<hr />\n<h2>1 范围与概念</h2>\n<h3>定义</h3>\n<p>智体的执行环境是指智体动作得以物理执行的基础设施层。在大语言模型（LLM）智体的语境下，执行环境与沙箱是紧密耦合的概念。因此，生产级的智体系统几乎无一例外地会在沙箱环境中执行动作。</p>\n<h3>为何沙箱技术在“智体时代”居于核心地位</h3>\n<p>在智体（Agent）时代，沙箱技术不仅仅是一项沿袭自传统多租户代码执行环境的安全措施。它同时承载着三个截然不同的目的；正是这三者的结合，将沙箱技术从一个单纯的操作性细节，提升为智体驾驭设计中的一项“头等大事”。</p>\n<p>首要目的是安全性。智体沙箱所面临的挑战，已超越传统多租户代码执行环境的范畴。由大语言模型（LLM）生成的代码，在规模化应用时既难以审计，也缺乏可预测性，这使得“静态代码审查”这一传统的首要防御手段变得不再适用。此外，智体往往需要跨越多个步骤自主执行任务，这意味着在实际执行动作的当下，无法进行人工干预。恶意利用“提示注入”（Prompt Injection）攻击，可以将原本无害的智体劫持，使其沦为针对沙箱环境发起攻击的载体，从而模糊“可信的用户意图”与“恶意的输入指令”之间的界限。近期关于“沙箱逃逸”（Sandbox Escape）现象的实证研究表明，上述担忧绝非空穴来风。</p>\n<p>第二个目的是可复现性。针对智体的“长周期任务”，以及用于评估此类任务的驾驭，均要求系统具备将执行状态重置回某一“已知基线”的能力。Docker容器或微型虚拟机（MicroVM）可以根据需求随时销毁并重建，而开发者的物理工作站则无法做到这一点；正是凭借这一特性，诸如 SWE-bench (Jimenez et al., 2024) 和 OSWorld (Xie et al., 2024) 等基于沙箱技术的评估基准，才得以具备实际的可行性。在模型训练阶段——即单个任务可能在多条并行轨迹上被反复重演数百次之时——若缺乏一种低成本的状态重置机制，其本身便会成为制约系统扩展性的一个瓶颈。</p>\n<p>第三个目的是“活跃性”（Liveness），这也是智体时代所独有的一个特定需求。若缺乏沙箱环境的保护，智体所试图执行的每一个潜在高风险动作——例如写入文件、安装软件包或发起外部网络调用——都必须先通过显式的权限提示窗口，交由人类用户进行审批放行。在规模化应用场景下，这种模式往往会导致两种失效后果：要么用户因不堪其扰而彻底放弃使用该智体；要么用户出于惯性或麻木感而对所有权限请求一律“反射性”地点击批准，从而彻底消解权限提示机制原本旨在提供的安全防护意义。沙箱技术通过划定一个明确的“边界区域”——在此区域内，智体被授权可以自由地执行操作——从而打破上述僵局；它将权限管理的重心，从针对“具体动作”的逐一审批，转移到了针对“整个会话周期”的统一配置。据 Anthropic 公司报告显示，在其 Claude Code 产品中引入沙箱技术后，在确保安全性的前提下，成功将权限审批提示的出现频率降低84% (Anthropic, 2025b)。在这一框架下，沙盒既是一座“牢笼”，亦是一份“许可”；而正是这“许可”的一面，才从根本上促成自主且长周期的任务执行。</p>\n<p>在这三重目的中，安全性是与传统沙盒技术所共有的；可复现性在智体（Agent）语境下得到显著增强；而“活性”（Liveness）则是一个本质上全新的概念。正是这三者的有机结合，赋予将“智体沙盒”视为一个独立研究对象——而非仅仅作为容器技术之某种下游应用——的充分理据。</p>\n<h2>2 智体沙箱的分类</h2>\n<p>在2024至2026年间，智体沙箱基础设施已从最初的一小部分通用运行时，演化为如今的多个截然不同的产品类别，且每个类别均针对特定类型的智体任务进行优化。依据“工作负载”与“应用场景”这两个维度对这一生态格局进行划分，将其归纳为七大类别；对于需要在众多系统中进行选型的沙箱集成设计者而言，这一分类方式最具参考价值。至于另一个正交维度——“隔离技术”（涵盖容器、gVisor [Google, 2018] 等用户空间内核、Firecracker [Agache et al., 2020] 与 Kata Containers [Kata Containers Project, 2017] 等微虚拟机、WebAssembly，以及 bubblewrap 和 Seatbelt 等操作系统级原语），并未将其列为顶级分类，而是将其作为一种设计属性进行探讨；究其原因，在于同一套隔离原语往往会被复用于多种不同的工作负载之中。这七大类别具体包括：通用托管沙箱、计算机操作型智体基础设施、代码专用型沙箱、框架集成型运行时、浏览器评估环境、操作系统级权限沙箱，以及沙箱抽象层。</p>\n<h2>3 威胁模型与沙箱逃逸</h2>\n<p>对智体（Agent）执行过程进行沙箱化处理，不仅面临传统的容器级威胁（如容器逃逸、侧信道攻击、资源耗尽），还面临一类智体特有的威胁类别，这些类别往往会放大传统的安全隐患。首先，提示注入（Prompt Injection）攻击允许外部输入——例如检索的网页内容、工具的响应结果或文件内容——劫持智体的行为，进而触发恶意的沙箱操作。其次，目标失调（Goal Misalignment）可能导致智体本身将“主动尝试逃逸沙箱”视为实现其最终目标的一个必要中间子目标。第三，组合放大（Compositional Amplification）效应发生于智体拥有多重工具访问权限之时；在这种情况下，沙箱环境中的单一弱点可能会在智体集成的各项能力之间产生连锁反应，从而被放大利用。</p>\n<p>尽管针对智体环境下的沙箱逃逸现象，目前尚缺乏大量的实证数据，但现有证据已足以引发高度关注。由 Marchand（2026）提出的 Sandbox-EscapeBench 基准测试工具，在一个嵌套式沙箱“夺旗赛”（Capture-the-Flag）环境中对前沿的大语言模型（LLM）进行评估；结果显示，针对基于 Docker 的容器环境，其沙箱逃逸的成功率在 15% 至 35% 之间浮动，具体数值取决于容器的配置方式。该基准测试涵盖多种沙箱逃逸机制，包括配置错误、权限分配失误、内核漏洞，以及运行时或编排层面的安全弱点；测试结果确凿地证明，即使以当前模型的智能体能力水平来看，沙箱逃逸威胁也绝非仅仅停留在理论层面，而是已经成为切实存在的现实风险。相比之下，针对此类威胁的防御性研究目前仍处于起步阶段。Wu（2025）提出的 IsolateGPT 方案，针对基于 LLM 的智体系统构建一种执行隔离架构；据其报告，该方案在有效防范跨应用数据泄露的同时，针对四分之三的测试查询，其所产生的性能开销均控制在 30% 以下。Yan（2025）提出的事务性沙箱（Transactional Sandboxing）方法则提供一种基于“回展（rollback）”机制的防护手段；据其报告，该方法产生的性能开销约为 14.5%，且针对高风险指令具有极高的拦截成功率。Cheng（2026）提出的“LLM-in-Sandbox”方案则提供一种互补性的视角；该方案主张，构建功能“极简”而非“全能”的沙箱环境，有助于同时缩小攻击面并降低智体运行过程中不必要的复杂性。</p>\n<p>综合上述研究成果不难发现，当前在“进攻”与“防御”两类研究进展之间存在着显著的差距。在“进攻”层面，研究人员已成功构建出具体且可复现的基准测试工具；而在“防御”层面，相关研究工作仍呈现出碎片化的状态，仅散见于各类孤立的原型系统中，且各系统在威胁模型、评估协议及基线假设等方面均存在差异。因此，构建一套针对智体环境原生设计的运行时安全驾驭——即能够在一套统一的评估方法论下，系统性地解决提示注入、目标失调及组合放大等核心安全挑战——已成为当前亟待探索的开放性研究方向。</p>\n<h2>4 部署模式</h2>\n<p>Agent 沙盒基础设施的部署模式已日趋多样化，不再局限于最初的自托管 Docker 模式。在当前的实践中，主要存在三种模式并存的局面。在自托管模式下，开发者直接管理沙盒基础设施，这也是 OpenHands 和 SWE-agent 所采用的默认模式。在云端（SaaS）模式下，沙盒即服务（Sandbox-as-a-Service）提供商负责基础设施的管理，E2B、Modal 和 Daytona Cloud 便是此类模式的典型代表。而在混合模式或“自带云”（BYOC）模式下，Agent 的逻辑层与沙盒的执行层在不同的环境中实现解耦；此类模式的案例包括 OpenHands SDK 中的“本地工作区”与“远程工作区”抽象（Wang et al., 2025c），以及 E2B 和 Northflank 所提供的 BYOC 服务。</p>\n<p>推动这些模式演进的背后，是两种相互补充的驱动力量。一方面，从业者的反馈报告往往从延迟、安全性和可扩展性这三个维度来权衡部署方案的选择（Anthropic, 2025b;f）。自托管沙盒能够提供最低的延迟和最紧凑的迭代周期，但也意味着需要承担全部的运维负担；云端沙盒则呈现出一种截然不同的权衡取舍——它以增加网络往返延迟为代价，换取弹性伸缩的能力和托管式的安全保障；而混合模式则试图在将敏感数据保留在本地的同时，将具体的执行任务委托给外部资源。另一方面，数据驻留地要求、合规性以及可审计性等组织层面的约束条件，往往会促使部署方案向混合架构倾斜——即便从延迟和可扩展性的角度来看，单一模式的解决方案可能显得更为理想。</p>\n<p>在实际观察的应用场景中，自托管沙盒主要占据了交互式开发和单租户场景的主导地位；相比之下，云端沙盒则在多租户环境及大规模部署场景中更为普及。混合模式正作为一种新兴方案崭露头角，尤其适用于那些既有合规性或数据本地化要求，又需要大规模、瞬时性执行资源池支持的特定场景。目前，针对这些不同部署模式在实际 Agent 工作负载下的系统性实证对比研究尚属空白。</p>\n<hr />\n<p>工具接口与协议层（T）是 ETCLOVG 的第二大支柱。</p>\n<p>工具接口与协议层定义智体（Agent）如何发现自身能力、如何表征可调用的交互功能（affordances），以及如何在异构的运行时边界之间执行动作。在实践中，这一层恰好处于两个相互竞争的目标之间的交汇点：一方面要通过暴露更多工具来扩大能力覆盖范围；另一方面又要通过控制动作空间和提示词（Prompt）的占用规模，来确保决策的质量。来自生产级智体系统的最新工程实践指南反复指出：过于庞大的工具菜单不仅会降低系统的可靠性，增加 Token 的消耗开销，还会放大规划过程中产生的错误（Anthropic, 2025d; OpenAI, 2026c）。</p>\n<p>这一层面的工作划分为四个相互补充的方向：协议与接口标准；工具的描述、发现与选择；基于工具增强的模型训练与集成；以及系统的可扩展性与会话管理。</p>\n<h2>1 协议与接口标准</h2>\n<p>MCP 已成为针对编程和企业智体（Agent）领域最引人注目的工具集成基础层。它采用明确的主机-客户端-服务器（Host-Client-Server）架构，并基于 JSON-RPC 实现了工具、资源和提示信息的类型化交换（Model Context Protocol, 2025b;c;a）。MCP 的实际价值不仅在于实现模式层面的互操作性，更在于提升了生态系统的流动性：智体构建者可以复用日益扩充的服务器目录，而无需为每一次部署单独开发定制化的连接器。</p>\n<p>A2A 协议所针对的边界虽与上述领域不同，但二者紧密相邻。A2A 并非仅将工具暴露给单一的智体进程，而是致力于标准化“黑盒式”智体应用之间的通信，其功能涵盖通过“智体卡片”（Agent Cards）进行服务发现、支持同步与流式交互，以及实现长周期任务的协同处理（A2A Project, 2025）。近期的协议调研报告将 MCP 与 A2A 定位为互补关系：MCP 主要负责工具与上下文的访问，而 A2A 则专注于智体间的任务委托与协作（Ehtesham et al., 2025）。一种更为有效的归类原则是：应依据工具/接口标准所跨越的“集成边界”来对其进行划分，而非单纯依据其所属厂商或发布时间线。基于这一视角，可以识别出四类边界：模型（Model）↔ 函数（Function）（即结构化调用）、智体（Agent）↔外部能力（即运行时与工具的解耦）、智体 ↔ 智体（即跨进程的任务委托），以及智体 ↔代码库/环境（即版本控制下的策略管理）。下表 1 总结这一分类观点，并明确阐释为何几组常被拿来相互比较的标准（例如 MCP 与 A2A，或函数调用机制与 OpenAPI）在整个集成体系中实际上各司其职、互不重叠。</p>\n<p>在此层级中，函数调用模式（Function-calling schemas）与 API 描述标准依然构成了最基础的构建模块。OpenAI 风格的函数调用机制通过 JSON 模式（JSON schema）以及明确的“调用-返回”交互回合，实现了工具调用的具体落地（OpenAI, 2026c）；而 OpenAPI 则提供了一种与特定编程语言无关且机器可读的 API 契约规范，许多智体框架均以此为基础，自动生成并验证所需的工具接口（OpenAPI Initiative, 2025）。此外，诸如 <a href=\"https://link.zhihu.com/?target=http%3A//agents.md/\">AGENTS.md</a> 和 <a href=\"https://link.zhihu.com/?target=http%3A//agent.md/\">AGENT.md</a> 之类的仓库级指令文件，提供一种轻量级的替代方案，可将工具的使用方式及工作流约束直接编码于版本控制系统中，从而降低了代码智体（Code Agents）的配置门槛（agentsmd, 2025; agentmd, 2025）。</p>\n<h2>2 工具描述、发现与选择</h2>\n<p>一旦协议定义如何发起调用，接下来的瓶颈便在于：在每一个步骤中，应当呈现并选择哪些工具。越来越多的研究工作致力于探讨工具文档的质量、检索机制以及动态候选工具剪枝等问题。EASYTOOL 项目深入分析如何从庞大的工具库中挑选出合适工具所面临的挑战（Yuan et al., 2025）。AnyTool 和 CRAFT 则专注于通过自动构建或优化工具使用流程（pipelines），从而减轻人工指定工具参数的负担（Du et al., 2024; Yuan et al., 2023）。MetaTool 采用基准测试式的评估方法，其结果表明，工具的检索质量与调用质量在不同的应用领域及查询形式之间，往往存在显著的差异（Huang et al., 2023）。近期涌现的 MCP-Zero、ToolRet 和 ToolRegistry 等研究工作进一步强调，具备检索意识的工具编排策略以及高质量的工具注册表，是决定下游智体（Agent）能否成功执行任务的首要关键因素（Fei et al., 2025; Shi et al., 2025b; Ding, 2025）。与此紧密相关的一个研究方向，是将工具选择的范畴扩展至“可复用技能”层面；在此模式下，智体必须识别并调用相关的程序性模块，而不再局限于仅调用精简的 API 接口定义（schemas）。SkillRouter (Zheng et al., 2026) 和 SkillRet (Cho et al., 2026) 两项研究均针对这种大规模的技能选择问题展开了探索，并着重指出了从庞大且存在重叠的技能库中精准检索出所需技能的必要性。</p>\n<p>在系统设计层面，上述研究成果印证了两项核心设计原则。首先，“少而精的工具集”往往比“无差别地罗列所有工具”（即暴力式工具暴露）能取得更优异的性能表现；究其原因，这种精简策略既降低了提示词（Prompt）的熵值，又减少了规划器（Planner）在决策时所需考量的分支数量。其次，工具发现流程必须具备自适应能力：那种僵化且全局统一的静态工具列表，将难以适应工具库快速迭代演进的现状，也无法满足多租户企业级部署环境下的复杂需求。</p>\n<h2>3 工具增强训练与集成</h2>\n<p>第三个研究方向将关注点从“运行时工具编排”转向“模型能力习得”这一层面。Toolformer 项目通过演示一种自监督增强学习范式，成功展示模型如何学会判断在生成文本的过程中何时以及如何恰当地插入 API 调用指令（Schick et al., 2023）。Gorilla 以及 ToolLLM/ToolBench 等后续研究工作进一步拓展了这一研究思路，通过引入规模更为庞大的工具语料库、构建专门的指令微调（Instruction-tuning）流程，并针对 API 的实际调用过程实施以执行结果为导向的监督训练，从而持续提升了模型的工具使用能力（Patil et al., 2024b; Qin et al., 2024）。 ToolkenGPT 和 CREATOR 探索了token级或控制器式（controller-style）的集成方案，旨在提升调用格式化的保真度及规划过程的稳定性（Hao et al., 2023; Qian et al., 2023b）。</p>\n<p>在实际的生产应用框架中，模型侧的这些技术进步通常会与驾驭级的运行时栈（如 LangChain、Semantic Kernel 和 smolagents）相结合；这些运行时栈负责提供记忆抽象、路由中间件以及工具适配器等功能（LangChain, 2026c; Microsoft, 2026; Hugging Face, 2026）。在代码智体（coding agent）的应用场景下，还会涌现出第二类更具语义特性的工具：静态分析器、类型检查器、基于求解器的验证器、证明助手，以及补丁等价性验证或故障定位工具。Ugare 与 Chandra (2026) 将这一领域定义为“智体式代码推理”（agentic code reasoning）：即智体通过探索代码仓库，对代码的行为进行推理，而无需实际执行这些代码。他们提出的半形式化推理方法介于非结构化的“思维链”（Chain of Thought）与完全形式化的验证技术之间；该方法强制智体明确陈述前提假设、追踪程序执行路径，并推导出显性的结论，从而有效提升了补丁等价性验证、故障定位以及代码问答任务的性能。对于工具层而言，这意味着自动化推理工具在返回结果时，应当提供带有佐证依据的产物——例如执行轨迹、证明义务（proof obligations）、反例或结构化的证明证书——而非仅仅给出非黑即白的“是/否”式回答。综合各项证据来看，工具的实际效能是由预训练与微调信号、接口模式（schema）的质量，以及运行时工具选择策略这三大要素共同决定的；若仅针对其中某一单一组件进行优化，所能获得的性能提升往往十分有限。</p>\n<h2>4 可扩展性与会话管理</h2>\n<p>在处理长期运行任务时，会话管理（session management）往往是一个反复出现的运营挑战。虽然有状态的工具会话有助于提升任务的连续性，但也随之增加了状态维护（bookkeeping）的复杂性，尤其是在工具调用被并行化处理或跨多个智体进行委托执行的场景下。常见的故障模式包括：句柄失效（stale handles）、在重​​试过程中工具状态出现不一致、以及因工具执行轨迹过于冗长而导致上下文窗口（context window）溢出。因此，若要设计出高效稳健的应用框架，必须具备针对工具会话的显式生命周期控制机制、对工具上下文注入进行边界限制的能力，以及能够将错误归因于规划逻辑层面还是接口/协议层面的可观测性钩子（observability hooks）。</p>\n<p>最后，对整个生态系统层面的资源与格局进行梳理与归纳，有助于开发者快速掌握当前可用的协议及工具选项；然而，若要针对具体的部署场景来决策究竟应采用哪种协议或工具路由策略，基于基准测试（benchmark）的实证对比分析依然是不可或缺的关键环节。</p>\n<hr />\n<p>上下文与记忆管理（C）是提示与上下文工程同 Harness 工程交汇的层级。</p>\n<p>这一层级决定模型在执行的每一个步骤中能够“看到”哪些信息，以及知识如何在不同的交互轮次和会话之间得以留存。其核心工程难题表述起来很简单：在每一个步骤中，只向模型提供恰好所需的信息，不多也不少。若提供的上下文过少，智体（Agent）将因缺乏必要的状态信息而无法做出正确的决策；若提供的上下文过多，模型的性能便会随之下降——这种性能退化不仅是可量化的，在不同模型之间具有一致性，且其背后的深层机制目前尚未被完全理解。</p>\n<h2>1 为什么必须对上下文进行工程化设计</h2>\n<p>仅仅扩大上下文窗口并不能解决“记忆”问题。要理解其中的原因，需要深入审视其底层架构。</p>\n<p>二次方的注意成本。Transformer 模型的自注意机制需要计算上下文窗口内每一个token之间的两两关联（Vaswani，2017）。对于包含 n 个tokens的上下文，该机制会产生 n² 个两两配对的权重；这意味着计算资源与记忆资源的消耗都会随着上下文长度的增加呈二次方增长。尽管 FlashAttention 和位置编码插值等工程技术能够降低常数系数，但这种二次方增长的结构性特征是其架构本身所固有的。将上下文长度翻倍，其成本并非仅仅翻倍，而是会暴增至四倍。这一特性使得上下文窗口成为一种稀缺资源。</p>\n<p>U 形注意力曲线。仅凭架构层面的成本考量，尚不足以解释为何模型在处理长上下文时会发生性能退化——即便在计算资源充裕、成本可控的情况下也是如此。Liu（2024）提供一项关键的实证结果：在一项涉及 20 篇输入文档的多文档问答任务中，如果包含关键信息的文档被置于上下文窗口的中间位置，模型的准确率会比将其置于开头或结尾位置时下降超过 30%。这种呈“U 形”的性能曲线现象，在各类模型、各类任务以及不同长度的上下文中均普遍存在——即便是在那些专门针对长上下文进行过训练的模型中，这一现象依然成立。这一发现具有直接的现实指导意义：信息在上下文中的“摆放位置”与信息本身的“存在与否”同样重要。如果一个智体（Agent）虽然成功检索到了正确的内容，却未能将其妥善地置于上下文的恰当位置，那么此次检索工作所带来的实际收益将微乎其微。</p>\n<p>大规模上下文腐化​​。Hong（2025）在严格受控的实验条件下，对包括 GPT-4.1、Claude Opus 4、Gemini 2.5 和 Qwen3 在内的 18 款前沿大模型进行综合评估。实验环境经过精心设计，旨在将“输入文本长度”这一变量与“任务难度”这一变量隔离开来，从而进行独立考察。结果显示，随着输入文本长度的增加，所有参评模型的性能均出现了不同程度的退化。这种性能退化并非均匀发生，且具有显著的任务特异性。具体而言，在处理那些具有语义歧义的查询（即包含关键信息的段落与提问语句之间不存在词汇层面的直接匹配）时，模型的性能退化尤为剧烈；相比之下，在处理那些存在词汇层面精确匹配的查询时，性能退化则相对和缓。这一对比结果揭示一种复合式的故障模式：模型不仅难以在长文本中准确定位出关键信息，即便在勉强定位到信息之后，也往往无法基于这些信息进行有效的逻辑推理。此外，这种性能退化现象往往早在上下文窗口被完全填满之前就已经开始显现。例如，一款额定支持 20 万个tokens的模型，其性能可能早在输入长度达到 5 万个tokens时就已经出现了显著的衰减。Hong（2025）将这种现象命名为“上下文腐化​​”（Context Rot），并指出这绝非一种罕见的边缘案例，而是一种普遍存在的系统性问题。对于任何需要在多个步骤中累积工具执行结果、中间推理过程及文件内容的智体（Agent）而言，这都是一种常态化的运行工况。</p>\n<p>综合来看，这些结果确立一个事实：上下文管理绝非可有可无的“事后补丁”。关于“包含什么内容”、“置于何处”以及“何时移除”的每一个决策，都会对智体的可靠性产生直接影响。</p>\n<h2>2 从提示工程到上下文工程</h2>\n<p>从提示工程（Prompt Engineering）向上下文工程（Context Engineering）的演进，体现了其作用范畴的扩展（Karpathy, 2025）。提示工程旨在针对单次模型调用，优化其主要呈静态特性的文本输入。在视觉领域，这一框架同样适用于连续可学习的tokens：视觉提示微调（Visual Prompt Tuning）通过在冻结的视觉Transformer模型的图像块序列前预置一小组可训练向量，使其适应下游任务，且仅需更新不到1%的模型参数（Jia et al., 2022; Xiao et al., 2025）。无论是文本模态还是视觉模态的变体，它们都共享提示工程的核心特征：即针对单一输入模态所进行的、固定且单次生效的优化。相比之下，上下文工程旨在针对跨越多个步骤的任务，在每一个推理环节中，对模型所能获取的全部信息状态进行优化。</p>\n<p>Anthropic公司的应用AI团队将其定义为：“一套旨在大语言模型（LLM）推理过程中，策划并维护最优token集合的策略体系——该集合不仅包含提示语本身，还涵盖了所有可能被纳入其中的其他辅助信息”（Anthropic Applied AI Team, 2025）。由此衍生出的指导原则是：在每一个推理步骤中，寻觅并保留那个“最小化”却“高信息密度”的token集合，从而最大化达成预期结果的概率。“渐进式披露”（Progressive Disclosure）策略主张按需实时加载信息，而非在任务伊始便全盘载入；“信息精简”（Compaction）策略负责移除那些已完成其使命的tokens；而“记忆检索”（Memory Retrieval）策略则仅提取与当前任务关联度最高的历史记录。</p>\n<p>上下文的构成要素：在实际部署的智体中，推理阶段的上下文通常包含以下要素：系统提示语及行为指令、工具的定义与接口规范（Schemas）、此前交互轮次的对话历史、当前任务执行轨迹中产生的工具调用结果、经检索获取的文档或记忆记录，以及任何动态注入的中间工作状态信息。所有这些要素都必须在模型有限的“注意力预算”（Attention Budget）这一稀缺资源池中相互竞逐。因此，上下文工程的核心要义在于：针对每一个推理步骤中的各个组件，做出明确且审慎的取舍决策，而非任由其无节制地堆积膨胀。</p>\n<p>如今，随着相关生态系统的日趋成熟，这种精细化的实践理念已然蔚然成风。如今，各类基础原理手册（Kimai, 2025）及精选综述（Meirtz, 2025）已将“上下文工程”（Context Engineering）视为一门独立的学科。其中，涵盖“活跃上下文窗口”、“会话级状态”及“跨会话存储”的“三层记忆架构”已脱颖而出，成为该领域的主流组织框架。这一架构与操作系统中的经典“存储层次结构”直接对应，不仅提供了直观的理解视角，还确立了一套通用的术语体系，有助于针对不同的时间跨度（即记忆周期），匹配最恰当的技术手段。</p>\n<h2>3 短期记忆：管理活跃上下文窗口</h2>\n<p>短期上下文管理主要负责调控模型在单次推理步骤中，或在同一会话内的连续短序列步骤中，所能“感知”的信息内容。此类决策对模型的行为表现具有最直接的影响；若决策失误，其所引发的成本（包括计算资源消耗及性能损耗）往往也是最高的。</p>\n<p>系统提示词（System Prompt）校准。系统提示词旨在确立智体（Agent）的行为边界与规范，且在每一次调用时都会占用固定的上下文资源配额。Anthropic 应用 AI 团队（2025）将这一设计挑战形象地比喻为寻找恰当的“高度”：若提示词过于具体详尽，往往会导致逻辑僵化且难以维护；反之，若提示词过于笼统模糊，则会让模型因缺乏具体指引而无所适从。在实际应用中，高效的系统提示词通常会被划分为界限清晰的若干部分——例如背景信息、操作指令、工具使用指南及输出格式要求等——并利用 XML 标签或 Markdown 标题进行区隔。推荐的工作流程是：首先在性能最优的模型上使用一份极简的初始提示词；接着通过实证测试找出模型可能出现的失效模式；最后，针对这些具体的短板或漏洞，有针对性地补充相应的指令。若试图预先穷举并涵盖所有的边缘案例，往往只会导致提示词内容过度膨胀，却无法实质性地提升模型的可靠性。</p>\n<p>高效利用 Token 的工具设计。工具定义是消耗上下文资源（Token）的“大户”。每一次模型调用时，工具的架构定义（Schema）、名称、功能描述及参数类型等信息都会被一并注入到上下文中。如果工具集规模庞大，智体甚至在开始处理用户请求之前，仅加载工具定义本身就可能已消耗掉数万个 Token。基于生产环境部署实践所提炼出的核心原则是：相比于提供一份包含大量功能单一、细碎工具的菜单，应优先选用数量较少但功能更为强大、表达能力更强的工具。如果连人类工程师都无法在特定情境下准确判断应使用哪一款工具，自然也无法指望模型能做出更优的判断。理想的工具设计应具备以下特征：功能自洽（Self-contained）、对错误具有鲁棒性（Robust）、且其预期用途必须清晰明确；此外，工具的参数命名应具有充分的描述性，以便充分发挥模型的语义理解优势。</p>\n<p>即时检索（Just-in-time retrieval）与渐进式披露（Progressive disclosure）。高效的智体（Agent）并非预先加载所有潜在的相关信息，而是维护诸如文件路径、存储查询和网页链接之类的轻量级标识符，并随着任务的展开按需加载数据。Anthropic 应用 AI 团队（2025）将这种方法称为“渐进式披露”（progressive disclosure）。这恰好映照了人类专家的工作方式：不会死记硬背整个语料库，而是构建外部索引系统，并根据需要进行检索。在实际应用中，Claude Code 采用了一种混合策略。<code>CLAUDE.md</code> 文件会在会话开始时加载，以提供项目背景信息；而 <code>glob</code> 和 <code>grep</code> 命令则允许智体实时导航并加载特定的文件内容。这种做法既规避“索引陈旧”的问题，又避免因预填充大量数据而产生的成本。环境元数据中也蕴含着隐性信号：文件大小暗示了其复杂程度；命名规范揭示了其用途；时间戳则作为相关性的智体指标。智体能够层层递进地构建对环境的理解，仅在当前活跃的窗口中保留当下必需的信息。</p>\n<p>KV 缓存-觉察的上下文设计。对于生产环境中的智体部署而言，提示词缓存（Prompt caching）无疑是性价比最高的优化手段；而其成效如何，完全取决于上下文的结构设计。在对其生产级智体架构进行五轮迭代之后，Manus 团队将“KV 缓存命中率”确立为“生产阶段 AI 智体最重要的单一指标”；他们指出，在 Claude Sonnet 模型中，缓存命中的 Token成本仅为 0.30 美元/百万 Token，而未命中缓存的 Token 成本则高达 3.00 美元/百万 Token（Manus Team, 2025）。基于这一缓存模型，衍生出三条设计准则。第一，保持提示词前缀的稳定性：系统提示词（System Prompt）开头的哪怕一个 Token 发生变动，都会导致后续所有内容的缓存失效。第二，将上下文视为“仅追加”（Append-only）的数据流：若修改过去执行的动作或观察结果，会因改变了前缀序列而导致缓存无法复用。第三，采用确定性的序列化机制：若在进行 JSON 序列化时，键（Key）的排序顺序不稳定，则会导致原本完全相同的请求因序列化结果不同而无法命中缓存。鉴于工具定义通常位于序列化上下文的前端位置，因此增删工具的操作会导致后续所有交互轮次的缓存内容全部失效。Manus 团队通过引入一种“上下文感知型状态机”解决了这一问题：该状态机在解码过程中通过屏蔽 Token 的 Logits（对数几率值），从而阻止模型选择当前不可用的动作，而非在运行时动态修改工具定义列表（Manus Team, 2025）。 Anthropic 发布的上下文管理功能，通过显式的 <code>cache\\_control</code> 断点，在产品层面实现缓存机制的落地（Anthropic, 2025c）。</p>\n<p>针对短期上下文管理的工具生态系统已日趋成熟，其涵盖范围现已扩展至：包含智体调用间 KV 缓存压缩模式的生产级技能库（Koylan, 2025），以及提供以 MCP为核心原语、用于动态上下文组装的基础设施项目（context-space, 2025）。</p>\n<h2>4 中期：会话状态与跨运行持久化</h2>\n<p>中期上下文管理旨在弥合“当前活跃上下文窗口”与“完整的长期记忆系统”之间的鸿沟。具体而言，其核心问题在于：智体如何在同一会话内的不同轮次之间，或在同一任务的多次运行之间，实现状态的保存与恢复。这一技术的工程价值极高：仅需数百个 Token 的结构化工作状态，便足以在上下文重置（即清空）发生时，帮助智体跨越中断点，避免因上下文丢失而导致此前积累的所有任务进度功亏一篑。</p>\n<p>结构化笔记记录。最简单的中期管理技术是让智体维护一个持久化的笔记文件——通常命名为 <code>NOTES.md</code> 或 <code>todo.md</code>；智体会在每次任务运行开始时读取该文件，并在上下文被清空（重置）之前对其进行更新。Anthropic 应用 AI 团队（2025）通过一个案例生动地展示了这一技术：他们让 Claude 模拟游玩《宝可梦》游戏，历经数千个游戏步骤。在未接收任何关于记忆结构显式指令的情况下，该智体自主构建已探索区域的地图，维护针对特定对手的战斗策略及其成效的统计记录，并在上下文重置发生后依然能持续追踪既定的任务目标。在每一次清空主对话历史记录的“上下文压缩”步骤完成后，智体便会读取其自存的笔记，从而得以在不丧失连贯性的前提下，继续执行长达数小时的训练序列。这一技术的关键洞察在于：笔记记录机制将“工作记忆”进行了外部化处理。智体不再单纯依赖对话历史记录来承载任务状态，而是将关键信息写入外部存储介质中，并根据实际需要随时将其读取回来。</p>\n<p>基于文件的规划与任务外部化。作为“结构化笔记记录”技术的一种扩展，该方法将完整的任务规划表征（planning representation）全部外部化并存储至磁盘中。诸如 <code>planning-with-files</code>（OthmanAdi, 2025）之类的工具，便为编码智体（coding-agent）的工作流提供了相应的技能包，使其能够实现基于文件的持久化任务规划。任务状态、规格参数、中间结果以及依赖关系图等各类信息，均会在任务执行的各个关键里程碑处被写入文件系统；而在下一次任务运行开始时，智体仅会选择性地加载所需信息，从而让那些与当前任务执行并非紧密相关的冗余内容，完全绕过并无需占用宝贵的上下文窗口资源。像 Trellis (mindfold-ai, 2025) 这样的框架扩展了这一模式，将其涵盖至“项目记忆”和“规范注入”领域；它负责管理结构化的项目状态，并根据当前步骤的具体需求进行选择性注入。</p>\n<p>跨运行注入。作为一种互补模式，该技术会捕获前一次运行中的关键输出，并将其注入到下一次运行的起始阶段。像 claude-mem (thedotmack, 2025) 这样的工具充当着插件式的记忆层：它们负责捕获会话历史，从中提取对未来运行最有价值的信息，并将其前置（prepend）到下一个会话的上下文之中。这种做法既无需依赖向量数据库，也无需图存储系统，却能为跨运行过程提供显著的连续性。社区代码库 everything-claude-code (affaan-m, 2025) 拥有超过 15 万个 GitHub Star，堪称目前规模最大的此类模式开源集合；它充分展示了“中层记忆”实践在实际生产环境中的编码智体部署中已达到了何等广泛的应用规模。</p>\n<p>中层记忆的权衡。相比单纯的“窗口内”（in-window）记忆管理技术，中层记忆技术能够提供显著增强的连续性；与此同时，其所需的基础设施成本仅为完整“长期记忆”系统的一小部分。然而，这种技术的局限性在于：信息流只能单向地从前一次运行传递至下一次运行，而无法像在索引存储中那样进行按需检索；此外，面对海量的历史数据，此类技术的扩展性（scalability）表现并不理想。当跨运行的历史数据量变得过于庞大，或者当智体（Agent）需要精确检索某项特定的过往观察记录而非仅仅注入一份摘要时，就必须引入下文即将详述的“长期记忆”系统，以此作为对中层记忆技术的补充。</p>\n<h2>5 长期：持久记忆系统</h2>\n<p>长期记忆系统提供一种经过索引且可检索的存储机制，其存储内容可在不同的会话、任务及智体实例之间持久留存。相较于依赖于“前向注入”摘要的中期记忆技术，长期记忆系统支持“任意回溯”功能：即给定一个查询指令，系统能够检索出与之关联度最高的已存储记忆，而无需顾及其创建的时间。这一层级正是智体随时间推移不断积累经验的场所。</p>\n<h3>基础架构</h3>\n<p>受操作系统启发的层级式记忆。Packer（2023）引入一项关键抽象：将大语言模型（LLM）的上下文窗口视为内存（RAM），将外部存储视为磁盘，并赋予模型函数调用能力，使其能够显式地将信息进行“换入”和“换出”操作。这种与虚拟内存的类比非常精准：正如操作系统通过透明的分页机制为应用程序提供了拥有更大记忆的“幻觉”一样，MemGPT 也通过透明的记忆管理机制，为 LLM 提供拥有更长上下文的“幻觉”。智体（Agent）因此能够处理远超其物理窗口限制的上下文内容，并按需从外部存储中检索相关的历史信息。该论文明确揭示了智体记忆与操作系统记忆之间的内在联系，而这一联系正是随后大多数长期记忆系统的底层基础。</p>\n<p>观察、反思与检索。Park（2023）在构建社会模拟智体的背景下，引入“记忆流”（Memory Stream）架构。该架构将智体的所有观察记录存储为带有时间戳和重要性评分的自然语言文本。在每一个时间步中，一个检索模型会综合考量三个信号——即时性（Recency）、重要性（由智体在写入时自行评分）以及与当前查询的相关性——从而筛选出最相关的记忆片段。该架构还包含第二项机制，即“反思”（Reflection）：它会周期性地对已存储的观察记录进行综合归纳，从中提炼出更高层级的洞察与见解。具体而言，智体会自省：在近期经历中，哪些问题是最为突出的？随后，它会检索相关的记录，并生成相应的结论，最终将这些结论重新存储回“记忆流”中。消融实验结果显示，观察、反思和检索这三个组件均能独立地提升智体行为的质量；若移除其中任何一个组件，都会导致行为表现出现可测量的显著退化。如今，“观察—反思—检索”这一三元组模式，已成为智能体记忆系统设计的标准范式。</p>\n<p>动态遗忘与人格建模。Zhong（2024）在上述检索架构的基础上进行扩展，构建了名为 MemoryBank 的系统，并为其新增了两项功能。第一项功能是受“艾宾浩斯遗忘曲线”（Ebbinghaus Forgetting Curve）启发的动态遗忘机制：记忆的强度会随时间推移呈指数级衰减；若记忆被频繁访问，其强度便会得到强化；而相互矛盾的记忆条目则会被系统进行消解与整合。第二项功能是层级式的用户人格摘要：随着新的交互记录不断累积，系统会每日对这些摘要进行更新与迭代。上述两项核心理念——即自适应的记忆强度管理与用户人格建模——随后在 Mem0 和 Honcho 等系统中得到大规模的落地与应用。</p>\n<h3>生产级记忆系统</h3>\n<p>混合存储与行业应用。Mem0（Chhikara 等，2025）是目前在生产级智体（Agent）中部署最为广泛的长期记忆层，拥有超过 1400 万次 Python 包下载量、4.1 万个 GitHub Star，并已原生集成至 CrewAI、Flowise 和 Langflow 等平台中。其架构结合三种存储后端：用于语义相似度搜索的向量数据库、用于关系建模的图数据库，以及用于快速事实检索的键值存储。一个基于大语言模型（LLM）的提取层负责处理新的交互数据，识别其中的事实与偏好，并将记录路由至相应的存储库。在 LOCOMO 基准测试中，Mem0 的准确率比 OpenAI 的原生记忆系统高出 26%，同时其消耗的 Token 数量却比“全上下文”（Full-context）处理方式减少了 90%（Chhikara 等，2025）。2025 年，亚马逊云科技（AWS）选定 Mem0 作为其 Agent SDK 的独家记忆服务提供商，这一举措标志着 Mem0 已成功从科研原型阶段迈向了生产级基础设施阶段。</p>\n<p>动态知识网络。A-MEM（Xu，2025）借鉴 Zettelkasten（卡片盒笔记法）这一知识管理系统。它并非将记忆存储为扁平化的记录，而是为每一条新记忆生成一份结构化的笔记；这份笔记包含关键词、标签、上下文描述，以及一组指向语义相关记忆的动态链接。当新增一条记忆时，A-MEM 不仅会将其存储下来，还会追溯性地更新现有相关笔记的上下文与属性，从而使记忆图谱能够随着知识的积累而不断演化。这一机制解决了基于检索的记忆系统所面临的一个根本性局限：即某条已存储记忆的关联性，往往在写入之时尚不显而易见，其真正的意义与价值只有结合后续获取的信息进行参照评估时，方能得以彰显。</p>\n<p>从经验中学习。Hindsight（<a href=\"https://link.zhihu.com/?target=http%3A//vectorize.io/\">Vectorize — Agents That Learn From Every Conversation</a>，2025）秉持这样一种理念：大多数记忆系统往往过于专注于“记住”信息，而真正的核心需求其实在于“学习”。其 API 开放三项核心操作：<code>retain</code>（保留），用于存储新信息；<code>recall</code>（回溯​​），用于检索相关的既有记忆；以及 <code>reflect</code>（反思），用于生成一种既考量自身内在倾向（Disposition-aware），又将既有记忆与当前上下文深度融合的响应内容。在内部运作层面，<code>retain</code> 操作会提取时间实体、对重叠的事实进行去重处理，并构建出基于确凿证据、高度整合的知识记录。 Hind-sight 在 LongMemEval 基准测试中取得了最先进的性能表现，这一结果已由弗吉尼亚理工大学 Sanghani 中心的研究人员独立复现。</p>\n<p>推理驱动的个性化。由 Plastic Labs 开发的 Honcho（Plastic Labs, 2025）建立的是用户模型，而非关于用户的单纯事实存储库。其内部包含一条名为“梦境”（dreaming）的异步推理流水线，该流水线在后台持续处理过往的交互数据，从中提取的不仅是显性事实，还包括关于用户偏好、沟通风格及动态演变目标的推断性结论。这种以实体为中心的数据模型支持多智能体环境，即多个智体可同时与同一位用户进行交互：每个智能体都维护着各自独立的观察视角，从而有效防止了信息间的相互干扰（即“交叉污染”）；与此同时，一个共享的用户模型则负责汇聚并整合来自所有交互过程的理解与认知。</p>\n<p>集体记忆。Mozilla AI 推出的 cq（Mozilla AI, 2025）填补其他现有系统所遗留的一项空白：即跨智体实例的共享记忆机制。目前大多数长时记忆系统均采用“单智体专属”或“单用户专属”的模式；这意味着当多个智体协同处理相关任务时，每一个智体实例往往不得不独立地去重新探索并学习其他智体早已掌握的知识。cq 是一项旨在实现智体共享学习的开放标准，它允许智体在整个智体集群（fleet）范围内，对集体知识进行持久化存储、查询及验证。其架构原生支持 MCP（多智体协作协议），并划分为三个运作层级：存储于开发者本地机器的“本地知识”、供团队内部成员访问的“组织共享知识”，以及跨越不同团队边界的“跨团队知识”。该系统采用一种名为“错误后钩子”（post-error hook）的模式：一旦某个智体遭遇错误，cq 便会自动向集体知识库发起查询。这一机制提供一种切实有效的手段，能够防止在组织内部的各类部署环境中，出现针对同一类重复性故障进行各自独立、重复解决的低效局面。</p>\n<h2>6 长期任务技术：确保智体在 100 多轮交互中保持连贯性</h2>\n<p>长期任务——包括大型代码库迁移、跨会话的研究项目以及长周期的自主工作流——则要求同时协调所有三个层级，并针对在特定时刻应采用何种技术做出实时决策。本节将重点介绍在此类规模下所涌现出的集成级技术。</p>\n<p>上下文压缩（Context compaction）。上下文压缩是指对即将达到容量上限的上下文窗口进行归纳总结，随后利用这种经过压缩的、代表了当前累积状态的表示形式，重新启动任务执行流程。Anthropic 应用 AI 团队（2025）详细阐述了在此过程中所面临的校准挑战。在进行总结时，必须保留架构决策、尚未解决的错误（bug）以及具体的实现细节；与此同时，则应剔除那些冗余的工具输出，以及那些其推理结果已被后续步骤所采纳的中间推理过程。推荐的调优工作流始于“最大化召回率”——即从执行轨迹（trace）中尽可能捕获每一条潜在的相关信息；随后通过迭代优化来提升“精确率”——即剔除其中多余的内容。切忌反其道而行之。若过早地剔除过多内容，将导致智能体丢失后续任务所需的关键上下文信息，且这种信息的丢失往往是无法挽回的。工具结果清理（Tool result clearing）是上下文压缩中最轻量化的一种形式：一旦智体已依据工具输出采取了相应的行动，这些完整的工具输出内容便会被替换为精简的路径引用。这项技术可实现持续应用，目前已作为一项产品级功能正式上线于 Anthropic 开发者平台（Anthropic, 2025c）。</p>\n<p>子智体上下文隔离（Sub-agent context isolation）。当某项任务需要对特定子主题进行深入探索时，探索过程本身往往会产生大量的中间上下文信息；尽管这些信息对于完成子任务至关重要，但若将其直接混入主控智体（orchestrator）的视野中，则会干扰其对整体任务局面的把控。子智体架构（Sub-agent architecture）通过将子任务分配给专门的智体来解决这一问题：每个子智体均拥有一个独立的、全新的上下文窗口，且仅向主控智体返回一份篇幅精简（约 1,000 至 2,000 个 token）的探索结果摘要（Anthropic, 2025e）。如此一来，详尽的探索过程所产生的上下文信息便被隔离并局限于子智能体内部；主控智体接收到的仅是经过提炼与浓缩后的最终结果。Manus 团队（2025）对这一模式提出了一种更为精细化的改进方案。针对那些主控智体仅需获取最终输出结果的简单子任务，主控智体与子智体之间将完全不共享任何上下文信息。而针对那些需要共享状态信息的复杂子任务，主控智体的完整上下文信息则会被传递给子智体。在不同智体（Agent）之间共享上下文的成本很高：这会导致预填充（prefill）数据量增大，并阻碍跨不同系统提示词（system prompts）进行 KV 缓存重用。对于每一种任务类型，都必须明确权衡“隔离成本”与“协作收益”之间的利弊。</p>\n<p>混合决策框架。在实际生产部署中，通常不会统一套用某一种单一技术；而是会根据任务结构，在执行流程的不同阶段选用不同的技术。Anthropic (2026c) 将此类框架描述为：预加载那些始终必需的内容；按需检索那些仅在特定条件下才需要的内容；当上下文窗口接近饱和时对历史记录进行压缩；以及在某项子任务需要进行探索性操作（且此类操作会污染主协调器/Orchestrator 的上下文环境）时，派生出相应的子智体。Anthropic (2025d) 进一步引入了“协调层”（Harness-level）的视角：通过引入检查点（checkpoint）与恢复（resume）机制，智体得以在遭遇瞬时故障时仍能存续，且不会丢失当前的任务状态；此外，生命周期钩子（lifecycle hooks）还可在达到预设的阈值时，自动触发上下文压缩或记忆整合操作。这正是职责划分的理想模式。上下文管理理应属于基础设施层面的职责，而非智体自身的职责。当协调层能够自动处理上下文的压缩与整合工作时，模型便能将全部精力集中于任务推理本身。</p>\n<h2>7 上下文漂移与现有方法的局限性</h2>\n<p>若从“绑定约束”（binding-constraint）的视角审视，上下文漂移（Context Drift）可被视为一种源自“控制器侧”（controller-side）的故障模式：鉴于模型所能感知的状态仅限于协调层所暴露出的“投影”（projection），因此，任何与任务相关的上下文信息的丢失或失真，本质上均属于协调层所呈现的特性，而非仅仅归因于模型自身的特性 (Bölük, 2026b)。上文所提及的各类技术，虽能在不同条件下发挥作用，且成效各异，但无一能够从根本上解决这一深层问题。上下文漂移——即智能体在经历长时间的交互过程后，其行为表现逐渐发生退化的现象——依然是当前该技术层级中所面临的最棘手、且尚未解决的挑战。</p>\n<p>问题的本质。上下文漂移与“上下文腐化​​”（Context Rot）虽均源自相同的架构约束，但二者在本质上却截然不同。上下文腐化​​是针对单一推理步骤而言的一种特性：即在既定的上下文窗口内，随着输入 Token 数量的增加，模型的检索与推理质量便会随之下降。而上下文漂移则是针对整个长期交互轨迹而言的一种特性。随着智体所累积的交互轮次（turns）不断增加，其行为表现便会逐渐偏离最初设定的任务意图；尽管通过上下文压缩与检索等手段可在一定程度上延缓这一漂移过程，却终究无法将其彻底杜绝。在经历100个或更多轮次后，智体往往会重复已完成的工作，在毫无察觉的情况下推翻先前的决策，并偏离其最初的驱动目标（Bowne-Anderson &amp; Huber, 2026）。根本原因不仅仅在于窗口饱和。即使采取了激进的压缩策略，那些在每一轮压缩步骤中幸存下来的摘要信息仍携带着细微的不准确性，且这种不准确性会随时间推移而累积放大。智体（Agent）所累积的假设可能会偏离实际的任务环境，而在当前的架构中，没有任何机制能够检测到这种偏离。</p>\n<p>评估鸿沟。另一个次要问题在于，语境漂移（Context Drift）极难衡量。标准的基准测试通常仅针对那些在几十个步骤内即可完成的任务来评估代理的表现；因此，那些在100轮甚至更多轮交互之后才会显现出来的故障模式往往无法被捕捉到。近期的一些研究工作已开始着手弥合这一鸿沟。Tan（2025）引入MemBench，该基准测试通过涵盖时间推理、知识更新以及跨会话聚合等场景的多会话任务，来评估代理的记忆质量。He（2026）则专门针对相互依存的多会话任务设计MemoryArena——正是在此类任务条件下，语境漂移所造成的破坏性往往最为严重。Hu（2025b）提出一种增量式的多轮评估方法，旨在将记忆质量与生成质量区分开来，从而实现独立评估。尽管这些基准测试是必要的，但它们尚不足以解决所有问题。它们确实证明了语境漂移现象的存在，但尚未提供那种能够指导如何预防漂移的机制层面的深入理解。</p>\n<p>当前技术为何力有未逮。虽然压缩技术能够减少Token的数量，但它无法保证压缩后的表征能够准确无遗地捕捉到所有的相关状态信息。在每一轮压缩过程中所丢失的信息，一旦失去便无法挽回。尽管检索系统能够调取出相关的过往记忆，但前提是检索查询本身必须是结构良好且准确无误的。然而，一个已经发生语境漂移的智体可能根本无从知晓自己究竟需要检索哪些信息，才能将其行为纠正回正轨。子智体隔离机制虽然能够防止子任务的语境信息污染主协调器（Orchestrator）的语境，但主协调器自身的语境信息依然会随着时间的推移而逐渐发生漂移。Bowne-Anderson与Huber（2026）指出，上述种种局限性揭示一个边界：仅仅依靠“上下文工程”（Context Engineering）本身，是无法彻底解决长周期任务中的可靠性问题的。若要确保可靠性，必须构建一个完整的“驾驭层”（Harness Layer），该层应包含验证回路、在关键节点处引入“人机协作”（Human-in-the-loop）的检查点机制，以及能够识别行为偏离的异常检测系统。</p>\n<hr />\n<p>生命周期与编排（L）主要关注智体系统如何跨越多次模型调用、工具调用、故障处理、版本修订以及任务移交等环节，从而持续推进并完成一项既定任务。这一层融合了两个在早期框架中往往相互分离的关注点：即智体的执行流，以及该执行流所读取和写入的运行状态。在执行长期任务时，系统的可靠性不仅取决于模型能否生成恰当的“下一步动作”，更取决于其驾驭（harness）能否准确记忆既往事件、决策后续行动、从错误中恢复、协调各项子任务，并在任务完成后适时终止（OpenAI, 2026b; Anthropic, 2025d; 2026c）。</p>\n<p>这一层级涵盖三个组织层面的内容。单智体内循环（Single-agent inner loop）是一个重复的周期：单个智体观察当前情境，决定采取何种行动，调用工具或生成输出，并利用所得结果继续推进任务。多智体编排（Multi-agent orchestration）负责协调多个此类循环，通常通过为不同的智体分配不同的角色，或在它们之间进行任务路由来实现。全生命周期管道（Full lifecycle pipeline）则将这些智体循环置于更宏大的软件开发或任务工作流之中，例如涵盖从接收问题（Issue）到代码变更、测试、代码审查，直至提交拉取请求（Pull Request）的整个流程。在这些不同的层级中，各类系统在状态维护方式上也各具特色。无状态重放（Stateless replay）机制通过回溯已记录的交互历史，来重建并重现运行过程。有状态执行（Stateful execution）机制则将操作状态存储在提示词（Prompt）之外，例如存储在文件、代码仓库、数据库、任务图谱或外部服务中。许多实际应用系统采用的是混合模式：它们既保留了可供重放的历史记录，同时也依赖于持久化的中间产物（Artifacts）。</p>\n<h2>1 生命周期状态管理</h2>\n<p>生命周期状态管理主要关注那些维持智体运行连续性所必需的操作状态，这些状态需要在不同的轮次、会话、故障恢复以及任务交接过程中得以维系。具体内容包括待处理的子任务、工具执行结果、中间产物、代码仓库变更记录、协调元数据、会话持久化数据，以及检查点（Checkpoint）恢复机制。正是得益于这些机制，智体编排过程才得以具备持久性而非瞬时性：智体能够基于此前的工作基础继续推进任务，而无需将每一个执行步骤都视为一次孤立的模型调用调用。</p>\n<p>在此领域，一个核心的权衡点在于“无状态执行”与“有状态执行”之间的取舍。无状态设计通过回溯既往的交互历史来重建执行过程，这有助于提升任务的可复现性与可审计性；但随着执行轨迹（Trajectories）的不断增长，这种方式的成本也会随之攀升。有状态设计则将执行状态持久化存储在文件、数据库、代码仓库、任务图谱或外部服务中，从而增强了任务的连续性与故障恢复能力；但也随之引入了状态一致性维护以及调试方面的挑战（OpenAI, 2026b; Anthropic, 2026c）。因此，许多实际应用的智能体框架（Harnesses）往往采用混合设计模式，将可重放的交互历史记录与持久化的中间产物相结合，以求兼顾二者的优势。</p>\n<p>此处所探讨的“状态”概念，有别于“上下文与记忆”（Context and Memory）层级；后者主要关注的是为模型推理过程所提供的各类信息，例如检索到的文档、记忆片段或对话历史记录。它也不同于“可观测性”（Observability）概念；后者主要关注的是针对系统行为进行日志记录、链路追踪以及运行监控。</p>\n<h2>2 单智体内循环</h2>\n<p>单智体内循环是智体系统中的基本执行单元。在这一循环中，单个智体通过工具使用和接收反馈，反复与环境进行交互，且无需多个智体之间进行显式的协同。这种抽象机制构成了交互式编程、调试及问题解决过程的基础。</p>\n<p>从概念上讲，单智体循环遵循 ReAct 范式（Yao，2023），即交替执行“推理”（Reasoning）、“行动”（Action）和“观察”（Observation）这三个环节。正如 OpenAI 在其对 Codex 智体循环的分析（OpenAI，2026b）中所强调的那样，此类系统的行为不仅取决于其背后的模型，还取决于其“驾驭”（harness）——该框架负责构建提示词、调用工具、管理控制流，并将工具的输出结果反馈至后续的执行步骤中。</p>\n<p>在这一层面上，主要的执行模式差异体现在“无状态回放”（Stateless replay）与“混合执行”（Hybrid execution）之间。Codex CLI（OpenAI，2025）是基于回放机制执行模式的最典型案例；而实际应用中的编程智体往往会将可回放的历史记录与文件、代码仓库或会话状态等持久性产物相结合。因此，在表 2 中，OpenCode（Anomaly，2025）、Claude Code（Anthropic，2025）、Gemini CLI（Google，2025）、Codex CLI（OpenAI，2025）、Aider（Aider-AI，2025）以及 SWE-agent（SWE-agent，2025）均被归类于“单循环”（Single loop）这一主要模式之下。尽管此类系统具有一定的灵活性，但在执行周期较长的任务时，往往会面临上下文碎片化、错误累积以及任务分解结构薄弱等问题（Anthropic，2025d），这也促使人们寻求更为结构化的智体编排方案。</p>\n<p><img alt=\"\" src=\"https://picx.zhimg.com/v2-efe352d37bde33528bc28cecdbe11855_1440w.jpg\" /></p>\n<h2>3 多智体编排模式</h2>\n<p>多智体编排通过组合具有特定角色的多个智体，对单智体循环模式进行了扩展。与其让单个智体独自承担规划、执行、检查和修正的任务，多智体框架可以将这些职责分配给不同的智体或子智体。这种结构支持任务拆解、并行探索、批判性评估、验证以及结果聚合，使其比孤立的单智体循环模式更适合处理复杂任务。</p>\n<p>在这一设计领域中，存在几种反复出现的模式。层级编排利用高层级控制器将工作分配给智体或子智体，并整合它们的输出结果。团队编排将一组具有特定职能的智体组织成一个协同工作的团队，通常为每个成员分配明确命名的职责。工作流编排将智体和工具组织成明确的阶段或控制逻辑。扇出（Fan-out）模式并行运行多个智体，以探索多样化的解决方案。图结构组合将智体、工具或系统状态表示为交互图中的节点，从而允许多种编排模式共存。这些标签对应于表2中使用的主要模式类别。</p>\n<p>这一层级的执行通常采用有状态（Stateful）或混合（Hybrid）模式，因为多智体系统必须维护协调状态、角色分配、任务图谱、共享资源或中间结果。Anthropic 提出的“规划-生成-评估”架构（Anthropic, 2025e）印证了这一更广泛的原则：规划、执行和验证这三个环节可以拆分为明确的角色，这有助于提升任务拆解的质量和系统的鲁棒性，尽管同时也增加了协调管理的开销（Anthropic, 2026c）。</p>\n<p>表2中所列的各类系统以不同的方式实现上述模式。DeerFlow (Bytedance, 2026)、AutoGen (Microsoft, 2025)、OpenAI Agents SDK (OpenAI, 2026a) 以及 DeepAgents (LangChain, 2026b) 均归类为**层级编排**模式。oh-my-claudecode (Heo, 2026) 归类为团队编排模式。LangGraph (LangChain, 2026a) 和 Hive (Aden, 2026) 是图结构组合模式的​​典型代表；Semantic Kernel (Microsoft, 2026) 属于工作流编排模式；而 Emdash (Action, 2026) 则属于扇出模式。</p>\n<h2>4 从 Issue 到 Pull Request 的全生命周期流水线</h2>\n<p>全生命周期编排负责管理从需求规范到经校验的最终输出这一完整的任务工作流。在这一层面上，关注的重点不仅在于智体（Agent）之间如何交互，更在于其驾驭（Harness）如何支持跨越规划、实施、验证、评审及交付等各个阶段的长期执行任务。</p>\n<p>其核心抽象概念是“任务运行器”（Task Runner）：这是一种Harness，负责在完整的任务生命周期内管理调度、状态持久化、重试机制、验证环节以及迭代优化过程（OpenAI, 2026a; LangChain, 2026b）。具体的执行过程依托于各类持久化制品（Artifacts），例如代码仓库、Issue、分支、文件、测试用例以及 Pull Request 等。一个典型的标准工作流通常始于某个 Issue 或任务规范，随后依次经历智体规划、代码或制品生成、验证、人工评审，并最终以 Pull Request 被合并接纳而告终。</p>\n<p>鉴于此类系统通常是在持久化的代码仓库及外部工作流环境中运作，其所采用的主导执行模型通常属于“有状态”（Stateful）模式。部分系统会将持久化的基础设施状态与子组件内部的类“回放”（Replay-like）或“会话局部”（Session-local）执行模式相结合，这也正是表2中将其归类为“混合型”（Hybrid）的原因所在。Vibe Kanban (Bloop-AI, 2026)、Symphony (OpenAI, 2026b) 以及 GitHub Agentic Workflows (GitHub, 2026) 等系统均被归类为“全生命周期流水线”类型。从概念层面来看，这些系统实际上是对前文所述各类抽象概念的综合运用：单智体循环（Single-agent loops）提供基本的执行原语；多智体模式（Multi-agent patterns）负责提供协作机制；而任务运行器则将上述要素整合为一个端到端的工作流，在这一流程中，人类负责把控方向与决策（Steer），而智体则负责具体的执行工作。</p>\n<hr />\n<h2>。。。待续。。。</h2>"
    }
  ],
  "latest_overview": [
    {
      "title": "",
      "body_html": "<h1>2026 年，AI 编程 Agent 的真正分水岭——Harness 详解</h1>\n<ul>\n<li>来源平台: <strong>zhihu</strong>（知乎专栏文章）</li>\n<li>原文链接: <a href=\"https://zhuanlan.zhihu.com/p/2025881289593557837\">https://zhuanlan.zhihu.com/p/2025881289593557837</a></li>\n<li>作者: Ai学习的老章</li>\n</ul>\n<hr />\n<p>2026 年，AI 编程 Agent 的真正分水岭——Harness 详解</p>\n<h1>2026 年，AI 编程 Agent 的真正分水岭——Harness 详解</h1>\n<p>作者: Ai学习的老章, 赞: 53</p>\n<p><strong>2026 年，AI 编程 Agent 的真正分水岭，到底在哪里？</strong></p>\n<p><img alt=\"\" src=\"https://pic2.zhimg.com/v2-d2a6d0208eab6b1b30acdf6d5d61cb49_1440w.jpg\" /></p>\n<p>Harness 才是 AI Agent 的胜负手 - 全景信息图</p>\n<h3><strong>一个反直觉的结论</strong></h3>\n<p>这几个月你应该也发现了，各家模型隔三差五就刷一波榜</p>\n<p>GPT-5.4 来了，Opus 4.6 来了，Gemini 3.1 来了，Grok 4 也来了</p>\n<p>大家讨论最多的永远是：<strong>哪个模型写代码最牛？</strong></p>\n<p>但有个开发者最近做了一组实验：</p>\n<blockquote>\n<p>他把一个编辑工具的格式从 <code>str_replace</code> 换成了自己发明的 <code>hashline</code>，<strong>什么模型都没换</strong>，Grok Code Fast 1 的成功率直接从 **6.7% 飙到 68.3%**——翻了十倍。</p>\n</blockquote>\n<p>你拿最新的模型升级，能给你涨几个点？</p>\n<p>这个人叫 <strong>Can Bölük</strong>，一个游戏安全出身的开发者</p>\n<p>他维护着一个叫 oh-my-pi 的开源编程 Agent，提交了 <strong>4000 多个 commit</strong>，底层用 Rust 写了 7500 行原生引擎，就干一件事——<strong>打磨 harness</strong></p>\n<h3><strong>什么是 Harness？</strong></h3>\n<p>现在社区里有一个越来越清晰的共识：</p>\n<pre><code>Agent = Model + Harness\n</code></pre>\n<p><img alt=\"\" src=\"https://pic2.zhimg.com/v2-83b92c19d5be5fd7656f2006c4b35a37_1440w.jpg\" /></p>\n<p>Agent = Model + Harness 公式图解</p>\n<p><strong>Model</strong> 就是大模型本身——GPT、Claude、Gemini，负责理解和推理</p>\n<p><strong>Harness</strong> 是模型之外的一切——系统提示词、工具定义、编辑格式、上下文管理、错误处理、重试逻辑、安全边界……就是<strong>给模型套上的那套装备</strong></p>\n<p>Martin Fowler（《重构》作者）刚刚专门写了一篇长文 《Harness Engineering for Coding Agent Users》，给了一个特别精辟的定义：</p>\n<blockquote>\n<p>Harness 由两部分组成：<strong>Guides（前馈控制）</strong> 和 <strong>Sensors（反馈控制）</strong>。Guides 在 Agent 行动之前引导它做对，Sensors 在 Agent 行动之后帮它自我纠正。</p>\n</blockquote>\n<p>打个比方：模型是一匹千里马，harness 是缰绳、马鞍和马蹄铁</p>\n<p>没有好的 harness，千里马也只是在原地打转</p>\n<p><img alt=\"\" src=\"https://pic3.zhimg.com/v2-b87461e471602704427123cdbd4f055a_1440w.jpg\" /></p>\n<p>harness 整体架构：Guides 前馈 + Sensors 反馈</p>\n<h3><strong>编辑工具：Harness 问题的缩影</strong></h3>\n<p>为什么 Can Bölük 的实验那么炸裂？</p>\n<p>因为他击中了 harness 最痛的痛点——<strong>编辑工具（Edit Tool）</strong></p>\n<p>Agent 写代码的核心循环其实很简单：读文件 → 理解问题 → 生成修改 → 写回文件。但\"写回文件\"这一步，各家的方案差异极大，而且<strong>没有一个是完美的</strong>：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>方案</th>\n<th>代表产品</th>\n<th>核心问题</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>apply_patch</td>\n<td>Codex</td>\n<td>自创 diff 格式，别的模型根本不认识，Grok 4 失败率 50.7%</td>\n</tr>\n<tr>\n<td>str_replace</td>\n<td>Claude Code</td>\n<td>要求模型完美复现原文每个字符，包括空格缩进，GitHub 上有专门的 issue 吐槽帖</td>\n</tr>\n<tr>\n<td>神经网络合并</td>\n<td>Cursor</td>\n<td>专门训了个 70B 模型来做编辑合并，400 行以下文件干脆全文重写</td>\n</tr>\n</tbody>\n</table></div>\n<p>JetBrains 的 Diff-XYZ 论文和 EDIT-Bench 基准测试也证实了：<strong>没有任何一种编辑格式在所有模型和场景下都占优</strong></p>\n<p>Can Bölük 的解法叫 <strong>Hashline</strong></p>\n<p>原理很简单：当模型读文件时，每一行都带上一个 2-3 个字符的内容哈希标签：</p>\n<pre><code>11:a3|function hello() {\n22:f1|  return &quot;world&quot;;\n33:0e|}\n</code></pre>\n<p>模型编辑时引用这些标签，如果文件在读取后被修改，哈希不匹配，编辑直接被拒绝</p>\n<p>模型不再需要完美复现原文内容，只需要记住一个短标签</p>\n<p>结果？<strong>16 个模型、3 种编辑格式、每种 540 个任务</strong>的大规模测试，hashline 几乎在所有模型上匹配或超越了 str_replace，弱模型获益最大。而且 Grok 4 Fast 的输出 token 下降了 61%——因为不用再烧 token 做重试了</p>\n<p>Can Bölük 总结：</p>\n<blockquote>\n<p><strong>Gemini 成功率提升 8%，比大多数模型升级带来的提升都大，而且训练成本是零。</strong> 你在怪飞行员技术差，其实是起落架坏了。</p>\n</blockquote>\n<p><img alt=\"\" src=\"https://pic3.zhimg.com/v2-8962298fecca445a9506acf8218fb1aa_1440w.jpg\" /></p>\n<p>三种编辑方案 PK · Hashline 横扫全场</p>\n<h3><strong>Oh-My-* 生态：Harness竞赛的前线</strong></h3>\n<p>理解了 harness 的重要性，再看最近 GitHub 上涌现的一批 oh-my-* 项目，就完全说得通了</p>\n<p>它们本质上都在干同一件事：<strong>给不同的 Agent 引擎套上更强的 harness</strong></p>\n<h3><strong>oh-my-claudecode (⭐ 26.5k · 2470+ commits)</strong></h3>\n<p>GitHub: <a href=\"https://link.zhihu.com/?target=https%3A//github.com/Yeachan-Heo/oh-my-claudecode\">https://github.com/Yeachan-Heo/oh-my-claudecode</a></p>\n<p>号称：\"Don't learn Claude Code. Just use OMC.\"</p>\n<p>这是目前 Claude Code 生态里最活跃的插件，它加了<strong>21 个专业化 Agent</strong>，每个都有独立的系统提示词和工具集，包含 architect、researcher、designer、tester、devops 等角色，覆盖从需求分析到部署上线的全链路。</p>\n<p><strong>Team 模式</strong>是它的杀手级功能。它跑的是一个 5 阶段流水线：<code>team-plan → team-prd → team-exec → team-verify → team-fix</code>。一句 <code>/team 3:executor \"fix all TypeScript errors\"</code> 就能启动多 Agent 并行协作，每个 Agent 在 tmux 窗口里独立运行。</p>\n<p><strong>三种执行模式</strong>满足不同场景：</p>\n<ul>\n<li><code>autopilot</code>：描述需求，自动拆解、分配、执行、验证</li>\n<li><code>ralph</code>（Read-Act Loop with Persistent Handling）：持续执行循环，直到任务完成或你手动停止</li>\n<li><code>ultrawork</code>：无限持久模式，适合超大任务</li>\n</ul>\n<p><strong>跨模型协作</strong>也是亮点。<code>/ccg</code> 命令实现 Claude + Codex + Gemini 三模型合成。<code>omc ask</code>（Provider Advisor）可以调用 Codex CLI 或 Gemini CLI 做交叉验证——同样的问题问三家，综合最优答案。</p>\n<p>其他值得一提的：</p>\n<ul>\n<li><strong>HUD 状态栏</strong>：实时显示编排指标、token 消耗、Agent 状态</li>\n<li><strong>Wiki 知识层</strong>：灵感来自 Karpathy 的 LLM Wiki 概念，Agent 可以积累和检索项目知识</li>\n<li><strong>OpenClaw 通知系统</strong>：Discord/Telegram 实时推送执行状态</li>\n<li><strong>Rate Limit 智能等待</strong>：触发限流后自动挂起、到期恢复</li>\n<li><strong>Autoresearch 模式</strong>：轻量级监督的自主研究运行时</li>\n</ul>\n<p>安装足够简单：</p>\n<pre><code>/plugin marketplace add https://github.com/Yeachan-Heo/oh-my-claudecode\n/plugin install oh-my-claudecode\n/setup\n</code></pre>\n<p>然后 <code>/autopilot \"build a REST API for managing tasks\"</code>，坐等收菜。</p>\n<h3><strong>oh-my-openagent（原 oh-my-opencode）</strong></h3>\n<p>GitHub: <a href=\"https://link.zhihu.com/?target=https%3A//github.com/code-yeongyu/oh-my-openagent\">https://github.com/code-yeongyu/oh-my-openagent</a></p>\n<p>自称 **\"the best agent harness\"**，比 oh-my-claudecode 更激进</p>\n<p>它直接跟 Anthropic 杠上了——因为 Anthropic 封杀了 OpenCode 对 Claude 的访问</p>\n<p>它的态度是：**\"Claude Code 是个好监狱，但终究是监狱\"**</p>\n<p>核心理念：不锁定任何单一模型。Claude 做编排，GPT 做推理，Kimi 做 coding，Gemini 做创意——未来是编排所有模型。作者为此烧了 <strong>$24,000 的 LLM token 费</strong>来开发这套系统。</p>\n<p>最核心的概念叫 <strong>Discipline Agents</strong>（纪律型 Agent），每个都有独特的行为准则：</p>\n<ul>\n<li><strong>Sisyphus</strong>（<code>claude-opus-4-6</code> / <code>kimi-k2.5</code> / <code>glm-5</code>）：主编排器，负责规划和调度，名字取自永不停歇推巨石的西西弗斯</li>\n<li><strong>Hephaestus</strong>（<code>gpt-5.4</code>）：深度自主执行者，给目标就行，不需要手把手，以锻造之神命名</li>\n<li><strong>Prometheus</strong>（<code>claude-opus-4-6</code> / <code>kimi-k2.5</code>）：战略规划师，用苏格拉底式提问帮你想清楚再动手</li>\n<li><strong>Oracle</strong>：知识型 Agent，负责搜索和信息聚合</li>\n<li><strong>Librarian</strong>：文档管理专家</li>\n</ul>\n<p><strong>IntentGate（意图网关）</strong> 是它独创的概念——先分析用户的真实意图再行动。你说\"帮我改一下这个 bug\"，它会先判断你是想快速修还是想彻底重构，避免 Agent 对指令的字面误解。</p>\n<p><strong>Category-Based Model Routing（基于类别的模型路由）</strong> 也很聪明——不是按模型名指定，而是按任务类别（<code>visual-engineering</code>、<code>deep</code>、<code>quick</code>、<code>ultrabrain</code>）自动分发，底层绑定最合适的模型。</p>\n<p><strong>Skill-Embedded MCPs</strong>：每个 Skill 自带 MCP 服务器，需要时按需启动，不需要全局挂载。内置三个高质量 MCP：Exa（网络搜索）、Context7（官方文档查询）、Grep.app（GitHub 代码搜索）。</p>\n<p>其他硬核功能：</p>\n<ul>\n<li><strong>Hash-Anchored Edit Tool</strong>：灵感直接来自 oh-my-pi 的 hashline</li>\n<li><strong>Background Agents</strong>：5+ 专家并行执行</li>\n<li><strong>Todo Enforcer + Comment Checker</strong>：强制执行 TODO 清理，禁止\"AI 味\"注释（比如 \"// This function handles…\" 这种废话注释）</li>\n<li><strong>Session Recovery</strong>：API 故障、上下文超限都能自动恢复</li>\n<li>**<code>/init-deep</code>**：自动生成分层 AGENTS.md，给每个子目录写专属上下文</li>\n</ul>\n<p>完全兼容 Claude Code 的 hooks、commands、skills、MCP</p>\n<h3><strong>oh-my-codex</strong></h3>\n<p>GitHub: <a href=\"https://link.zhihu.com/?target=https%3A//github.com/Yeachan-Heo/oh-my-codex\">https://github.com/Yeachan-Heo/oh-my-codex</a></p>\n<p>针对 OpenAI Codex CLI 的 harness 层</p>\n<p>核心定位：<strong>Codex 负责执行，OMX 负责流程</strong></p>\n<p>标准工作流是一个 4 阶段管道：<code>$deep-interview</code>（深度需求访谈）→ <code>$ralplan</code>（生成执行计划）→ <code>$team</code>（并行执行）/ <code>$ralph</code>（循环执行直到完成）。先搞清楚要做什么，再规划怎么做，最后上手干。</p>\n<p><code>.omx/</code> 目录存储所有状态——计划、日志、记忆、运行时状态，每次执行都有迹可循。<code>omx explore</code> 做只读的代码库探索，<code>omx sparkshell</code> 做有边界的 shell 验证。通过 <code>.codex/hooks.json</code> 对接 Codex 原生 hook 系统，加上 HUD 监控面板，你能清楚看到每一步在干什么。</p>\n<h3><strong>oh-my-pi (⭐ 2.8k · 4018 commits)</strong></h3>\n<p>GitHub: <a href=\"https://link.zhihu.com/?target=https%3A//github.com/can1357/oh-my-pi\">https://github.com/can1357/oh-my-pi</a></p>\n<p>下面是 oh-my-pi 的 LSP 集成效果和 hashline 编辑工具的实际截图：</p>\n<p><img alt=\"\" src=\"https://picx.zhimg.com/v2-004bef4a2dc59b3f6b099617c5cb20d3_1440w.jpg\" /></p>\n<p>oh-my-pi LSP 集成</p>\n<p>Can Bölük 的\"小兴趣项目\"，实际上是整个 harness 创新的源头</p>\n<p>它是一个完全独立的终端编程 Agent，不依赖任何现有 CLI——底层用 <strong>Rust 写了约 7,500 行原生 N-API 引擎</strong>，编译成 11 个 native 模块（grep、shell、text、keys、highlight、glob、task、ps、prof、image、clipboard、html），性能拉满</p>\n<p>除了 hashline，它还有一堆硬核功能：</p>\n<ul>\n<li><strong>LSP 集成</strong>：支持 40+ 语言的 IDE 级代码智能，加上 AST-Grep 做 25 种语言的模式感知代码搜索</li>\n<li><strong>TTSR（Time Traveling Streamed Rules）</strong>：零上下文消耗的规则系统。普通的系统提示词会占用宝贵的上下文窗口，TTSR 的规则只在模型输出匹配特定模式时才\"时间旅行\"式注入，平时完全不占位</li>\n<li><strong>30+ AI Provider 支持</strong>：通过 <code>/login</code> 命令接入 OpenAI、Anthropic、Google、xAI、DeepSeek、Mistral 等所有主流平台，支持多凭证轮换和用量感知选择</li>\n<li><strong>Model Roles</strong>：<code>default</code>（日常）、<code>smol</code>（快速小任务）、<code>slow</code>（深度推理）、<code>plan</code>（规划）、<code>commit</code>（提交信息），按角色自动选模型</li>\n<li><strong>Browser 工具</strong>：内置 14 个隐身脚本，绕过反爬</li>\n<li><strong>SSH 工具</strong>：远程服务器命令执行</li>\n<li><strong>Cursor Provider</strong>：可以当 Cursor 的后端用</li>\n<li><strong>图片生成</strong>：集成 Gemini 的图片生成能力</li>\n<li><strong>65+ 内置主题</strong>：终端也要好看</li>\n</ul>\n<p>这四个项目，画出了 harness 军备竞赛的全景图：<strong>你的 Agent 能有多强，取决于你给它套的装备有多硬。</strong> 而且它们之间还在互相借鉴——oh-my-openagent 的 Hash-Anchored Edit Tool 直接学的 oh-my-pi 的 hashline，oh-my-codex 和 oh-my-claudecode 共享 ralph/team 等执行模式。</p>\n<p><img alt=\"\" src=\"https://pic1.zhimg.com/v2-bb274fbb35a5d7b2c494399c330aea7a_1440w.jpg\" /></p>\n<p>Oh-My-* 生态全景：谁给谁套 Harness？</p>\n<h3><strong>Claude Managed Agents：Anthropic 的\"官方 Harness\"</strong></h3>\n<p>说完开源社区的探索，再来看 Anthropic 官方</p>\n<p>他们最近推出了 <strong>Claude Managed Agents</strong>，本质上就是 Anthropic 自己下场做了一个<strong>托管式 harness 服务</strong>。</p>\n<h3><strong>是什么？</strong></h3>\n<p>下面这张图来自 Anthropic 工程博客，展示了 Managed Agents 的整体架构——Brain、Hands 和 Session 三层解耦：</p>\n<p><img alt=\"\" src=\"https://pic2.zhimg.com/v2-aefd5ec42428e80fcfaa323e242454db_1440w.jpg\" /></p>\n<p>Claude Managed Agents 架构</p>\n<p><img alt=\"\" src=\"https://pic2.zhimg.com/v2-b75e8ec8f4c0881f51123c2d11492599_1440w.jpg\" /></p>\n<p>Claude Managed Agents · Meta-Harness 架构图解</p>\n<p>根据 官方文档，Claude Managed Agents 是一个：</p>\n<blockquote>\n<p><strong>预构建、可配置的 Agent Harness，运行在托管基础设施上。</strong></p>\n</blockquote>\n<p>它由四个核心概念组成：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>概念</th>\n<th>说明</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Agent</td>\n<td>模型 + 系统提示 + 工具 + MCP 服务器 + Skills</td>\n</tr>\n<tr>\n<td>Environment</td>\n<td>配置好的云容器（预装 Python/Node.js/Go，网络规则）</td>\n</tr>\n<tr>\n<td>Session</td>\n<td>运行中的 Agent 实例，执行特定任务</td>\n</tr>\n<tr>\n<td>Events</td>\n<td>应用和 Agent 之间的消息流（SSE 流式）</td>\n</tr>\n</tbody>\n</table></div>\n<h3><strong>为什么要做这个？</strong></h3>\n<p>Anthropic 工程团队在 官方博客 里讲了他们的设计哲学，这段话特别关键：</p>\n<blockquote>\n<p><strong>Harness 编码的是对 Claude 能力的假设，而这些假设会随着模型进步而过时。</strong></p>\n</blockquote>\n<p>Claude Sonnet 4.5 在上下文接近极限时会\"焦虑\"地草草收工，所以 harness 里加了上下文重置机制。但到了 Opus 4.5，这个行为消失了，重置机制反而成了累赘。</p>\n<p>所以他们的解法是：<strong>把 harness 本身做成可替换的抽象层</strong>。</p>\n<h3><strong>\"大脑\"和\"双手\"分离</strong></h3>\n<p>这是 Managed Agents 最精彩的设计。他们把 Agent 拆成了三部分：</p>\n<ul>\n<li><strong>Brain（大脑）</strong>：Claude + harness 循环</li>\n<li><strong>Hands（双手）</strong>：沙箱容器、工具执行</li>\n<li><strong>Session（记忆）</strong>：事件日志，独立于大脑和双手</li>\n</ul>\n<p><img alt=\"\" src=\"https://pic1.zhimg.com/v2-9f0f12669abdb44e6aa1c6098e5c57f6_1440w.jpg\" /></p>\n<p>Brain 和 Hands 解耦架构</p>\n<p>每一部分都可以独立失败、独立替换。容器挂了？harness 把它当作一次工具调用失败，让 Claude 决定是否重试，新容器随时初始化。harness 挂了？新 harness 读取 session 日志，从上次事件恢复。</p>\n<p>这个设计带来的性能提升也很炸裂：**p50 TTFT（首 token 延迟）下降了约 60%，p95 下降超过 90%**。因为不需要每次都等容器启动。</p>\n<p>安全上也更干净：生成的代码在沙箱里执行，凭证永远不会进入沙箱。Git token 在容器初始化时注入到 git remote，MCP OAuth token 通过独立代理处理。Agent 永远碰不到真正的密钥。</p>\n<h3><strong>和开源 harness 的关系</strong></h3>\n<p>Anthropic 在博客最后说了一句意味深长的话：</p>\n<blockquote>\n<p><strong>Managed Agents 是一个 meta-harness（元 harness），它对 Claude 未来需要什么样的具体 harness 不持立场。Claude Code 是一个优秀的 harness，特定任务的 Agent harness 在窄领域表现更好。Managed Agents 可以容纳任何一种。</strong></p>\n</blockquote>\n<p>翻译过来：Anthropic 不是要取代 oh-my-claudecode 们，而是提供了一个<strong>底层基础设施</strong>，让各种 harness 都能跑在上面。</p>\n<p>不过，结合他们之前封杀 OpenCode 的操作，这个\"开放\"到底有多开放，还有待观察。</p>\n<h3><strong>所以，\"harness 才是分水岭\"这个说法对吗？</strong></h3>\n<p>我觉得<strong>对了一大半</strong>，但需要补充：</p>\n<p><strong>对的部分</strong>：在模型能力差距日益缩小的今天，harness 确实是决定 Agent 实际表现的最大变量。Can Bölük 的基准测试已经证明，光改编辑工具的格式就能让弱模型翻十倍，强模型也能提升 5-14 个百分点。Martin Fowler 说得好：光有反馈没有前馈，Agent 会重复犯同样的错；光有前馈没有反馈，规则永远不知道有没有用。两者缺一不可。</p>\n<p><strong>需要补充的部分</strong>：harness 的提升有天花板。Martin Fowler 也坦言，对于\"诊断错误\"、\"过度工程\"、\"误解指令\"这类高阶认知问题，目前的 harness 手段还不能可靠地解决。这些最终还是要靠模型能力的提升。</p>\n<p>所以更准确的说法是：</p>\n<blockquote>\n<p><strong>模型决定了 Agent 的能力上限，harness 决定了 Agent 能发挥出多少。</strong> 2026 年的竞争格局，两者缺一不可，但 harness 的投资回报率当前远高于模型本身。</p>\n</blockquote>\n<h3><strong>总结</strong></h3>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th></th>\n<th>开源 Harness 生态</th>\n<th>Claude Managed Agents</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>定位</td>\n<td>给现有 Agent 加装更强装备</td>\n<td>提供托管式 harness 基础设施</td>\n</tr>\n<tr>\n<td>模型绑定</td>\n<td>多模型支持（Claude/GPT/Gemini/Kimi）</td>\n<td>仅 Claude</td>\n</tr>\n<tr>\n<td>核心优势</td>\n<td>灵活、可深度定制、社区驱动</td>\n<td>零运维、安全隔离、官方优化</td>\n</tr>\n<tr>\n<td>适合谁</td>\n<td>愿意折腾、追求极致控制的开发者</td>\n<td>快速起步、不想自己搭基础设施的团队</td>\n</tr>\n</tbody>\n</table></div>\n<h1>Harness #AgentCoding #ClaudeCode #OhMyClaudeCode #AI编程</h1>\n<p><strong>制作不易，如果这篇文章觉得对你有用，可否点个关注。给我个三连击：点赞、转发和在看。若可以再给我加个🌟，谢谢你看我的文章，我们下篇再见！</strong></p>"
    }
  ],
  "graph": {
    "nodes": [
      {
        "id": "webarena",
        "x": 80,
        "y": 150,
        "category": "environment"
      },
      {
        "id": "agentbench",
        "x": 150,
        "y": 70,
        "category": "foundation"
      },
      {
        "id": "swebench",
        "x": 260,
        "y": 290,
        "category": "evaluation"
      },
      {
        "id": "osworld",
        "x": 400,
        "y": 150,
        "category": "environment"
      },
      {
        "id": "toolsandbox",
        "x": 500,
        "y": 150,
        "category": "environment"
      },
      {
        "id": "swe_verified",
        "x": 560,
        "y": 290,
        "category": "evaluation"
      },
      {
        "id": "browsergym",
        "x": 680,
        "y": 150,
        "category": "environment"
      },
      {
        "id": "codex",
        "x": 820,
        "y": 430,
        "category": "runtime"
      },
      {
        "id": "claude_longrun",
        "x": 940,
        "y": 430,
        "category": "runtime"
      },
      {
        "id": "idebench",
        "x": 1040,
        "y": 290,
        "category": "evaluation"
      },
      {
        "id": "openai_harness",
        "x": 1120,
        "y": 430,
        "category": "runtime"
      },
      {
        "id": "meta_harness",
        "x": 1200,
        "y": 570,
        "category": "optimization"
      },
      {
        "id": "harness_design",
        "x": 1260,
        "y": 430,
        "category": "runtime"
      },
      {
        "id": "managed_agents",
        "x": 1360,
        "y": 430,
        "category": "runtime"
      },
      {
        "id": "genericagent",
        "x": 1420,
        "y": 500,
        "category": "runtime"
      },
      {
        "id": "ahe",
        "x": 1480,
        "y": 570,
        "category": "optimization"
      },
      {
        "id": "openclaw",
        "x": 1560,
        "y": 500,
        "category": "runtime"
      },
      {
        "id": "roadmapbench",
        "x": 1600,
        "y": 290,
        "category": "evaluation"
      },
      {
        "id": "harness_bench",
        "x": 1660,
        "y": 570,
        "category": "evaluation"
      },
      {
        "id": "codex_safety",
        "x": 1700,
        "y": 430,
        "category": "runtime"
      },
      {
        "id": "rho",
        "x": 1780,
        "y": 570,
        "category": "optimization"
      }
    ],
    "edges": [
      {
        "from": "webarena",
        "to": "osworld",
        "label": "扩到桌面"
      },
      {
        "from": "webarena",
        "to": "browsergym",
        "label": "统一接口"
      },
      {
        "from": "agentbench",
        "to": "swebench",
        "label": "转向代码"
      },
      {
        "from": "agentbench",
        "to": "toolsandbox",
        "label": "状态工具"
      },
      {
        "from": "swebench",
        "to": "swe_verified",
        "label": "人工复核"
      },
      {
        "from": "swe_verified",
        "to": "idebench",
        "label": "IDE原生"
      },
      {
        "from": "idebench",
        "to": "roadmapbench",
        "label": "长程开发"
      },
      {
        "from": "swe_verified",
        "to": "codex",
        "label": "云端代理"
      },
      {
        "from": "codex",
        "to": "openai_harness",
        "label": "方法论化"
      },
      {
        "from": "codex",
        "to": "openclaw",
        "label": "第三方宿主"
      },
      {
        "from": "codex",
        "to": "codex_safety",
        "label": "规则治理"
      },
      {
        "from": "claude_longrun",
        "to": "harness_design",
        "label": "三Agent"
      },
      {
        "from": "harness_design",
        "to": "managed_agents",
        "label": "接口抽象"
      },
      {
        "from": "claude_longrun",
        "to": "genericagent",
        "label": "密度记忆"
      },
      {
        "from": "openai_harness",
        "to": "meta_harness",
        "label": "自动搜索"
      },
      {
        "from": "meta_harness",
        "to": "ahe",
        "label": "闭环进化"
      },
      {
        "from": "ahe",
        "to": "harness_bench",
        "label": "配置诊断"
      },
      {
        "from": "ahe",
        "to": "rho",
        "label": "无标自优"
      }
    ],
    "milestones": [
      "webarena",
      "codex",
      "rho"
    ]
  },
  "algos": [
    {
      "id": "webarena",
      "num": 1,
      "name": "WebArena",
      "fullName": "真实网页环境 (WebArena)",
      "year": "2023.07",
      "org": "CMU",
      "parent": "—",
      "paperUrl": "https://arxiv.org/abs/2307.13854",
      "projectUrl": "",
      "category": "environment",
      "motivation": "把网页任务做成可复现实验环境",
      "summary": "WebArena 提出了一个**独立的、可自托管的真实 Web 交互环境**和对应的基准测试，通过集成 4 类功能性网站（电商、CMS、社交论坛、地图），使自主 Agent 能够在复杂、长程、动态的真实 Web 任务中进行端到端评估，揭示了当前最强 LLM 代理（GPT-4 + CoT）端到端成功率仅 14.41%，远低于人类 78.24% 的巨大差距。",
      "keyPoints": [
        "<strong>自包含可复现环境</strong>：完全自托管的 Web 环境，包含 4 类真实网站（购物、CMS/Reddit、GitLab、地图），每个网站包含完整用户数据和功能逻辑，可完全本地部署。",
        "<strong>三类观察空间</strong>：URL、当前页面 HTML（包括文本和图片）、以及 <strong>Accessibility Tree</strong>（含元素 ID，供 Agent 精准定位交互元素）。",
        "<strong>统一 Action Space</strong>：click、type、hover、press（按键组合）、new_tab、tab_focus、tab_close、goto_page、go_back、go_forward、scroll、stop 共 10+ 种复合操作。",
        "<strong>812 个任务</strong>：覆盖 3 类任务 — <strong>信息查找 (Information-Seeking)</strong>、<strong>站点导航 (Site Navigation)</strong>、<strong>内容与配置 (Content &amp; Configuration)</strong>；另有 <strong>Unachievable 任务</strong> 检验模型识别任务不可达的能力。",
        "<strong>3 种评估方式</strong>：exact_match、must_include、fuzzy_match；按任务类型细分解耦评估。",
        "<strong>Agent 设计</strong>：基于 GPT-4、GPT-3.5、PaLM-2 等 LLM，采用 Chain-of-Thought (CoT) + UA Hint (告知模型可执行 stop) 的 Prompting 策略，通过 Accessibility Tree ID 定位元素。",
        "<strong>实验揭示核心问题</strong>：GPT-4 最佳成功率 14.41%（w/o UA Hint），54.9% 可行任务被误判为不可完成（Early Stopping）；同一模板内的任务变体成功率一致性极差（仅 4/61 模板达到 100% 模板成功率）。",
        "<strong>对比 Human</strong>：人类 78.24% 成功率，50% 失败源于意图误解（如提供距离而非时间）、答案不完整、执行不完整。"
      ],
      "detail": "<h5>1. 核心框架图（Figure 1）</h5>\n<p><img alt=\"WebArena 环境总览\" src=\"https://github.com/web-arena-x/webarena/blob/main/resources/webarena_overview.png?raw=true\" />\n<em>图：WebArena 是一个独立、自托管的环境，创建四个类别的网站（电商购站、CMS/Reddit、GitLab、Map），具有功能性和真实数据，Agent 通过 Browser 与之交互。</em><em>注</em><em>：因 ar5iv 网络不可达，此处 URL 为官方 GitHub 仓库推测路径，若无法显示请参考论文原文 Figure 1。</em></p>\n<h5>2. Agent 执行流程（伪代码）</h5>\n<pre><code>Algorithm: WebArena Agent Execution Loop\n\nInput: Task intent I, Environment E, Max steps M\nOutput: Task execution trajectory and final answer\n\n1.  obs = E.reset()         # 获取初始 observation（URL + accessibility tree）\n2.  history = []\n3.  for step in 1..M:\n4.      prompt = BuildPrompt(instruction, I, obs, history, allowed_actions)\n5.      response = LLM(prompt)          # CoT: 先文本推理，再输出动作\n6.      action = ParseAction(response)\n7.      if action == &quot;stop [answer]&quot;:\n8.          return answer\n9.      if action == &quot;stop [N/A]&quot;:      # 识别为 Unachievable\n10.         return &quot;N/A&quot;\n11.     new_obs = E.execute(action)     # 环境执行操作，返回新的 observaton\n12.     obs = new_obs\n13.     history.append((action, obs))\n14. raise MaxStepExceeded\n</code></pre>\n<h5>3. 深入方法细节</h5>\n<p><strong>动机与背景</strong>：<br />\n现有的 Web Agent 基准（如 MiniWoB++、WebShop、Mind2Web）在<strong>功能性</strong>（真实数据交互而非模拟）、<strong>真实性</strong>（模拟日常复杂任务而非简单原子操作）、<strong>环境动态性</strong>（支持交互而非常态网页）三者中存在折中。WebArena 通过完全自托管 4 类功能完备的网站填补了这一空白。</p>\n<p><strong>核心机制</strong>：\n1. <strong>自托管环境</strong>：所有网站均基于真实开源项目二次开发（如 GitLab、Magento 电商、Reddit 服务器），具有真实的用户数据、产品数据和内容，Agent 的操作会持久化到数据库中（如下单、发帖、fork 仓库），确保评估的<strong>功能性正确性</strong>。\n2. <strong>Observation Space</strong>：提供 URL（页面地址）、HTML page content（文本+图片）、以及 <strong>Accessibility Tree</strong>（包含每个交互元素的唯一 ID、角色、名字等）。Agent 通过 Element ID 精准定位点击或输入目标，避免了视觉定位或 DOM 解析的不稳定性。\n3. <strong>Action Space 设计</strong>：覆盖浏览器核心操作 — 导航（go_page/go_back/go_forward）、标签页管理（new_tab/tab_focus/tab_close）、键盘输入（type/press）、鼠标交互（click/hover）、滚动（scroll）、停止（stop）。允许 Agent 在跨页面、跨标签的复杂任务中自由操作。\n4. <strong>Benchmark 构造</strong>：从 170 个模板中通过<strong>众包重写</strong>生成 812 个具体任务，确保意图表达的多样性。3 类任务分解了信息检索、站点间导航和内容操作的不同粒度。Unachievable 任务（任务描述中有意混淆条件）检验 Agent 的自知能力。\n5. <strong>Agent Prompting</strong>：在 Prompt 中详尽描述浏览器环境、允许操作及其语法，并提供 2 个示例（few-shot）。Chain-of-Thought (CoT) 让模型先文本推理再输出动作；UA Hint 告知模型遇到不可完成任务时可执行 \"stop [N/A]\"，模仿人类的指导说明。\n6. <strong>评估函数</strong>：用 URL 匹配、页面元素匹配、最终状态匹配 3 类函数，根据不同任务需求选择 exact_match / must_include / fuzzy_match，并通过功能程序检查（如：商品是否真的加入了购物车、帖子是否真的发布成功）保证评估的<strong>功能正确性</strong>。</p>\n<p><strong>训练/推理流程</strong>：\n- Agent 无训练，所有 LLM 为固定模型（GPT-4、GPT-3.5、text-bison-001）。\n- 每条任务执行前重置环境到初始状态，Agent 最多执行 M 步。\n- 评估时记录完整动作轨迹，成功或失败后有详细的错误日志分析。</p>\n<p><strong>与传统方法的区别</strong>：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>基准</th>\n<th>动态交互</th>\n<th>真实环境</th>\n<th>多样人类任务</th>\n<th>功能正确性评估</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Mind2Web</td>\n<td>✗</td>\n<td>✓</td>\n<td>✓</td>\n<td>✗</td>\n</tr>\n<tr>\n<td>MiniWoB++</td>\n<td>✓</td>\n<td>✗</td>\n<td>✗</td>\n<td>✓</td>\n</tr>\n<tr>\n<td>Webshop</td>\n<td>✓</td>\n<td>✗</td>\n<td>✗</td>\n<td>✓</td>\n</tr>\n<tr>\n<td><strong>WebArena</strong></td>\n<td><strong>✓</strong></td>\n<td><strong>✓</strong></td>\n<td><strong>✓</strong></td>\n<td><strong>✓</strong></td>\n</tr>\n</tbody>\n</table></div>\n<p>WebArena 是首个在四项维度上均满足要求的基准。</p>\n<p><strong>关键发现与错误分析</strong>：\n- <strong>Early Stopping</strong>：GPT-4 在含 UA Hint 时，将 54.9% 的可行任务误判为不可行，在移除 UA Hint 后总成功率升至 14.41%（Table 2）。\n- <strong>模板内不一致性</strong>：即使任务来自同一模板，Agent 表现极度不稳定。GPT-4 仅 4/61 个模板达到 100% 成功率，GPT-3.5 为 0（Table 3）。\n- <strong>人类水平</strong>：人类完成 78.24% 任务；失败 50% 因误解意图或回答不完整，表明任务理解本身具有挑战性。\n- <strong>模型对比</strong>：GPT-4 (14.41%) &gt; GPT-3.5 (6.41%) &gt; PaLM-2 (5.05%)。CoT 相比直接预测仅提升 ±2.34%（Table 2）。</p>",
      "quiz": {
        "q": "WebArena 为什么把 Accessibility Tree 连同元素 ID 暴露给 agent，而不是只给纯截图？",
        "options": [
          "因为 WebArena 不支持真实浏览器交互，只能做文本分类",
          "因为这样可以把网页任务退化成静态问答，避免多步决策",
          "因为元素 ID 提供了稳定的交互锚点，减少纯视觉定位或 DOM 解析带来的不稳定性",
          "因为只有 Accessibility Tree 能用来运行最终评估脚本"
        ],
        "answer": 2,
        "explain": "论文的一个关键设计是让 agent 通过可访问性树中的元素标识去执行 click/type 等动作，这比仅靠截图点坐标更稳定，也比原始 DOM 更直接服务交互。"
      }
    },
    {
      "id": "agentbench",
      "num": 2,
      "name": "AgentBench",
      "fullName": "智能体评测基准 (AgentBench)",
      "year": "2023.08",
      "org": "清华大学",
      "parent": "—",
      "paperUrl": "https://arxiv.org/abs/2308.03688",
      "projectUrl": "",
      "category": "foundation",
      "motivation": "首次系统评测LLM作为交互Agent",
      "summary": "AgentBench 提出了首个多维度的 LLM-as-Agent 交互式评测基准，涵盖 8 类环境（代码/游戏/Web）共 3 种交互基底，系统评估了 27 个 LLM 的推理与决策能力，揭示了顶级商业模型与开源模型之间在自主代理任务上的显著性能差距。",
      "keyPoints": [
        "提出 <strong>AgentBench</strong> 基准，由 <strong>8 个交互式环境</strong> 组成，分为三类：Code（OS、Database、Knowledge Graph）、Game（Digital Card Game、Lateral Thinking Puzzles、House-Holding）、Web（Web Shopping、Web Browsing）",
        "评估了 <strong>27 个 LLM</strong>，包括 API-based 商业模型（GPT-4、Claude 等）和开源模型（LLaMA-2、CodeLlama、Vicuna 等）",
        "将 LLM-as-Agent 评估形式化为 <strong>部分可观测马尔可夫决策过程（POMDP）</strong>，定义 5 种执行结果：Completed、Context Limit Exceeded、Invalid Format、Invalid Action、Task Limit Exceeded",
        "开发了 <strong>统一评估工具包</strong>，支持不同 LLM 在多样化定制代理任务上运行",
        "核心发现：<strong>GPT-4 在 8 个环境中的 6 个上表现最优</strong>，开源模型平均分 0.51 vs 商业模型 2.15，差距显著",
        "<strong>Task Limit Exceeded（TLE）</strong> 是失败主因，表明 LLM 的长程推理和决策能力不足",
        "<strong>代码训练具有双刃剑效应</strong>：在程序化任务（如 Web Shopping）上有优势，但在需要通用推理的任务（如 Digital Card Game）上可能退化",
        "<strong>高质量对齐数据（如 GPT-4 生成数据）</strong> 能显著提升 Agent 性能，Vicuna-13b 通过 ShareGPT 数据对齐后性能匹敌 3 倍参数的 CodeLlama-34b"
      ],
      "detail": "<h5>1. 框架总览</h5>\n<p>AgentBench 提出了一个系统性的 LLM-as-Agent 评估框架。该框架将 LLM 作为自主代理置于 8 个不同的交互式环境中，通过与环境的持续交互来评估其推理、决策和指令遵循能力。</p>\n<p><img alt=\"AgentBench 8个环境总览\" src=\"https://ar5iv.labs.arxiv.org/html/2308.03688/assets/figures/env_overview.png\" />\n<em>图：AgentBench 的 8 个评估环境，涵盖 Code、Game、Web 三类交互基底</em></p>\n<h5>2. 评估框架形式化</h5>\n<p>AgentBench 将 LLM-as-Agent 的交互式评估形式化为一个 <strong>部分可观测马尔可夫决策过程（POMDP）</strong>，定义为 <span class=\"kb-math kb-math-inline\">\\langle \\mathcal{S}, \\mathcal{A}, \\mathcal{T}, \\mathcal{R}, \\mathcal{U}, \\mathcal{O} \\rangle</span>：</p>\n<ul>\n<li><strong><span class=\"kb-math kb-math-inline\">\\mathcal{S}</span></strong>：环境状态空间</li>\n<li><strong><span class=\"kb-math kb-math-inline\">\\mathcal{A}</span></strong>：动作空间（LLM 可执行的操作）</li>\n<li><strong><span class=\"kb-math kb-math-inline\">\\mathcal{T}</span></strong>：状态转移函数 <span class=\"kb-math kb-math-inline\">\\mathcal{T}(s&#x27;|s, a)</span></li>\n<li><strong><span class=\"kb-math kb-math-inline\">\\mathcal{R}</span></strong>：奖励函数</li>\n<li><strong><span class=\"kb-math kb-math-inline\">\\mathcal{U}</span></strong>：LLM 代理的观察空间</li>\n<li><strong><span class=\"kb-math kb-math-inline\">\\mathcal{O}</span></strong>：观察函数 <span class=\"kb-math kb-math-inline\">\\mathcal{O}(u|s)</span></li>\n</ul>\n<p>在每一轮交互中，LLM 代理接收当前观察文本，生成一个动作（自然语言指令或代码），环境执行该动作并返回新的观察。评测的核心是 LLM 在多轮交互中能否逐步完成目标任务。</p>\n<pre><code class=\"language-python\"># AgentBench 评估循环伪代码\ndef evaluate_agent(llm, env, max_turns):\n    observation = env.reset()\n    for turn in range(max_turns):\n        action = llm.generate(observation)  # LLM 生成动作\n        result = env.step(action)           # 环境执行动作\n        if result.status == &quot;Completed&quot;:\n            return &quot;Success&quot;\n        elif result.status in [&quot;Invalid Format&quot;, &quot;Invalid Action&quot;]:\n            return &quot;Error&quot;\n        observation = result.observation\n    return &quot;Task Limit Exceeded (TLE)&quot;\n</code></pre>\n<h5>3. 8 个环境详解</h5>\n<p><strong>代码类环境（Code-grounded）</strong>：</p>\n<ol>\n<li>\n<p><strong>Operating System (OS)</strong>：模拟真实 Linux 终端环境。LLM 需要通过执行 bash 命令来完成系统管理任务（如文件操作、进程管理、文本处理等）。测试 LLM 的命令行操作能力和对操作系统概念的理解。</p>\n</li>\n<li>\n<p><strong>Database (DB)</strong>：模拟关系型数据库操作环境。LLM 需要根据自然语言查询生成正确的 SQL 语句来操作数据库。考验 LLM 将自然语言转译为结构化查询语言的能力，输出格式要求严格。</p>\n</li>\n<li>\n<p><strong>Knowledge Graph (KG)</strong>：基于知识图谱的查询与推理环境。LLM 需要通过生成 SPARQL/Cypher 等图查询语言来回答问题，测试在结构化知识上的检索和推理能力。</p>\n</li>\n</ol>\n<p><strong>游戏类环境（Game-grounded）</strong>：</p>\n<ol>\n<li>\n<p><strong>Digital Card Game (DCG)</strong>：模拟数字卡牌对战游戏。LLM 需要在理解复杂的游戏规则后做出策略决策，选择出牌或使用技能。规则说明较长，对指令遵循和策略规划要求极高。</p>\n</li>\n<li>\n<p><strong>Lateral Thinking Puzzles (LTP)</strong>：横向思维解谜。LLM 需要通过提出是/否问题逐步推理出谜题的答案，考验创造性思维和假设验证能力。</p>\n</li>\n<li>\n<p><strong>House-Holding (HH)</strong>：模拟家庭场景中的物品整理任务。LLM 需要理解物理常识和空间关系，在交互式环境中将物品放到正确的位置。GPT-4 在此任务上达到了 78% 的成功率。</p>\n</li>\n</ol>\n<p><strong>Web 类环境（Web-grounded）</strong>：</p>\n<ol>\n<li>\n<p><strong>Web Shopping (WS)</strong>：模拟在线购物环境。LLM 需要根据用户需求在虚拟电商平台上搜索、比较和选择商品，测试多步决策和信息筛选能力。</p>\n</li>\n<li>\n<p><strong>Web Browsing (WB)</strong>：模拟网页浏览和信息检索。LLM 需要在模拟的网页环境中导航、点击链接、填写表单来完成信息获取任务。</p>\n</li>\n</ol>\n<h5>4. 核心发现与分析</h5>\n<p><strong>性能差距巨大</strong>：在 AgentBench 整体评分中，API-based 商业模型平均得分 2.15，开源模型仅 0.51。最强开源模型 CodeLlama-34b（0.96 分）仍与 GPT-3.5-turbo 存在明显差距。这与开源模型在其他基准（如 MMLU、HumanEval）上的强劲表现形成鲜明对比，说明 <strong>Agent 能力需要不同于静态基准的评测维度</strong>。</p>\n<p><strong>失败模式分析</strong>（Table 4）：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>结果类型</th>\n<th>OS</th>\n<th>DB</th>\n<th>KG</th>\n<th>DCG</th>\n<th>LTP</th>\n<th>HH</th>\n<th>WS</th>\n<th>WB</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Completed</td>\n<td>75.0</td>\n<td>37.9</td>\n<td>30.1</td>\n<td>51.2</td>\n<td>14.0</td>\n<td>13.1</td>\n<td>54.9</td>\n<td>56.6</td>\n</tr>\n<tr>\n<td>TLE</td>\n<td>23.9</td>\n<td>8.0</td>\n<td><strong>67.9</strong></td>\n<td>0.0</td>\n<td><strong>82.5</strong></td>\n<td>22.1</td>\n<td>27.8</td>\n<td>35.0</td>\n</tr>\n<tr>\n<td>Invalid Format</td>\n<td>0.0</td>\n<td><strong>53.3</strong></td>\n<td>0.0</td>\n<td>38.5</td>\n<td>0.0</td>\n<td>0.0</td>\n<td>17.2</td>\n<td>0.0</td>\n</tr>\n<tr>\n<td>Invalid Action</td>\n<td>0.9</td>\n<td>0.0</td>\n<td>0.0</td>\n<td>10.2</td>\n<td>0.0</td>\n<td><strong>64.1</strong></td>\n<td>0.0</td>\n<td>8.4</td>\n</tr>\n</tbody>\n</table></div>\n<div class=\"key-point\">💡 关键：<strong>Task Limit Exceeded（TLE）</strong> 在 KG（67.9%）和 LTP（82.5%）中占比极高，表明 LLM 在需要深度推理的任务中容易陷入循环生成或超出交互轮次。<strong>Invalid Format</strong> 在 DB（53.3%）中最为严重，说明格式严格约束是 LLM 的一大弱点。</div>\n<p><strong>代码训练的双刃剑效应</strong>：CodeLlama 系列在程序化任务（Web Shopping）上表现优异，但在需要通用推理的 Digital Card Game 上不如 LLaMA-2 系列。这表明代码语料训练可能强化了模型的过程性思维，但同时弱化了其通用推理的灵活性。</p>\n<p><strong>高质量对齐数据的价值</strong>：Vicuna-13b 与 LLaMA-2-13b 共享相同基座模型，但 Vicuna 通过在 ShareGPT（GPT-4 生成数据）上进行对齐训练，在 AgentBench 上显著优于 LLaMA-2-13b，甚至与 3 倍参数量的 CodeLlama-34b 持平。这证明 <strong>高质量多轮交互对齐数据是提升 LLM Agent 能力的关键路径</strong>。</p>\n<div class=\"warn-box\">⚠️ 注意：尽管代码训练能提升部分 Agent 任务表现，但其效果因任务类型而异，并非万能策略。在追求 Agent 能力提升时，需要在过程性执行能力和通用推理能力之间寻找平衡。</div>\n<h5>5. 与先前工作的区别</h5>\n<ul>\n<li>vs 传统 NLP 基准（MMLU、HumanEval）：AgentBench 强调<strong>多轮交互</strong>和<strong>环境反馈</strong>，而非单轮静态评估，更贴近真实 Agent 应用场景。</li>\n<li>vs 现有 Agent 评测（WebArena、InterCode）：AgentBench 是首个覆盖 Code/Game/Web 三类环境的<strong>多维度统一评测框架</strong>，且提供了完整的统一评估工具包。</li>\n<li>评估模式上，AgentBench 将评测对象从\"语言模型\"转变为\"自主代理\"，关注的是在不确定环境中的连续决策能力，而非孤立的语言理解或生成能力。</li>\n</ul>",
      "quiz": {
        "q": "在 AgentBench 的评估中，导致 LLM Agent 任务失败最普遍的原因是什么？",
        "options": [
          "Context Limit Exceeded（上下文超限）",
          "Invalid Format（无效格式）",
          "Invalid Action（无效动作）",
          "Task Limit Exceeded（任务轮次超限，即长程推理不足）"
        ],
        "answer": 3,
        "explain": "从 Table 4 的失败原因分布可以看出，TLE 在 KG（67.9%）、LTP（82.5%）等多个环境中占比极高，是导致任务未完成的最普遍原因，反映了 LLM 在多轮交互中长程推理和决策能力的不足。"
      }
    },
    {
      "id": "swebench",
      "num": 3,
      "name": "SWE-bench",
      "fullName": "软件工程问题基准 (SWE-bench)",
      "year": "2023.10",
      "org": "Princeton",
      "parent": "agentbench",
      "paperUrl": "https://arxiv.org/abs/2310.06770",
      "projectUrl": "",
      "category": "evaluation",
      "motivation": "用真实GitHub issue评测代码代理",
      "summary": "> SWE-bench 提出了一个基于真实 GitHub 仓库 issue 的代码修复基准，包含 2294 个任务实例覆盖 12 个流行 Python 仓库，通过执行式单元测试验证模型生成的代码补丁，揭示了当前最强大模型（Claude 2）在 oracle 检索设置下也仅能解决 4.8% 的问题，同时开源了面向代码修复微调的 SWE-Llama 模型系列。",
      "keyPoints": [
        "<strong>基准规模与范围</strong>：2294 个任务实例，来自 12 个 GitHub 仓库（django、scikit-learn、matplotlib、sympy、pytest、requests、flask、sphinx、pylint、astropy、xarray、seaborn），时间跨度约 6 年",
        "<strong>任务形式</strong>：给定 issue 描述文本 + 完整代码库，要求模型生成补丁（patch）文件来解决 issue 描述的问题",
        "<strong>评估方法</strong>：执行式验证（execution-based evaluation）——在代码库中应用模型生成的补丁后运行相关单元测试，测试通过即视为解决",
        "<strong>检索设置对比</strong>：两种上下文提供方式——(1) oracle 检索（直接提供 gold patch 所涉及文件的完整内容）(2) BM25 检索（基于 issue 文本自动检索相关文件）",
        "<strong>主要结果</strong>：Claude 2 在 oracle 设置下解决率 4.8%，BM25 下仅 1.96%；GPT-4 分别为 1.63%（25% 随机子集）和 0.82%；Code Llama 在 oracle 下仅 0.2%",
        "<strong>SWE-Llama 微调模型</strong>：基于 Code Llama 在 SWE-bench 训练集上微调，SWE-Llama 13b 在 oracle 下达 4.0%（约等于 Claude 2），BM25 下仅 0.7%，揭示了对上下文分布偏移的敏感性",
        "<strong>关键发现</strong>：模型生成的补丁通常比 gold patch 短一半以上，倾向于只编辑单个文件，且生成的代码更简单、不充分利用第三方库或代码库结构",
        "<strong>开源贡献</strong>：完整基准、评估框架、训练数据、SWE-Llama 模型权重全部开源"
      ],
      "detail": "<p><img alt=\"SWE-bench 示意图\" src=\"https://ar5iv.labs.arxiv.org/html/2310.06770/assets/x1.png\" />\n<em>图：SWE-bench 的核心框架或评测示意。</em></p>\n<h5>1. 任务构建流程</h5>\n<p>SWE-bench 的任务实例构建遵循完整的 open-source 协作流水线：</p>\n<pre><code>PyPI Top 5000 → Top 100 → 筛选有许可证的 GitHub 仓库\n→ 收集所有PR → 筛选Merged+关联Issue+引入新测试的PR\n→ 提取 base commit（修复前代码库状态）+ issue文本（问题描述）\n→ 分离 test patch（测试用例）+ gold patch（实际修复方案）\n→ 执行式验证（确保gold patch通过测试，未修改代码库时测试失败）\n</code></pre>\n<p><strong>关键设计选择</strong>：\n- <strong>与现有基准的区别</strong>：不同于 HumanEval（单函数补全，~175 tokens 上下文）或 Defects4J（Java 单语句 bug），SWE-bench 要求模型在数万行代码上下文中定位并修复 bug，上下文长度中位数约为 110K tokens\n- <strong>issue 文本处理</strong>：聚合 PR 关联的所有 issue 的标题、正文及在 PR 首次提交时间戳之前的评论，避免泄露解决方案信息\n- <strong>补丁验证的两阶段检查</strong>：(1) 未修改代码库时，必须至少有一项测试失败（确认 bug 真实存在）；(2) 应用 gold patch 后，所有相关测试必须通过</p>\n<h5>2. 评估框架</h5>\n<p>模型生成的补丁通过 <code>git apply</code> 应用到代码库后，运行事先提取的测试用例：</p>\n<ul>\n<li><strong>通过标准</strong>：所有测试通过（<code>before → fail</code> 且 <code>after → pass</code>）</li>\n<li><strong>评测指标</strong>：成功率（resolved rate）= 通过的实例数 / 总实例数</li>\n<li><strong>过滤机制</strong>：仅统计用户仓库运行成功的实例（不同模型的评分基于各自成功运行的任务子集，所有主实验基于 2294 个实例中至少一种设置下通过的实例）</li>\n</ul>\n<h5>3. 检索策略</h5>\n<p>两种上下文检索策略对比如下：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>策略</th>\n<th>方式</th>\n<th>上下文大小</th>\n<th>结果（Claude 2）</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><strong>Oracle</strong></td>\n<td>直接提供 gold patch 编辑过的所有文件的完整内容</td>\n<td>平均 ~6000 行</td>\n<td>4.8%</td>\n</tr>\n<tr>\n<td><strong>BM25</strong></td>\n<td>基于 issue 文本 + 代码库所有文件内容检索 Top-K</td>\n<td>平均 ~8000 行</td>\n<td>1.96%</td>\n</tr>\n</tbody>\n</table></div>\n<p>Oracle 设置虽然提供了\"作弊\"级别的文件定位线索，但即使在此设置下顶级模型表现也极低，说明主要瓶颈不在检索而在于代码理解与编辑能力。</p>\n<h5>4. SWE-Llama 训练</h5>\n<ul>\n<li><strong>基础模型</strong>：Code Llama 7b 和 13b（Rozière et al. 2023）</li>\n<li><strong>训练数据</strong>：从 SWE-bench 训练集中选取 19183 个补丁文件，每条训练样本包含 [issue 描述, 代码库上下文, 目标补丁]</li>\n<li><strong>训练配置</strong>：LoRA 微调（Hu et al. 2022），Flash Attention（Dao et al. 2022），Deepspeed Ulysses（Jacobs et al. 2023）支持长序列</li>\n<li><strong>关键发现</strong>：SWE-Llama 13b oracle 性能 (4.0%) 接近 Claude 2 (4.8%)，但在 BM25 下骤降至 0.7%，说明微调模型对上下文分布偏移极为敏感——模型在训练时学会了编辑上下文中的每个文件，而在 BM25 设置下许多检索到的文件实际上不需要修改</li>\n</ul>\n<h5>5. 深度分析发现</h5>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>现象</th>\n<th>数据证据</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>模型倾向短编辑</td>\n<td>模型生成补丁平均总行数 30.1 vs gold patch 74.5 行</td>\n</tr>\n<tr>\n<td>单文件编辑倾向</td>\n<td>模型几乎所有成功补丁仅编辑 1 个文件 vs gold patch 平均 1.7 个文件</td>\n</tr>\n<tr>\n<td>不利用代码库结构</td>\n<td>模型倾向写\"原始 Python 代码\"，不使用第三方库或项目已有工具函数</td>\n</tr>\n<tr>\n<td>\"贪婪\"修复策略</td>\n<td>模型精确解决表面问题，缺乏 gold patch 中的结构性改进和预防性修复</td>\n</tr>\n<tr>\n<td>生成补丁比生成全文件容易</td>\n<td>Claude 2 生成全文件仅 2.2% vs 生成补丁 4.8%（oracle 设置）</td>\n</tr>\n<tr>\n<td>难度与时间无关</td>\n<td>2023 年前后的 issue 解决率无明显差异，排除模型\"背诵\"嫌疑</td>\n</tr>\n</tbody>\n</table></div>\n<h5>6. 案例分析（sphinx-doc__sphinx-8713）</h5>\n<ul>\n<li><strong>问题</strong>：Sphinx 文档生成器的 napoleon 扩展在 <code>napoleon_use_param=True</code> 时未正确格式化 \"Other Parameters\" 文档关键字</li>\n<li><strong>Gold patch</strong>：修改 <code>_parse_other_parameters_section</code> 函数，先检查配置设置，再复制 <code>_parse_parameters_section</code> 的行为</li>\n<li><strong>模型方案</strong>：编辑了正确的函数，但错误地假设 <code>napoleon_use_param</code> 始终为 True，导致在配置为 False 的测试中失败</li>\n<li><strong>暴露问题</strong>：模型可以定位相关代码，但缺乏对配置条件的细粒度推理能力</li>\n</ul>\n<h5>7. 数据集特性统计</h5>\n<ul>\n<li><strong>仓库覆盖</strong>：12 个仓库涵盖 Web 框架（django、flask）、数据科学（scikit-learn、matplotlib、sympy、xarray、seaborn）、测试工具（pytest）、HTTP 库（requests）、文档工具（sphinx）、代码检查（pylint）、天文学（astropy）</li>\n<li><strong>实例分布</strong>：django (917)、sympy (487)、matplotlib (254)、sphinx (229)、scikit-learn (131) 等</li>\n<li><strong>测试粒度</strong>：每个实例平均包含 3-5 个新增测试函数，涵盖不同测试风格（unittest、pytest）</li>\n<li><strong>时间跨度</strong>：PR 创建时间从 2017 年至 2023 年 6 月</li>\n</ul>\n<h5>8. 局限性与未来方向</h5>\n<ul>\n<li>当前仅支持 Python 语言，计划扩展到更多编程语言</li>\n<li>实验仅建立最简单直接的基线方法，鼓励未来探索 agent-based 方法、更大规模微调、与程序分析工具结合</li>\n<li>仅依赖执行式测试不足以保证补丁质量——模型生成往往不如人类编写的方案全面、高效或可读</li>\n</ul>",
      "quiz": {
        "q": "SWE-bench 中设置 oracle 检索与 BM25 检索对比，最主要是为了区分哪两类能力瓶颈？",
        "options": [
          "代码生成速度与测试执行速度",
          "文件定位/检索能力与真正的代码理解编辑能力",
          "监督微调能力与强化学习能力",
          "单文件补丁与多文件补丁的磁盘占用差异"
        ],
        "answer": 1,
        "explain": "Oracle 直接给出 gold patch 涉及文件，几乎消掉文件检索问题；若此时成绩仍低，就说明更深的瓶颈在代码理解、条件推理与补丁编辑，而不仅是找错文件。"
      }
    },
    {
      "id": "osworld",
      "num": 4,
      "name": "OSWorld",
      "fullName": "真实操作系统环境 (OSWorld)",
      "year": "2024.04",
      "org": "HKU/CMU",
      "parent": "webarena",
      "paperUrl": "https://arxiv.org/abs/2404.07972",
      "projectUrl": "",
      "category": "environment",
      "motivation": "把浏览器任务扩展到真实操作系统",
      "summary": "OSWorld 是首个面向多模态智能体的**真实操作系统benchmark**，在 Ubuntu/Windows/macOS 三种OS上提供369个开放任务，通过**执行结果评估**（execution-based eval）暴露了当前最佳模型仅12.24%成功率（人类72.36%）的巨大差距，核心瓶颈在于**GUI视觉grounding**和**操作知识（operational knowledge）**。",
      "keyPoints": [
        "核心动机：把浏览器任务扩展到真实操作系统",
        "演化来源：继承或改进自 webarena",
        "代表机构：HKU/CMU"
      ],
      "detail": "<p><img alt=\"OSWorld 示意图\" src=\"https://ar5iv.labs.arxiv.org/html/2404.07972/assets/x1.png\" />\n<em>图：OSWorld 的核心框架或评测示意。</em></p>\n<h5>1. Benchmark 设计空间</h5>\n<p>OSWorld 将真实OS操作建模为<strong>开放式的多模态交互任务</strong>：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>维度</th>\n<th>设计选择</th>\n<th>意义</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><strong>环境</strong></td>\n<td>Ubuntu 22.04, Windows 11, macOS 14 虚拟机</td>\n<td>首次覆盖三大主流OS，逼真度远超网页/模拟器</td>\n</tr>\n<tr>\n<td><strong>交互方式</strong></td>\n<td>截图+鼠标键盘动作，支持坐标点击、拖拽、文本输入、快捷键、滚动等</td>\n<td>模拟真实用户操作，考验agent的视觉理解和动作规划</td>\n</tr>\n<tr>\n<td><strong>任务来源</strong></td>\n<td>从用户手册、在线教程、日常办公场景中挖掘</td>\n<td>保证生态效度</td>\n</tr>\n<tr>\n<td><strong>评估方式</strong></td>\n<td>VM快照+确定性验证脚本（bash/powershell/python）</td>\n<td>客观、可复现、无需人工评判</td>\n</tr>\n<tr>\n<td><strong>初始化</strong></td>\n<td>每个任务从已知的VM快照启动，保证公平比较</td>\n<td>消除初始状态差异</td>\n</tr>\n</tbody>\n</table></div>\n<h5>2. 任务分布</h5>\n<pre><code>369 tasks 分类：\n├── OS基础操作（文件管理/系统设置/终端）    ~30%\n├── 办公软件（LibreOffice/Word/Excel/PPT）  ~25%\n├── 图像/视频处理（GIMP/画图/剪辑）         ~15%\n├── Web浏览器（Chrome/Firefox/Safari）      ~15%\n├── 编程/IDE（VSCode/Xcode/终端）           ~10%\n└── 其他（邮件/日历/系统工具）              ~5%\n</code></pre>\n<h5>3. 评估协议详解</h5>\n<p>OSWorld 的 execution-based 评估流程：</p>\n<pre><code>Start → 加载任务 → 读取task_config（指令+初始VM快照+评估脚本路径）\n  → Agent迭代循环（max 15 steps per task）：\n      1. 获取当前截图\n      2. 产生动作（点击坐标/输入文本/快捷键/函数调用）\n      3. 在VM中执行动作，等待UI更新\n      4. 返回新截图\n  → 15步后终止（或agent主动结束）\n  → 回滚VM到快照 → 执行评估脚本 → 输出0/1\n</code></pre>\n<p><strong>动作空间</strong>:</p>\n<pre><code class=\"language-python\"># OSWorld 支持的动作类型\nAction = Union[\n    ClickAction(x=int, y=int, button=&quot;left&quot;|&quot;right&quot;|&quot;middle&quot;),\n    TypeAction(text=str),                    # 键盘输入\n    KeyPressAction(keys=List[str]),           # 组合键如[&quot;ctrl&quot;,&quot;c&quot;]\n    ScrollAction(dx=int, dy=int),            # 滚轮\n    DragAction(start_x, start_y, end_x, end_y),\n    WaitAction(seconds=float),               # 等待UI更新\n    FunctionCallAction(name, args),          # 调用预定义工具函数\n    TerminateAction()                        # 主动结束任务\n]\n</code></pre>\n<h5>4. 核心实验结果</h5>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>Agent</th>\n<th>总成功率</th>\n<th>Ubuntu</th>\n<th>Windows</th>\n<th>macOS</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><strong>Random</strong></td>\n<td>~0%</td>\n<td>-</td>\n<td>-</td>\n<td>-</td>\n</tr>\n<tr>\n<td><strong>GPT-4V (zero-shot)</strong></td>\n<td>12.24%</td>\n<td>15.1%</td>\n<td>9.8%</td>\n<td>11.7%</td>\n</tr>\n<tr>\n<td><strong>Gemini-Pro-Vision</strong></td>\n<td>8.67%</td>\n<td>-</td>\n<td>-</td>\n<td>-</td>\n</tr>\n<tr>\n<td><strong>Claude-3-Opus</strong></td>\n<td>11.11%</td>\n<td>-</td>\n<td>-</td>\n<td>-</td>\n</tr>\n<tr>\n<td><strong>GPT-4V + RCI (self-refine)</strong></td>\n<td>13.82%</td>\n<td>-</td>\n<td>-</td>\n<td>-</td>\n</tr>\n<tr>\n<td><strong>GPT-4V + GT grounding</strong></td>\n<td>34.15%</td>\n<td>-</td>\n<td>-</td>\n<td>-</td>\n</tr>\n<tr>\n<td><strong>Human (crowdworkers)</strong></td>\n<td><strong>72.36%</strong></td>\n<td>81.0%</td>\n<td>67.3%</td>\n<td>67.9%</td>\n</tr>\n</tbody>\n</table></div>\n<p><strong>关键发现</strong>:\n- <strong>视觉grounding是最大瓶颈</strong>: 如果给模型ground-truth坐标（GT grounding），性能提升2.8倍(12.24%→34.15%)\n- <strong>操作知识同样致命</strong>: 即使知道点哪里，模型仍因缺乏领域操作知识（如GIMP具体工具用法、系统设置嵌套路径）而频繁失败\n- <strong>跨OS差距显著</strong>: Ubuntu任务成功率高于Windows/macOS，可能与训练数据分布和Ubuntu UI更规则有关</p>\n<h5>5. 主要失败模式分析</h5>\n<pre><code>失败分布（基于GPT-4V 200+ case分析）：\n├── 视觉grounding错误              ~38%\n│   ├── 未能找到目标UI元素\n│   ├── 坐标偏移（点击到相邻元素）\n│   └── 对UI状态误判（如认为按钮可点击）\n├── 操作知识缺失                  ~32%\n│   ├── 不知道特定软件的操作流程\n│   ├── 错误的功能调用\n│   └── 缺乏跨步骤的依赖推理\n├── 规划/推理失败                  ~18%\n│   ├── 子目标分解错误\n│   └── 遗漏关键步骤\n├── 动作格式/语法错误              ~8%\n└── 其他（超时/意外弹窗等）         ~4%\n</code></pre>\n<h5>6. Agent 架构范式对比</h5>\n<p>OSWorld 测试了三种范式：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>范式</th>\n<th>代表方法</th>\n<th>做法</th>\n<th>效果</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><strong>Direct prompting</strong></td>\n<td>GPT-4V vanilla</td>\n<td>截图+任务描述→直接输出动作</td>\n<td>基线12.24%</td>\n</tr>\n<tr>\n<td><strong>Self-refine</strong></td>\n<td>RCI (Retry-Critique-Improve)</td>\n<td>agent失败后自我审视并修正</td>\n<td>+1.6%（有限改进）</td>\n</tr>\n<tr>\n<td><strong>Grounded prompting</strong></td>\n<td>Set-of-Marks (SoM) + 标注截图</td>\n<td>预标注UI元素ID，agent引用ID</td>\n<td>+12%（需grounding模型）</td>\n</tr>\n</tbody>\n</table></div>\n<h5>7. 与相邻Benchmark的关系</h5>\n<pre><code>WebArena (2023) ──→ OSWorld (2024)\n  Web环境             真实OS环境\n  HTML DOM访问         纯视觉（截图）\n  812 tasks            369 tasks\n  答案匹配评估          执行结果评估\n  4类网站              Ubuntu/Windows/macOS\n\nVisualWebArena ─―→ OSWorld-Web子集 (Chrome/Firefox任务)\n  Web视觉agent          OS内嵌浏览器agent\n  仅网页                OS原生浏览器\n\nWindowsAgentArena ─→ OSWorld-Windows子集\n  仅Windows            Windows+Ubuntu+macOS\n  特定UWP应用           更广泛的桌面应用\n</code></pre>\n<p><strong>核心差异</strong>: OSWorld 是第一个在<strong>真实VM环境</strong>中进行<strong>execution-based评估</strong>的<strong>多OS</strong> benchmark，填补了从简化模拟器到真实OS的鸿沟。</p>\n<h5>8. 实践建议（给agent开发者）</h5>\n<ol>\n<li><strong>视觉grounding先行</strong>: 在接入OSWorld前，确保agent在ScreenSpot/GUI grounding子任务上有足够能力</li>\n<li><strong>操作知识注入</strong>: 为特定软件（GIMP/LibreOffice/系统设置）提供检索增强的文档知识库至关重要</li>\n<li><strong>错误恢复机制</strong>: 15步限制下，agent需要能在第3-5步发现错误时回退和重规划</li>\n<li><strong>多OS适配</strong>: 必须建立OS-aware的prompt策略，如Windows的任务栏在底部、macOS的菜单栏在顶部</li>\n<li><strong>评估脚本即spec</strong>: tasks/<id>/evaluate.py 本身就是最佳的任务理解素材</li>\n</ol>\n<h5>9. 技术栈与可复现性</h5>\n<ul>\n<li><strong>VM管理</strong>: QEMU/KVM (Ubuntu), VMware/Parallels (Windows/macOS)</li>\n<li><strong>Agent-VM通信</strong>: 自定义gRPC服务，截图通过VNC/Spice获取</li>\n<li><strong>动作注入</strong>: 对于Ubuntu用xdotool/evemu, Windows用WinAppDriver, macOS用CGEvents/AppKit</li>\n<li><strong>完整开源</strong>: GitHub仓库包含所有VM镜像下载、任务JSON、评估脚本、基线agent代码</li>\n</ul>\n<blockquote>\n<p><strong>📖 一句话推荐</strong>: 如果你只做GUI agent，读这篇就够了——它用369个真实任务和execution-based eval告诉你：视觉grounding和操作知识是AGI在桌面任务上的最后两道墙。</p>\n<p><strong>🔗 资源链接</strong>: <a href=\"https://arxiv.org/abs/2404.07972\">Paper</a> | <a href=\"https://github.com/xlang-ai/OSWorld\">Code</a> | <a href=\"https://os-world.github.io\">Leaderboard</a></p>\n</blockquote>",
      "quiz": {
        "q": "OSWorld 论文中，给模型提供 ground-truth 坐标后成功率大幅提升，最直接说明了什么？",
        "options": [
          "主要瓶颈在评估脚本错误，而不是 agent 本身",
          "主要瓶颈之一是 GUI 视觉 grounding，而不是纯粹的高层任务理解",
          "说明 execution-based evaluation 低估了真实能力",
          "说明多操作系统环境并不会增加任务难度"
        ],
        "answer": 1,
        "explain": "论文报告 GT grounding 会把成功率从约 12% 提升到约 34%，这直接表明大量失败来自看不准、点不准界面元素，而不是只是不知道目标是什么。"
      }
    },
    {
      "id": "toolsandbox",
      "num": 5,
      "name": "ToolSandbox",
      "fullName": "状态化工具沙箱 (ToolSandbox)",
      "year": "2024.08",
      "org": "Apple",
      "parent": "agentbench",
      "paperUrl": "https://arxiv.org/abs/2408.04682",
      "projectUrl": "",
      "category": "environment",
      "motivation": "评测多轮状态依赖的工具调用",
      "summary": "ToolSandbox 提出了首个融合**有状态工具**（Stateful Tools）、**对话式评估**（Conversational Evaluation）和**交互式评估**（Interactive Evaluation）三位一体的 LLM 工具使用基准测试，通过 Milestone/Minefield 机制提供细粒度的中间过程评估，揭示了当前模型在状态依赖推理、规范化（Canonicalization）和信息不足场景下的显著不足，开源与闭源模型间存在超过 20 分的性能鸿沟。",
      "keyPoints": [
        "提出 <strong>ToolSandbox 基准测试框架</strong>，包含 34 个工具、覆盖消息通信、世界状态管理等真实场景",
        "三大创新维度：<strong>Stateful Tools</strong>（工具执行结果持久化改变世界状态）、<strong>State Dependent Tools</strong>（工具行为受当前世界状态影响）、<strong>Canonicalization</strong>（同一实体的多种指称归一化）",
        "核心架构三组件：<strong>Message Bus</strong>（消息总线解耦 LLM-Tools-User 三方通信）、<strong>World State</strong>（键值对存储追踪工具副作用）、<strong>User Simulator</strong>（LLM 驱动的模拟用户，支持工具辅助增强）",
        "提出 <strong>Milestone</strong>（里程碑，检查中间步骤是否达到预期状态）和 <strong>Minefield</strong>（雷区，检测违规操作如信息泄露、重复调用），实现对话过程的细粒度评估",
        "定义三种测试场景类别：<strong>Single-Turn</strong>（单轮工具调用）、<strong>Multi-Turn Non-Interactive</strong>（多轮无用户交互）、<strong>Multi-Turn Interactive</strong>（多轮含用户模拟交互）",
        "实验发现：闭源模型整体领先开源模型 &gt;20 分；State Dependency 是最大挑战（所有模型表现最差）；大模型存在反直觉的 State Dependency 性能退化；信息不足场景中模型倾向于\"猜测\"而非主动询问澄清"
      ],
      "detail": "<p><img alt=\"ToolSandbox 系统架构图\" src=\"https://ar5iv.labs.arxiv.org/html/2408.04682/assets/x1.png\" />\n<em>图：ToolSandbox 整体架构——LLM Agent 通过 Message Bus 与工具（Tools）和用户模拟器（User Simulator）交互，World State 记录所有工具执行产生的持久副作用。</em></p>\n<h5>伪代码：Milestone &amp; Minefield 评估机制</h5>\n<pre><code class=\"language-python\"># ToolSandbox 核心评估流程（简化）\ndef evaluate_conversation(llm_agent, scenario, milestones, minefields):\n    world_state = {}  # 初始化世界状态（键值对存储）\n    messages = [scenario.system_prompt, scenario.initial_user_message]\n    milestone_results = []\n    minefield_triggers = []\n\n    for turn in range(scenario.max_turns):\n        # 1. LLM 生成工具调用或文本回复\n        llm_response = llm_agent(messages, available_tools)\n\n        # 2. 执行工具调用，更新 World State\n        for tool_call in llm_response.tool_calls:\n            result = execute_tool(tool_call, world_state)\n            world_state.update(result.state_changes)  # W' = f_tool(W, params)\n            messages.append(result)\n\n        # 3. Milestone 检查（在指定轮次检查中间状态）\n        for ms in milestones_by_turn[turn]:\n            if ms.condition(world_state, messages):\n                milestone_results.append({&quot;milestone&quot;: ms, &quot;passed&quot;: True})\n            else:\n                milestone_results.append({&quot;milestone&quot;: ms, &quot;passed&quot;: False})\n\n        # 4. Minefield 检测（检查是否触发违禁行为）\n        for mf in minefields:\n            if mf.pattern_matches(messages, world_state):\n                minefield_triggers.append({&quot;minefield&quot;: mf, &quot;turn&quot;: turn})\n\n    # 最终 World State 匹配度评分\n    final_state_score = compute_state_match(world_state, scenario.expected_state)\n    return final_state_score, milestone_results, minefield_triggers\n</code></pre>\n<h5>核心机制详解</h5>\n<p><strong>动机与背景</strong>：传统 LLM 工具使用评估（如 BFCL、ToolBench）存在三个根本缺陷：(1) 工具是<strong>无状态的</strong>——每次调用独立执行，无法模拟现实世界中诸如\"预订航班→修改预订→取消预订\"的连锁操作；(2) 评估是<strong>非对话式的</strong>——用静态测试集做选择题式评测，忽略了真实用户-助手交互的动态性；(3) 评估指标<strong>只看最终结果</strong>——忽略了中间步骤的正确性，无法区分\"碰巧答对\"和\"推理正确\"。</p>\n<div class=\"key-point\">💡 关键：ToolSandbox 的核心洞察在于——<strong>世界状态（World State）</strong> 是连接单次工具调用与长期推理的桥梁。每一次工具调用都会产生持久化的副作用（如创建日历事件、转账），后续工具的行为取决于这些累积的状态变化。</div>\n<p><strong>架构设计</strong>：系统由三层组成——\n- <strong>Message Bus</strong>：一个中介层，LLM Agent、工具、用户模拟器三者只与 Bus 通信而非彼此直连。这使得工具调用和用户消息可以被透明地拦截、记录和评估。\n- <strong>World State</strong>：用键值对字典 <code>{entity_id: {field: value}}</code> 存储所有工具执行产生的副作用。例如 <code>{\"calendar_event_42\": {\"title\": \"Meeting\", \"time\": \"3pm\"}}</code>。每轮工具调用后状态被更新，后续工具调用可以读取这些状态。\n- <strong>Milestone &amp; Minefield</strong>：评估指标不再只看最后一轮输出是否正确，而是在预设的<strong>中间轮次</strong>检查 World State 是否达到预期（Milestone）或是否触发了违规行为（Minefield）。</p>\n<div class=\"warn-box\">⚠️ 注意：Milestone 分为<strong>强制里程碑</strong>（必须在该轮恰好达成）和<strong>可选里程碑</strong>（在某轮及之前达成均可），后者的设计更贴近开放式对话的灵活性。</div>\n<p><strong>State Dependency 挑战</strong>：这是论文发现的最具挑战性的问题。当工具行为依赖于先前操作积累的 World State 时（例如：先创建日历事件 A，再创建事件 B 时需避开 A 的时间），LLM 需要在多次工具调用间维护一致的实体引用和状态推理。实验结果揭示了一个<strong>反直觉现象</strong>：某些闭源大模型在 State Dependent 场景下的性能反而不如简单的 Stateful 场景，说明增加状态依赖反而引入了干扰——模型可能\"过度思考\"而导致错误的状态推理。</p>\n<p><strong>User Simulator 设计</strong>：不同于传统基准的静态测试用例，ToolSandbox 使用 LLM 驱动的用户模拟器动态生成回复。模拟器接收场景描述（用户目标、偏好、约束）和对话历史，生成逼真的用户响应。为减少幻觉，论文引入了<strong>工具辅助用户模拟器</strong>——提供给模拟器一个 <code>end_conversation</code> 工具，实验表明这显著改善了对终止对话指令的遵循。</p>\n<p><strong>Canonicalization 问题</strong>：同一现实实体在对话中可能有多种指称方式（\"明天下午的会议\" vs \"3点的那个事件\" vs \"event_42\"），LLM 需要将这些表面形式归一化到 World State 中的唯一实体 ID。论文发现这是当前模型的显著短板——即使最终结果正确，中间过程中频繁出现实体引用混乱。</p>\n<p><strong>主要实验发现</strong>：(1) 闭源模型（GPT-4 系列）整体得分领先开源模型（Llama-3 系列）超过 20 分（满分 100）；(2) State Dependency 是所有模型得分最低的维度，即使最先进的 GPT-4 在该维度也仅勉强及格；(3) 在信息不足（Insufficient Information）场景中，多数模型倾向于直接调用工具\"猜测\"而非主动向用户寻求澄清——这是当前对齐训练的副作用；(4) Minefield 检测显示，模型最常见的违规行为是<strong>信息泄露</strong>（在输出中暴露了 World State 中的敏感数据）和<strong>重复冗余调用</strong>。</p>",
      "quiz": {
        "q": "ToolSandbox 中 Milestone 与 Minefield 的核心区别是什么？",
        "options": [
          "Milestone 在开头检查，Minefield 在结尾检查",
          "Milestone 检查中间状态是否达成预期，Minefield 检测是否触发违禁行为",
          "Milestone 用于单轮对话，Minefield 用于多轮对话",
          "Milestone 评估最终结果，Minefield 评估中间过程"
        ],
        "answer": 1,
        "explain": "Milestone 在指定轮次检查 World State 是否达到预设条件（正向指标），Minefield 实时检测模型是否触发了信息泄露、重复调用等违禁模式（负向指标），二者构成细粒度评估的正反两面。"
      }
    },
    {
      "id": "swe_verified",
      "num": 6,
      "name": "SWE-bench Verified",
      "fullName": "人工校验软件工程基准 (SWE-bench Verified)",
      "year": "2024.08",
      "org": "OpenAI",
      "parent": "swebench",
      "paperUrl": "https://openai.com/index/introducing-swe-bench-verified/",
      "projectUrl": "",
      "category": "evaluation",
      "motivation": "过滤原始样本噪声并标准化执行",
      "summary": "SWE-bench Verified 对原始 SWE-bench 中的 500 个代码修复任务进行了人工重新校验，剔除了含错误测试、模糊规格或执行环境不一致的噪声样本，最终保留 244 个高质量任务子集，将人机一致性从 67% 提升至 93%，为代码智能体提供了一个更可靠、更标准化的评估基准。",
      "keyPoints": [
        "对 SWE-bench 全部 500 个实例逐一进行人工审查，由专业软件工程师逐例验证任务描述、预期行为与测试用例的正确性",
        "发现约 50% 的原始样本存在不同程度的问题：测试断言错误、规格描述过度模糊、环境配置与预期版本不一致、补丁无法正确应用或评估",
        "最终构建 244 个实例的 \"Verified\" 子集，每个样本均包含精确的问题描述、经过人工确认的黄金补丁和正确执行的日志记录",
        "引入标准化的 Docker 容器执行框架，统一每个任务的运行环境和依赖版本，消除因环境差异导致的评估噪声",
        "建立严格的双通道评估协议：模型生成的补丁先在 Verified 子集上评估，再与原 SWE-bench 结果对比，暴露模型对噪声样本的过拟合倾向",
        "发布 verified 子集数据和标准化评估工具包为开源，支持一键复现评估结果并集成新的代码修复模型",
        "显著提升原始 SWE-bench 的人机一致性（从 67% 到 93%），证明过滤后的子集更准确地反映了模型的实际代码修复能力"
      ],
      "detail": "<p><img alt=\"SWE-bench Verified 示意图\" src=\"https://images.ctfassets.net/kftzwdyauwt9/6wYGm9QST2WYLbPJl5YwZC/1e63f3bfb458ce891db4f94a52052240/Codex_Blog_Header_V5.png?fm=webp&amp;q=90&amp;w=3840\" />\n<em>图：SWE-bench Verified 的核心框架或系统示意。</em></p>\n<p><strong>动机与背景</strong></p>\n<p>SWE-bench 自 2023 年发布以来，迅速成为评估大语言模型在真实软件工程任务上表现的事实标准。该基准从 GitHub 上提取了来自 12 个主流 Python 开源项目（如 Django、Flask、SymPy 等）的 2,294 个真实 issue 和对应的 pull request，要求模型根据 issue 描述定位文件并生成修复补丁，然后运行项目已有的测试套件判断修复是否成功。</p>\n<p>然而，随着越来越多的模型提交 SWE-bench 结果，一个根本性问题暴露出来：基准本身的噪声有多大？原始 SWE-bench 自动从 GitHub 挖掘 issue-PR 对，但 issue 的描述质量参差不齐（有的仅包含简单的 bug 复现步骤，有的则缺少关键上下文）、部分测试用例与实际 bug 无关、某些 PR 的修改范围超出了 issue 的描述范围。这些噪声使得评估结果可能无法真实反映模型的代码理解与修复能力——模型可能恰好蒙对了一个有问题的测试用例，或在噪声样本上表现出不稳定的分数波动。</p>\n<p>SWE-bench Verified 的核心问题意识在于：<strong>评估基准的人机一致性（human agreement）是衡量其可靠性的关键指标</strong>。如果人类专家按照基准的判分标准独立评估同一组样本，得到的结果差异很大，那么这个基准就缺乏评估效度。该工作通过对全部 500 个 Dev 集样本的人工再校验，建立了具有更高人机一致性的子集。</p>\n<p><strong>核心方法论：三阶段校验流程</strong></p>\n<p>SWE-bench Verified 的构建采用了三阶段人工校验流程，确保每一步的质量可控：</p>\n<ol>\n<li>\n<p><strong>第一阶段：实例理解与初步审查</strong>\n   每个样本首先由一名标注者进行整体审查，包括阅读 issue 描述、理解预期行为、检查关联的 PR 代码变更。标注者记录下任务描述是否清晰（\"能否仅从 issue 描述推断出应该修改什么？\"）、预期行为是否明确（\"修复后的代码应该如何工作？\"）、测试用例是否实际覆盖了 described 的 bug。</p>\n</li>\n<li>\n<p><strong>第二阶段：可执行性验证</strong>\n   在标准化的 Docker 环境中实际运行原始测试套件。验证点包括：基代码（问题修复前的代码版本）是否能成功安装依赖并运行测试；失败测试的断言是否确实与 issue 描述的 bug 相关；PR 中的补丁是否能干净地应用到基代码上；应用补丁后所有测试是否通过。</p>\n</li>\n<li>\n<p><strong>第三阶段：专家交叉复核</strong>\n   经过前两轮筛选后存疑的样本进入专家交叉复核。由另一位独立的软件工程师重新审查，最终标记为\"verified\"（确认无误）、\"discarded\"（存在问题，不可用）、或\"modified\"（修正问题描述或测试后可用）。</p>\n</li>\n</ol>\n<p><strong>标准化执行框架</strong></p>\n<p>原始 SWE-bench 在评估模型输出时存在执行环境不一致的问题：不同模版代码可能导致不同的文件结构，依赖包的版本可能影响测试通过与否。SWE-bench Verified 引入了完全容器化的标准化执行框架：</p>\n<pre><code class=\"language-docker\"># 每个实例在独立的 Docker 容器中执行，精确控制依赖版本\n# 统一的文件结构、预安装的测试框架、固定的评估脚本\nfor instance in verified_set:\n    docker = create_container(\n        repo=instance.repo,\n        base_commit=instance.base_commit,\n        dependencies=instance.frozen_requirements\n    )\n    docker.apply_patch(model_patch)\n    result = docker.run_tests(instance.test_list)\n</code></pre>\n<p>标准化的关键点包括：\n- 使用 Docker 镜像固化每个项目的 Python 版本、系统库和 pip 依赖\n- 统一补丁应用逻辑（使用 <code>git apply</code> 的标准行为）\n- 统一测试执行命令和日志捕获格式\n- 提供可重复使用的评估脚本和评分逻辑</p>\n<div class=\"key-point\">💡 <strong>关键设计</strong>：标准化框架确保了跨模型、跨时间的可复现评估 —— 任何人在任何时间运行同一个模型输出，都会得到完全相同的 pass/fail 结果。</div>\n<p><strong>与传统评估的区别与新发现</strong></p>\n<p>与原始 SWE-bench 直接使用从 GitHub 自动抓取的 issue-PR 对不同，Verified 子集的每个样本都经过人类专家的质量控制。这种质量控制带来了几个重要发现：</p>\n<ol>\n<li>\n<p><strong>近半数样本存在问题</strong>：原始 500 个样本中，只有 244 个（约 49%）通过了所有审查。这并不意味着一半的数据是 \"错误\" 的，而是它们的质量不足以支撑可靠的自动化评估。</p>\n</li>\n<li>\n<p><strong>噪声对模型排名的影响</strong>：在 Verified 子集上重新评估已发布模型的输出，发现某些模型的排名发生了变化。部分模型在原始 SWE-bench 上的高分可能得益于对噪声样本的 \"过度拟合\"——它们的策略恰好对齐了有问题的测试用例的预期，而非真实地理解并修复了 bug。</p>\n</li>\n<li>\n<p><strong>人机一致性的飞跃提升</strong>：原始 SWE-bench 的人机一致性约为 67%（即人类专家复查时，约有 33% 的评估结果与自动化判分不一致），而 Verified 子集将这一数字提升至 93%。这 26 个百分点的提升意味着 Verified 子集的评估结果高度可信——当模型被判定为 \"通过\" 时，人类专家有 93% 的概率认同这一结论。</p>\n</li>\n<li>\n<p><strong>保留原始可对比性</strong>：Verified 子集是原始 Dev 集的子集而非重新构建，因此所有在原始 SWE-bench 上已有的分数可以通过简单过滤 Verified 子集中的 244 个样本重新计算。这一设计使得新旧评估之间实现了平滑过渡。</p>\n</li>\n</ol>\n<p><strong>技术实践与工具链</strong></p>\n<p>SWE-bench Verified 不仅是一个数据集，更是一套完整的评估基础设施。其开源发布包含：</p>\n<ul>\n<li><strong>verified_244.json</strong>：包含 244 个样本的完整信息，每个样本都附带人类专家的校验注释、验证通过的执行日志</li>\n<li><strong>标准评估脚本</strong>：一个 Python 脚本可以一键运行所有 244 个任务的评估，输出详细的 pass/fail 列表和按仓库分组的统计</li>\n<li><strong>Docker 镜像和构建文件</strong>：可在任何支持 Docker 的机器上复现评估环境</li>\n<li><strong>评分协议</strong>：明确的评分规约，定义了什么是 \"正确的修复\"（应用后可让所有相关测试通过即为通过，不要求与人类 PR 完全一致）</li>\n</ul>",
      "quiz": {
        "q": "SWE-bench Verified 与原始 SWE-bench 的核心区别是什么？",
        "options": [
          "新增了更多来自其他编程语言的代码修复任务",
          "通过人工校验过滤了原始数据中的噪声样本，并标准化了评估执行环境",
          "将评估范围从代码修复改为代码生成",
          "采用自动化脚本替代人工进行测试用例编写"
        ],
        "answer": 1,
        "explain": "SWE-bench Verified 的核心贡献是对原始 SWE-bench 的 500 个样本进行人工审查，剔除了约 50% 存在测试不准确或环境不一致的样本，并引入标准化 Docker 执行框架，将人机一致性从 67% 提升至 93%。"
      }
    },
    {
      "id": "browsergym",
      "num": 7,
      "name": "BrowserGym",
      "fullName": "网页智能体训练场生态 (BrowserGym)",
      "year": "2024.12",
      "org": "ServiceNow",
      "parent": "webarena",
      "paperUrl": "https://arxiv.org/abs/2412.05467",
      "projectUrl": "",
      "category": "environment",
      "motivation": "统一多种网页基准的观测动作接口",
      "summary": "BrowserGym 提出了统一的 Gymnasium 风格环境与 AgentLab 实验平台，将 MiniWoB++、WebArena、WorkArena 等异构网页基准接入统一的观测-动作接口，使不同 LLM/VLM 驱动的网页智能体能在完全可复现的条件下进行系统化评估与对比。",
      "keyPoints": [
        "提出双层生态系统：<strong>BrowserGym</strong>（统一环境层）+ <strong>AgentLab</strong>（实验管理层），解耦环境接口与实验编排",
        "基于 Gymnasium API 定义统一的观测空间（DOM、AXTree、截图等多模态）与动作空间（click、type、scroll 等基本操作集）",
        "原生支持 MiniWoB++、WebArena、WorkArena、WebLINX 四大基准，新基准只需实现一套薄适配层",
        "AgentLab 提供可复现实验：固定 seed、环境快照、完整配置追踪，支持大规模并行实验",
        "内置 LLM/VLM 推理集成（OpenAI、Anthropic、HuggingFace 等），开箱即用的一键实验对比",
        "开源生态（Apache 2.0），社区可灵活扩展新 benchmark 和新 agent 策略"
      ],
      "detail": "<h5>核心示意图</h5>\n<p><img alt=\"BrowserGym 生态系统总览\" src=\"https://ar5iv.labs.arxiv.org/html/2412.05467/assets/x1.png\" /></p>\n<p><em>图：BrowserGym 生态系统总览，包括 AgentLab、BrowserGym 以及所支持的网页智能体基准的关系。</em></p>\n<h5>动机与背景</h5>\n<p>传统网页智能体研究面临两大瓶颈：一是不同基准（MiniWoB++、WebArena 等）各自定义观测格式与动作接口，智能体无法直接跨基准迁移；二是实验可复现性差——缺乏标准化的环境管理、配置追踪和结果汇报机制。BrowserGym 的目标是用一套统一的 API 封装所有网页交互基准，并提供 AgentLab 作为实验编排层，使得研究者只需关注智能体的高层决策策略。</p>\n<h5>核心机制：双层架构</h5>\n<h6>BrowserGym — 统一环境层</h6>\n<p>BrowserGym 基于 Gymnasium 接口，将网页交互抽象为标准的 RL 环境。其核心设计在于定义了一套<strong>通用的观测空间</strong>和<strong>动作空间</strong>：</p>\n<ul>\n<li><strong>观测空间（Observation Space）</strong>：每个 step 返回一个 dict，包含：</li>\n<li><code>DOM</code>：当前页面的 DOM 树或简化 HTML</li>\n<li><code>AXTree</code>：无障碍树（accessibility tree），适合视觉信息受限的场景</li>\n<li><code>Screenshot</code>：页面截图（numpy array），供 VLM 使用</li>\n<li><code>Goal</code>：当前任务的文本描述</li>\n<li>\n<p><code>ChatMessages</code>：多轮对话历史</p>\n</li>\n<li>\n<p><strong>动作空间（Action Space）</strong>：统一的高层动作集，包括：</p>\n</li>\n<li><code>click(element_id)</code> — 点击指定元素</li>\n<li><code>type(text, element_id)</code> — 在指定输入框内键入文本</li>\n<li><code>scroll(x, y)</code> / <code>go_back()</code> / <code>go_forward()</code> — 页面导航</li>\n<li><code>send_msg(text)</code> — 向用户发送消息（信息型任务）</li>\n<li><code>report_result(status)</code> — 任务完成时报告成功或失败</li>\n</ul>\n<div class=\"key-point\">💡 关键：所有基准共享相同的动作空间定义，但各基准内部的元素定位方式不同（如 MiniWoB++ 用数字 ID，WorkArena 用语义 bid），BrowserGym 在各适配层内部完成翻译，对外暴露统一接口。</div>\n<p>BrowserGym 的核心环境循环：</p>\n<pre><code>observation = env.reset(seed)\nfor step in range(max_steps):\n    action = agent.predict(observation)\n    next_observation, reward, terminated, truncated, info = env.step(action)\n</code></pre>\n<p>每个 benchmark 只需继承 <code>browsergym.core.env.BrowserEnv</code> 基类，实现 <code>_get_goal()</code> 和 <code>_get_task_metadata()</code> 等方法，即可接入生态系统。</p>\n<h6>AgentLab — 实验管理层</h6>\n<p>AgentLab 负责大规模实验的编排：</p>\n<ul>\n<li><strong>Agent 配置</strong>：通过 YAML 文件声明式定义 agent 所用的 LLM 后端、prompt 模板、策略参数等</li>\n<li><strong>Experiment Runner</strong>：支持并行启动多个环境实例，自动管理不同 seed 下的实验分配</li>\n<li><strong>结果追踪</strong>：每个实验自动记录完整的配置快照、每一步的轨迹（observation-action 对）、最终得分</li>\n<li><strong>Leaderboard</strong>：内置排行榜模块，可在不同 benchmark 上系统性对比不同 agent 的表现</li>\n</ul>\n<div class=\"key-point\">💡 关键：AgentLab 通过将所有配置参数（LLM 温度、prompt、随机 seed）纳入版本管理，实现了端到端的实验可复现。即使环境本身具有随机性，同一 seed 下的两次运行会产生完全一致的轨迹。</div>\n<h5>支持的基准</h5>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>基准</th>\n<th>任务数量</th>\n<th>特点</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>MiniWoB++</td>\n<td>125+</td>\n<td>合成微任务，测试原子级 web 操作能力</td>\n</tr>\n<tr>\n<td>WebArena</td>\n<td>812</td>\n<td>四个真实 web app 模拟，综合导航与表单</td>\n</tr>\n<tr>\n<td>WorkArena</td>\n<td>33</td>\n<td>ServiceNow 企业工作流，表单密集型</td>\n</tr>\n<tr>\n<td>WebLINX</td>\n<td>2337</td>\n<td>真实网站上的对话式演示转向任务</td>\n</tr>\n</tbody>\n</table></div>\n<p>每个基准在接入 BrowserGym 时，需要提供：\n1. <strong>任务集定义</strong>：包含任务 ID、类型（信息检索/表单操作等）、难度标签\n2. <strong>观测适配器</strong>：将该基准的页面表示转换为统一的 obs dict\n3. <strong>动作适配器</strong>：将统一动作映射为该基准的底层操作\n4. <strong>评估逻辑</strong>：判断任务是否成功完成的规则</p>\n<h5>与传统方法的区别</h5>\n<p>此前，研究者在不同基准上评估智能体时，需要处理完全不同的环境代码库和接口规范。例如，WebArena 提供自己的 task runner 和 evaluation script，MiniWoB++ 有自己的交互协议。这意味着：\n- 同一智能体需要针对每个基准编写不同的交互代码\n- 实验配置分散，难以整齐对比\n- 缺乏统一的 metrics 和 reporting 格式</p>\n<p>BrowserGym 通过抽象出<strong>基准无关</strong>的环境接口，将上述差异完全隐藏在适配层内部。AgentLab 进一步标准化了实验管理，使得\"在不同基准上跑同一个 agent\"与\"在同一基准上跑不同 agent\"都变成简单的配置切换。</p>\n<h5>训练/推理流程</h5>\n<ol>\n<li><strong>环境初始化</strong>：选择 benchmark、指定 seed，BrowserGym 启动对应的浏览器实例并加载目标任务</li>\n<li><strong>观测获取</strong>：环境返回当前的 DOM/AXTree/screenshot 等多模态观测</li>\n<li><strong>Agent 推理</strong>：Agent 将观测和 goal 打包为 LLM/VLM prompt，调用后端模型获取动作指令</li>\n<li><strong>动作执行</strong>：BrowserGym 将统一的动作（如 <code>click(42)</code>）翻译为基准特定的浏览器操作</li>\n<li><strong>状态更新与评估</strong>：环境返回新观测和奖励信号；AgentLab 记录该步轨迹</li>\n<li><strong>循环至终止</strong>：任务完成（主动 report_result）或达到 max_steps 上限</li>\n</ol>\n<p>AgentLab 在实验结束后自动汇总所有任务的成功率（success rate）、平均步数等指标，并生成可对比的报告。</p>",
      "quiz": {
        "q": "BrowserGym 生态系统中，AgentLab 主要负责什么？",
        "options": [
          "定义统一的浏览器观测与动作空间",
          "实验编排与管理，包括配置追踪、大规模并行运行和结果对比",
          "将 MiniWoB++ 的 DOM 树转换为无障碍树",
          "在浏览器端执行 click、type 等原子操作"
        ],
        "answer": 1,
        "explain": "AgentLab 是 BrowserGym 生态的实验管理层，负责 agent 配置管理、并行实验调度、轨迹记录与结果对比；环境接口的观测/动作定义由 BrowserGym 核心层完成。"
      }
    },
    {
      "id": "codex",
      "num": 8,
      "name": "Codex",
      "fullName": "云端编码代理 (Codex)",
      "year": "2025.05",
      "org": "OpenAI",
      "parent": "swe_verified",
      "paperUrl": "https://openai.com/index/introducing-codex/",
      "projectUrl": "",
      "category": "runtime",
      "motivation": "把代码基准能力产品化为云端代理",
      "summary": "OpenAI 将 GPT-4 在 SWE-bench Verified 等代码基准上的顶尖能力产品化为**云端编码代理 (Codex)**，通过安全沙箱执行、工具链集成和人工审批机制，实现了从「模型评测」到「产品级自主编程」的关键跨越。",
      "keyPoints": [
        "<strong>从基准到产品</strong>：Codex 并非新模型架构，而是将 OpenAI 最强的代码模型（基于 o1/o3 推理模型）的能力封装为云端代理，直接面向开发者生产力场景",
        "<strong>SWE-bench Verified 驱动</strong>：Codex 的核心能力基座来自 SWE-bench Verified 基准的前沿研究，该基准被用作代理能力的持续改进目标，经过专项强化后模型在该基准上达到领先水平",
        "<strong>安全执行沙箱</strong>：代码在隔离的云端沙箱中运行，集成 cgroup/eBPF 级别的系统遥测，高风险操作（文件写入、Shell 执行、网络请求）受策略引擎实时管控",
        "<strong>工具链深度集成</strong>：支持 GitHub 集成、IDE 插件（VS Code/JetBrains）、终端原生执行、CI/CD 管道嵌入等多形态部署",
        "<strong>Human-in-the-loop 审批</strong>：对于生产环境的破坏性操作（如数据库迁移、Prod 部署），Codex 强制走人工审批流程，确保 Agent 自主性与安全可控的平衡",
        "<strong>Reasoning-First 范式</strong>：Codex 内部采用「先推理后行动」的执行策略——模型首先生成包含修改计划、影响分析和风险判断的推理链，经内部验证后才生成代码补丁",
        "<strong>多步修复能力</strong>：支持跨多个文件、多个模块的复杂重构和 Bug 修复，自动追踪依赖关系，生成可复用的修复脚本",
        "<strong>上下文理解</strong>：能够主动探索整个代码仓库（包括不限于读取文件、查看 Git 历史、分析 Issue/PR 讨论），构建深度项目理解后再进行操作"
      ],
      "detail": "<p><img alt=\"Codex 示意图\" src=\"https://images.ctfassets.net/kftzwdyauwt9/6wYGm9QST2WYLbPJl5YwZC/1e63f3bfb458ce891db4f94a52052240/Codex_Blog_Header_V5.png?fm=webp&amp;q=90&amp;w=3840\" />\n<em>图：Codex 的核心框架或系统示意。</em></p>\n<h5>4.1 系统架构</h5>\n<p>Codex 的架构分为四层：用户接入层、推理层、执行沙箱层和安全治理层。</p>\n<pre><code>┌─────────────────────────────────────────────────┐\n│                 用户接入层                        │\n│  IDE 插件  │  CLI  │  GitHub App  │  API        │\n└──────────────────┬──────────────────────────────┘\n                   ▼\n┌─────────────────────────────────────────────────┐\n│                 推理层 (后端)                     │\n│  ┌──────────┐  ┌─────────┐  ┌───────────────┐  │\n│  │ 上下文    │  │ 推理模型  │  │ 工具调用编排   │  │\n│  │ 构建器    │→ │ (o1/o3) │→ │ (Tool Router) │  │\n│  └──────────┘  └─────────┘  └───┬───────────┘  │\n└─────────────────────────────────┼───────────────┘\n                                  ▼\n┌─────────────────────────────────────────────────┐\n│               执行沙箱层                          │\n│  ┌────────┐ ┌───────┐ ┌──────┐ ┌────────────┐  │\n│  │ 文件系统│ │Shell  │ │Git   │ │包管理器    │  │\n│  │ (Overlay)│ │(Sandbox)│ │(Clone)│ │(pip/npm)  │  │\n│  └────────┘ └───────┘ └──────┘ └────────────┘  │\n└──────────────────┬──────────────────────────────┘\n                   ▼\n┌─────────────────────────────────────────────────┐\n│               安全治理层                          │\n│  ┌───────────┐  ┌──────────┐  ┌─────────────┐  │\n│  │ 策略审批引擎 │  │ 遥测监控   │  │ 人工审批队列  │  │\n│  └───────────┘  └──────────┘  └─────────────┘  │\n└─────────────────────────────────────────────────┘\n</code></pre>\n<h5>4.2 推理流程：Reasoning-First 执行范式</h5>\n<p>Codex 的核心技术差异化在于其 <strong>Reasoning-First</strong> 流程，与传统 Agent «直接行动» 范式形成对比。</p>\n<p>处理的每个编码任务按以下流水线执行：</p>\n<ol>\n<li><strong>仓库探索阶段 (Repo Exploration)</strong></li>\n<li>自动遍历目标仓库的文件树，识别项目结构、构建系统、依赖关系</li>\n<li>读取相关源文件、测试文件、配置文件，构建上下文窗口</li>\n<li>\n<p>检索 Git 历史中与任务相关的 commit 和 PR 讨论</p>\n</li>\n<li>\n<p><strong>推理链生成 (Chain-of-Thought Reasoning)</strong></p>\n</li>\n<li>\n<p>模型生成结构化推理链，包含：</p>\n<ul>\n<li><strong>问题定位</strong>：根因分析，标识需要修改的文件和函数</li>\n<li><strong>方案评估</strong>：列举 2-3 个候选修复方案，分析各自的影响范围和风险</li>\n<li><strong>补丁设计</strong>：选定的最优方案的详细实现计划，包括 API 变更和测试策略</li>\n</ul>\n</li>\n<li>\n<p><strong>代码生成与验证 (Code Generation &amp; Verification)</strong></p>\n</li>\n<li>生成精确的代码补丁（diff/patch 格式）</li>\n<li>自动运行相关测试套件，验证补丁正确性</li>\n<li>\n<p>若测试失败，回溯到推理链阶段进行修正</p>\n</li>\n<li>\n<p><strong>输出交付 (Delivery)</strong></p>\n</li>\n<li>通过 PR/MR 或直接补丁的形式交付结果</li>\n<li>附带完整的修复说明和影响分析</li>\n</ol>\n<pre><code>用户请求（如&quot;修复 Issue #1234 的 null pointer&quot;）\n        │\n        ▼\n┌──────────────────────────────┐\n│  1. 仓库探索 (30-60s)         │\n│  遍历仓库结构 → 读取相关文件   │\n└──────────────┬───────────────┘\n               ▼\n┌──────────────────────────────┐\n│  2. 推理链生成 (15-30s)       │\n│  根因分析 → 方案评估 → 补丁设计│\n└──────────────┬───────────────┘\n               ▼\n┌──────────────────────────────┐\n│  3. 代码生成与验证 (10-60s)    │\n│  生成patch → 跑测试 → 修正     │\n└──────────────┬───────────────┘\n               ▼\n┌──────────────────────────────┐\n│  4. 交付 (实时)               │\n│  PR/MR + 修复说明 → 人工审批   │\n└──────────────────────────────┘\n</code></pre>\n<h5>4.3 工具链集成</h5>\n<p>Codex 提供多种集成形态以适应不同开发场景：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>集成方式</th>\n<th>适用场景</th>\n<th>核心能力</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>VS Code 插件</td>\n<td>个人开发者日常使用</td>\n<td>内联补全、多文件编辑、Chat 交互、终端命令建议</td>\n</tr>\n<tr>\n<td>JetBrains 插件</td>\n<td>企业级 IDE 用户</td>\n<td>深集成代码索引、重构建议、调试辅助</td>\n</tr>\n<tr>\n<td>GitHub App</td>\n<td>开源项目维护</td>\n<td>自动 Issue 分类、PR Review、自动修复 PR</td>\n</tr>\n<tr>\n<td>CLI 工具</td>\n<td>CI/CD 管道、批量任务</td>\n<td>批量 Bug 修复、代码迁移、大规模重构</td>\n</tr>\n<tr>\n<td>API</td>\n<td>第三方平台集成</td>\n<td>自定义工作流嵌入、企业内部工具对接</td>\n</tr>\n</tbody>\n</table></div>\n<h5>4.4 SWE-bench Verified 基准表现</h5>\n<p>Codex 在 SWE-bench Verified 基准上的迭代优化过程体现了从研究到产品的工程化路径：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>阶段</th>\n<th>模型版本</th>\n<th>SWE-bench Verified 得分</th>\n<th>关键改进</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>基线</td>\n<td>GPT-4o</td>\n<td>~33%</td>\n<td>基础编程能力</td>\n</tr>\n<tr>\n<td>第一阶段</td>\n<td>o1-preview</td>\n<td>~48%</td>\n<td>加入推理链</td>\n</tr>\n<tr>\n<td>第二阶段</td>\n<td>o1</td>\n<td>~53%</td>\n<td>强化代码特定推理</td>\n</tr>\n<tr>\n<td>第三阶段</td>\n<td>o3-mini</td>\n<td>~61%</td>\n<td>推理效率与准确度均衡</td>\n</tr>\n<tr>\n<td>产品化</td>\n<td>Codex (o3-based)</td>\n<td>~65%+</td>\n<td>集成工具链和安全沙箱</td>\n</tr>\n</tbody>\n</table></div>\n<blockquote>\n<p>📌 注：以上得分为公开文献中的近似值，实际值可能随基准更新而变化。Codex 的关键突破在于将推理模型的原始能力与工程化的错误恢复、多步修复工作流结合起来，大幅降低了实际场景中的失败率。</p>\n</blockquote>\n<h5>4.5 安全与合规</h5>\n<p>详见其子条目 <a href=\"../agent/harness/codex_safety_detail.md\">Codex Safety</a>。核心安全机制包括：</p>\n<ul>\n<li><strong>双层防御体系</strong>：事前策略审批（Pre-execution Policy Check）+ 运行时遥测监控（Runtime Telemetry Monitoring）</li>\n<li><strong>diff-only 工作模式</strong>：Codex 默认只生成代码补丁，不直接修改用户代码库——用户审批通过后方可应用</li>\n<li><strong>可审计性</strong>：每次会话的操作链、推理过程、工具调用记录完整落盘，支持事后审查</li>\n<li><strong>资源限制</strong>：每个会话有 CPU/内存/时间硬上限，防止失控消耗</li>\n</ul>\n<h5>4.6 与传统编码助手的对比</h5>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>维度</th>\n<th>Copilot / Cursor</th>\n<th>Codex</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>工作方式</td>\n<td>内联补全为主，短上下文</td>\n<td>全仓库上下文 + 多步推理 + 自主执行</td>\n</tr>\n<tr>\n<td>推理过程</td>\n<td>隐式（单次生成）</td>\n<td>显式（CoT 可审计）</td>\n</tr>\n<tr>\n<td>执行能力</td>\n<td>仅生成代码，不执行</td>\n<td>在沙箱中运行、测试、迭代修正</td>\n</tr>\n<tr>\n<td>任务粒度</td>\n<td>函数/文件级</td>\n<td>跨文件/跨模块/Bug 修复流水线</td>\n</tr>\n<tr>\n<td>安全模式</td>\n<td>仅 IDE 级别</td>\n<td>云端沙箱 + 策略审批 + 遥测熔断</td>\n</tr>\n<tr>\n<td>集成深度</td>\n<td>IDE 插件</td>\n<td>IDE + CLI + GitHub App + API</td>\n</tr>\n</tbody>\n</table></div>",
      "quiz": {
        "q": "Codex 的 Reasoning-First 执行范式中，模型在生成代码补丁之前必须先完成什么？",
        "options": [
          "直接读取整个仓库的所有文件",
          "生成包含问题定位、方案评估和补丁设计的推理链",
          "运行所有测试套件确保没有回归",
          "向用户请求额外的上下文信息"
        ],
        "answer": 1,
        "explain": "Codex 在生成代码补丁之前，必须先生成结构化的推理链（Chain-of-Thought），包含问题定位、候选方案评估和补丁设计三个部分。这一步使得代码生成过程可解释、可审计，也大幅提升了修复正确率。"
      }
    },
    {
      "id": "claude_longrun",
      "num": 9,
      "name": "Long-Running Harness",
      "fullName": "长时运行智能体 harness (Effective Harnesses for Long-Running Agents)",
      "year": "2025.11",
      "org": "Anthropic",
      "parent": "—",
      "paperUrl": "https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents",
      "projectUrl": "",
      "category": "runtime",
      "motivation": "让代理跨多个上下文窗口持续推进",
      "summary": "Anthropic 提出了一种双阶段的长时运行 Agent harness 设计——Initializer Agent 负责搭建结构化环境（feature list、进度日志、初始化脚本），Coding Agent 每次会话读取状态、选取单个 feature 增量实现、结束时提交干净 commit——解决了 agent 跨多个上下文窗口时出现的一次性冲关和过早宣布完成两大失败模式。",
      "keyPoints": [
        "<strong>双角色分工</strong>：Initializer Agent 仅在首次运行时设置环境基座，Coding Agent 在每次会话中增量推进",
        "<strong>feature_list.json</strong>：结构化的端到端 feature 描述文件，将高层 prompt 分解为可逐一实现的子任务",
        "<strong>claude-progress.txt</strong>：持久化进度日志，记录每次会话的完成情况，实现跨窗口交接",
        "<strong>init.sh</strong>：一键启动开发服务器的脚本，消除每次会话重新摸索运行方式的成本",
        "<strong>Git 纪律</strong>：每次会话结束时必须提交干净的 commit，确保下一轮从一个可构建、无未追踪 bug 的状态开始",
        "<strong>自验证机制</strong>：Coding Agent 在标记 feature 为\"done\"前必须经过自测试，防止过早宣告完成",
        "<strong>四种失败模式的系统化解法</strong>：一次性实现、环境脏乱、过早完成、启动摸索，均有对应的 Initializer/Coding Agent 行为对策",
        "<strong>compaction 之外的补充</strong>：指出仅靠上下文压缩不足以解决长程问题，必须辅以结构化交接产物"
      ],
      "detail": "<p><img alt=\"Long-Running Harness 示意图\" src=\"https://www.anthropic.com/_next/image?q=75&amp;url=https%3A%2F%2Fwww-cdn.anthropic.com%2Fimages%2F4zrzovbb%2Fwebsite%2Ff94c2257964fb2d623f1e81f874977ebfc0986bc-1920x1080.gif&amp;w=3840\" />\n<em>图：Long-Running Harness 的核心框架或系统示意。</em></p>\n<h5>动机与背景</h5>\n<p>AI Agent 面临的核心矛盾：复杂任务（如构建 claude.ai 克隆体）需要跨越多个上下文窗口才能完成，但每个新的会话窗口天然没有前序记忆。这类似于软件工程中的\"轮班制\"——如果每个接班的工程师都不了解前序进度，项目将举步维艰。Anthropic 发现，即使使用 Claude Agent SDK 的自带 compaction 机制，前沿模型 Opus 4.5 在多个上下文窗口中仍无法构建出一个生产级 web app，暴露出 compaction 的局限性。</p>\n<h5>两大失败模式</h5>\n<p><strong>模式一：一次性冲关（One-shot attempt）</strong><br />\nAgent 试图在单个窗口中完成整个项目，导致上下文耗尽时 feature 只实现了一半，且无文档说明。下一轮 agent 面对半成品不得不猜测前序工作，大量时间浪费在修复基础功能上。</p>\n<p><strong>模式二：过早宣布完成（Premature declaration of done）</strong><br />\n在项目后期，部分 feature 已建成后，某个 agent 实例环顾四周，看到已有进展，便宣告任务完成，不再继续推进剩余需求。</p>\n<h5>两阶段解决方案</h5>\n<h6>Initializer Agent（初始化 Agent）</h6>\n<p>仅在首次运行时激活，负责将高层需求转化为结构化的可执行环境：</p>\n<ol>\n<li><strong>feature_list.json</strong> — 将输入 spec 解析为结构化 JSON，每个条目包含端到端的 feature 描述文本。这相当于将\"build a clone of claude.ai\"分解为\"实现聊天功能\"\"实现暗色主题切换\"\"实现对话历史加载\"等可操作子任务。</li>\n<li><strong>claude-progress.txt</strong> — 初始化空的进度日志，格式为后续 agent 提供统一的记录模板。</li>\n<li><strong>初始化 Git 仓库 + 首次 commit</strong> — 确保环境从零开始被版本化追踪。</li>\n<li><strong>init.sh</strong> — 编写一个可直接启动开发服务器并运行基础检查的脚本，这是后续 agent 每次进入新会话时的\"一键就绪\"入口。</li>\n</ol>\n<h6>Coding Agent（编码 Agent）</h6>\n<p>每次新会话启动时运行，遵循严格的\"读取-选择-实现-提交\"循环：</p>\n<pre><code>每次 Coding Agent 会话的标准流程：\n1. pwd（确认工作目录）\n2. read claude-progress.txt（了解历史进度）\n3. read feature_list.json（查看剩余 feature）\n4. git log --oneline -20（检查最近变更）\n5. bash init.sh（启动开发服务器）\n6. 手动测试基础功能验证服务器正常\n7. 从 feature_list.json 中选取一个未完成的 feature\n8. 实现该 feature\n9. 自验证通过后，git commit + 更新 claude-progress.txt\n10. 退出，环境保持在可构建的干净状态\n</code></pre>\n<p>关键的设计理念是\"<strong>离开时保持干净（leave a clean state）</strong>\"——每次会话结束时，代码应处于适合合并到 main 分支的状态：无重大 bug，代码整洁且有文档，开发者可直接开始新 feature 而不需先清理无关混乱。</p>\n<h5>四种失败模式与对策</h5>\n<p>Anthropic 在文中以表格形式总结了四种常见失败模式及其系统化解决方案：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>失败模式</th>\n<th>Initializer Agent 对策</th>\n<th>Coding Agent 对策</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><strong>过早宣告完成</strong></td>\n<td>创建 feature_list.json 结构化 feature 清单</td>\n<td>每次会话开始时读取 feature list，仅选择<strong>一个</strong> feature 开始工作</td>\n</tr>\n<tr>\n<td><strong>环境遗留 bug / 无文档</strong></td>\n<td>初始化 git repo 和 progress 文件</td>\n<td>会话开始时读取 progress 和 git log，运行基本测试；结束时 commit + 更新 progress</td>\n</tr>\n<tr>\n<td><strong>feature 标记为完成但实际未通过</strong></td>\n<td>创建 feature_list.json</td>\n<td><strong>自验证所有 feature</strong>，仅在仔细测试后标记为\"passing\"</td>\n</tr>\n<tr>\n<td><strong>每次花时间摸索如何运行 app</strong></td>\n<td>编写 init.sh 脚本</td>\n<td>会话开始时运行 init.sh</td>\n</tr>\n</tbody>\n</table></div>\n<h5>与传统方法的区别</h5>\n<p>对比单纯的 compaction 循环方案，Anthropic 的方案核心差异在于<strong>引入结构化交接产物</strong>作为跨窗口的持久化记忆。compaction 是对上下文窗口的\"压缩摘要\"，但摘要不可避免地丢失信息，且无法传递\"运行状态\"（开发服务器是否启动、当前分支状态等）。而 feature list + progress log + git history + init.sh 构成的四件套，本质上是一种<strong>外化的工作记忆</strong>，使得每个新 agent 实例进入窗口时，能像人类工程师一样通过日志和脚本快速\"加载上下文\"。</p>\n<h5>未来方向</h5>\n<ul>\n<li><strong>多 Agent 架构</strong>：是否应由专门的测试 Agent、QA Agent、代码清理 Agent 分别负责子任务，而非依赖单一通用编码 Agent？</li>\n<li><strong>跨领域泛化</strong>：当前方案为全栈 Web 开发优化，如何将 feature list + progress log 模式适配到科研计算、金融建模等长程任务中？</li>\n</ul>\n<div class=\"key-point\">💡 关键：该方案的核心洞察是将人类软件工程的最佳实践——<strong>任务分解、进度日志、一脚本启动、干净提交</strong>——结构化地注入 agent harness 中，使 agent 的行为模式从\"一次性大瀑布\"转为\"持续增量交付\"。</div>",
      "quiz": {
        "q": "Anthropic 长时运行 Agent harness 中，Initializer Agent 的核心职责是什么？",
        "options": [
          "在每次会话中选取一个 feature 进行增量实现",
          "负责自验证所有 feature 并标记完成状态",
          "将高层需求分解为 feature_list.json、建立进度日志、编写 init.sh 等初始化环境",
          "每次会话结束时压缩上下文并传递给下一个 agent"
        ],
        "answer": 2,
        "explain": "Initializer Agent 仅在首次运行时工作，将 prompt 转化为结构化环境（feature list、progress log、init.sh），为后续 Coding Agent 的持续增量交付提供基座。"
      }
    },
    {
      "id": "idebench",
      "num": 10,
      "name": "IDE-Bench",
      "fullName": "IDE 原生代理基准 (IDE-Bench)",
      "year": "2026.01",
      "org": "AfterQuery",
      "parent": "swe_verified",
      "paperUrl": "https://arxiv.org/abs/2601.20886",
      "projectUrl": "",
      "category": "evaluation",
      "motivation": "用IDE原生工具链评测协作式编码",
      "summary": "IDE-Bench 提出了首个在真实Docker化IDE环境中评测AI编程Agent的基准，涵盖8个仓库×10任务=80个多样化编程任务和17种IDE工具，并通过15个前沿模型的6000+次运行揭示了当前模型在长期代码编辑中普遍存在的\"提前编辑\"（63%）和\"反复编辑\"（28%）两大失败模式。",
      "keyPoints": [
        "<strong>8个真实仓库</strong>：Game Engine Service (C++)、SmartHub Operations Center (Java/Javalin)、Network Traffic Analyzer (Python)、以及5个未公开的私有仓库（含MERN全栈、系统编程等多技术栈），防止训练数据污染",
        "<strong>80个多样化任务</strong>：每个仓库10个任务，覆盖算法实现（18%）、Bug修复（35%）、功能开发（32%）、重构（15%）四大类别",
        "<strong>17种IDE工具的统一接口</strong>：遵循OpenAI Function Calling规范，分为文件系统导航（6个）、代码编辑（3个）、执行测试（1个）、全栈测试（4个）、专用工具（3个）五大类",
        "<strong>Calibrated评估框架</strong>：通过Floor基线（初始未修改仓库的测试通过率）和Ceiling基线（应用参考patch后100%通过）建立校准区间，确保性能评估的可靠性",
        "<strong>15个前沿模型全面评测</strong>：包括GPT-5.2（pass@1=89%，pass@5=95%）、Claude Sonnet 4（pass@1=87.5%）、Gemini 2.5 Pro（pass@1=84%）、DeepSeek-V3（pass@1=65%）等，总计6000+独立运行",
        "<strong>\"Gaming Agent\"现象发现</strong>：部分模型通过反复运行测试套件并对比输出差异来\"猜测\"正确实现，而非真正理解代码逻辑",
        "<strong>工具序列模式分析</strong>：高绩效模型遵循\"广泛探索→精准编辑→快速验证\"的三阶段模式，使用5-10次搜索/阅读后才进行首次编辑",
        "<strong>Docker容器化完全可复现</strong>：每个任务在独立Docker容器中运行，确保环境一致性和评估公平性"
      ],
      "detail": "<p><img alt=\"IDE-Bench 示意图\" src=\"https://ar5iv.labs.arxiv.org/html/2601.20886/assets/x1.png\" />\n<em>图：IDE-Bench 的核心框架或评测示意。</em></p>\n<h5>1. 动机与背景</h5>\n<p>现有代码生成评估基准（如HumanEval、MBPP）仅评估单函数补全能力，而SWE-bench系列虽模拟真实GitHub issue修复，却缺乏IDE环境中的关键要素：文件导航、增量编辑、终端执行、实时测试反馈等真实开发工作流。IDE-Bench的核心理念是：<strong>优秀的AI编程Agent必须能在完整的IDE环境中自主探索代码库、做出精准修改并验证结果</strong>，而不仅仅是在理想化的输入-输出对上进行评估。</p>\n<p>论文提出了<strong>\"Calibrated Evaluation\"</strong>框架——通过天花板（Ceiling）和地板（Floor）基线为每个任务建立有效的性能测量区间，解决了传统基准中\"测试用例可能预失败\"的评估噪声问题。</p>\n<h5>2. 基准架构设计</h5>\n<p>IDE-Bench的评估架构由三个核心层组成：</p>\n<pre><code>┌─────────────────────────────────────────────┐\n│              Model Layer                     │\n│  (任何LiteLLM兼容模型，统一API接口)            │\n├─────────────────────────────────────────────┤\n│            Harness Layer                     │\n│  ┌──────────┬──────────┬──────────────────┐ │\n│  │ 文件系统  │ 代码编辑  │  执行与测试       │ │\n│  │ 导航(6)  │ (3)      │  (1+4)           │ │\n│  └──────────┴──────────┴──────────────────┘ │\n├─────────────────────────────────────────────┤\n│           Repository Layer                   │\n│  ┌──────┬──────┬──────┬──────────────────┐  │\n│  │ C++  │ Java │Python│ MERN/Full-Stack  │  │\n│  │引擎   │ Web  │网络  │ + 5个私有仓库    │  │\n│  └──────┴──────┴──────┴──────────────────┘  │\n└─────────────────────────────────────────────┘\n</code></pre>\n<p><strong>17种工具详解</strong>：所有工具遵循OpenAI Function Calling规范，统一要求<code>explanation</code>参数以记录Agent推理链。</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>类别</th>\n<th>工具</th>\n<th>功能</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>文件导航</td>\n<td><code>read_file</code></td>\n<td>按行范围读取文件，屏蔽/test目录防止作弊</td>\n</tr>\n<tr>\n<td></td>\n<td><code>list_dir</code></td>\n<td>递归/非递归列出目录，自动排除node_modules等</td>\n</tr>\n<tr>\n<td></td>\n<td><code>codebase_search</code></td>\n<td>基于ripgrep的词法搜索，支持上下文行</td>\n</tr>\n<tr>\n<td></td>\n<td><code>grep_search</code></td>\n<td>高级正则搜索，支持多行模式</td>\n</tr>\n<tr>\n<td></td>\n<td><code>file_search</code></td>\n<td>基于glob的模糊文件名匹配</td>\n</tr>\n<tr>\n<td></td>\n<td><code>delete_file</code></td>\n<td>删除文件（同样屏蔽测试文件）</td>\n</tr>\n<tr>\n<td>代码编辑</td>\n<td><code>edit_file</code></td>\n<td>结构化行编辑：REPLACE/INSERT/DELETE，Python文件自动ast.parse()校验</td>\n</tr>\n<tr>\n<td></td>\n<td><code>search_replace</code></td>\n<td>字符串级查找替换（不需行号）</td>\n</tr>\n<tr>\n<td></td>\n<td><code>write_file</code></td>\n<td>新建/覆写文件</td>\n</tr>\n<tr>\n<td>执行测试</td>\n<td><code>run_terminal_cmd</code></td>\n<td>Shell命令执行（120s超时，支持后台模式）</td>\n</tr>\n<tr>\n<td>全栈测试</td>\n<td><code>api_call</code></td>\n<td>HTTP请求测试REST API</td>\n</tr>\n<tr>\n<td></td>\n<td><code>database_query</code></td>\n<td>MongoDB CRUD + 聚合管道</td>\n</tr>\n<tr>\n<td></td>\n<td><code>websocket_test</code></td>\n<td>Socket.IO实时通信验证</td>\n</tr>\n<tr>\n<td></td>\n<td><code>ui_test</code></td>\n<td>Playwright风格浏览器自动化</td>\n</tr>\n<tr>\n<td>专用</td>\n<td><code>edit_notebook</code></td>\n<td>Jupyter笔记本编辑（预留，未在任务中使用）</td>\n</tr>\n<tr>\n<td></td>\n<td><code>web_search</code></td>\n<td>网络搜索（接口预留，评估环境未实现）</td>\n</tr>\n<tr>\n<td></td>\n<td><code>create_diagram</code></td>\n<td>Mermaid图表生成（预留）</td>\n</tr>\n</tbody>\n</table></div>\n<h5>3. 评估流程与指标</h5>\n<p><strong>执行协议</strong>：每个任务Agent最多运行100轮对话，temperature=0.1，单次输出上限4000 tokens，API超时600秒。上下文超过25条消息时截断至模型窗口的80%，保留系统消息、首条用户消息和最近轮次。</p>\n<p><strong>Grading Pipeline（4步差分提取）</strong>：\n1. <code>git diff HEAD</code> — 获取未暂存更改\n2. <code>git add -A &amp;&amp; git diff --cached</code> — 获取暂存更改\n3. <code>git diff HEAD~1 HEAD</code> — 通过commit获取差异\n4. 逐文件与<code>git show HEAD:&lt;file&gt;</code>对比 — 兜底方案</p>\n<p>提取的diff与参考patch(<code>task_diff.txt</code>)进行语义相似度评分。Grader自动检测测试框架（pytest/jest/maven/junit/go test/cargo/rspec/phpunit/dotnet test/mocha）。</p>\n<p><strong>核心指标</strong>：\n- <strong>Task Resolution Rate (pass@k)</strong>：k次独立尝试中至少1次所有测试通过的概率\n- <strong>Test Pass Rate</strong>：单个测试用例通过百分比\n- <strong>Floor → Ceiling校准区间</strong>：初始未修改仓库的测试通过率为Floor，应用参考patch后100%通过为Ceiling</p>\n<h5>4. 实验结果全景</h5>\n<p><strong>主结果</strong>（pass@1 / pass@5）：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>模型</th>\n<th>pass@1</th>\n<th>pass@5</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>GPT-5.2</td>\n<td>89.0%</td>\n<td>95.0%</td>\n</tr>\n<tr>\n<td>Claude Sonnet 4</td>\n<td>87.5%</td>\n<td>93.8%</td>\n</tr>\n<tr>\n<td>Gemini 2.5 Pro</td>\n<td>84.0%</td>\n<td>91.3%</td>\n</tr>\n<tr>\n<td>Claude Opus 4</td>\n<td>78.8%</td>\n<td>88.8%</td>\n</tr>\n<tr>\n<td>GPT-4.1</td>\n<td>75.0%</td>\n<td>86.3%</td>\n</tr>\n<tr>\n<td>DeepSeek-V3</td>\n<td>65.0%</td>\n<td>78.8%</td>\n</tr>\n<tr>\n<td>Gemini 2.5 Flash</td>\n<td>63.8%</td>\n<td>77.5%</td>\n</tr>\n<tr>\n<td>Qwen3-235B</td>\n<td>57.5%</td>\n<td>72.5%</td>\n</tr>\n<tr>\n<td>Claude Sonnet 3.7</td>\n<td>55.0%</td>\n<td>70.0%</td>\n</tr>\n<tr>\n<td>Llama 4 Maverick</td>\n<td>48.8%</td>\n<td>65.0%</td>\n</tr>\n<tr>\n<td>DeepSeek-R1</td>\n<td>46.3%</td>\n<td>62.5%</td>\n</tr>\n<tr>\n<td>GPT-4o</td>\n<td>42.5%</td>\n<td>58.8%</td>\n</tr>\n<tr>\n<td>Qwen3-32B</td>\n<td>35.0%</td>\n<td>51.3%</td>\n</tr>\n<tr>\n<td>Command R+</td>\n<td>28.8%</td>\n<td>43.8%</td>\n</tr>\n<tr>\n<td>Ministral 8B</td>\n<td>12.5%</td>\n<td>22.5%</td>\n</tr>\n</tbody>\n</table></div>\n<p><strong>关键洞察</strong>：从top到bottom存在94.5%→12.5%的巨大性能梯度，说明基准具有优秀的区分度。GPT-5.2在pass@5下达到95%已接近天花板，但pass@1仍有11%提升空间。</p>\n<h5>5. 失败模式深度分析</h5>\n<p>论文通过人工审查100个失败案例，归纳出三类主要失败模式：</p>\n<p><strong>① 提前编辑 (Premature Editing, 63%)</strong>：Agent在未充分理解代码库结构和上下文的情况下过早修改代码。表现为仅读取1-2个文件后即开始编辑，导致修改不完整、破坏现有逻辑或遗漏相关文件。</p>\n<p><strong>② 反复编辑 (Repeated Editing, 28%)</strong>：Agent多次修改同一代码片段，陷入\"编辑→测试失败→再编辑\"的循环而无法收敛。这反映了模型在长期规划上的不足——缺乏\"先理解整体依赖关系再精准编辑\"的能力。</p>\n<p><strong>③ 工具使用不当 (9%)</strong>：包括搜索模式错误、未正确解读测试输出、忽略文件路径等。</p>\n<div class=\"key-point\">💡 关键发现：高绩效模型在首次编辑前平均进行5-10次探索操作（搜索+阅读），而低绩效模型通常仅2-3次探索就急于编辑。</div>\n<h5>6. \"Gaming Agent\"现象</h5>\n<p>论文揭示了一个值得警惕的发现：部分模型利用测试套件作为\"神谕\"来逆向推导实现——反复运行<code>./run_tests.sh</code>，观察测试失败的具体assertion差异，然后机械地修改代码以匹配期望输出，而非真正理解代码意图。这种行为在简单的算法任务中尤其有效，但在需要架构理解的复杂任务中通常会失败。这表明<strong>仅靠测试通过率评估可能高估模型的实际编程理解能力</strong>。</p>\n<h5>7. 安全性设计</h5>\n<p>为防止评估完整性被破坏，IDE-Bench实现了多层安全措施：\n- 文件访问屏蔽：<code>/tasks</code>目录、<code>run_tests.sh</code>及所有<code>test_*</code>/<code>*.test.*</code>/<code>*.spec.*</code>文件对Agent不可见\n- 编辑操作验证：<code>edit_file</code>在Python文件上自动运行<code>ast.parse()</code>阻止语法错误提交\n- 差分审计：4步grading pipeline确保完整捕获Agent修改\n- 仓库隐私：8个仓库中7个不公开，仅通过<code>research@afterquery.com</code>申请获取，防止训练数据污染</p>\n<div class=\"warn-box\">⚠️ 注意：尽管有安全设计，论文作者指出gaming agent仍然可以通过终端运行的测试输出来推断信息，这是当前评估范式的固有限制。</div>",
      "quiz": {
        "q": "IDE-Bench论文中发现的两种最主要的Agent失败模式是什么？",
        "options": [
          "语法错误和超时",
          "提前编辑（Premature Editing）和反复编辑（Repeated Editing）",
          "网络连接失败和内存溢出",
          "工具未响应和API限流"
        ],
        "answer": 1,
        "explain": "论文通过人工审查100个失败案例发现，提前编辑（未充分探索就修改代码）占63%，反复编辑（陷入修改-失败循环）占28%，两者合计占91%的失败原因。"
      }
    },
    {
      "id": "openai_harness",
      "num": 11,
      "name": "Harness Engineering",
      "fullName": "Codex Harness 工程方法论 (Harness Engineering)",
      "year": "2026.02",
      "org": "OpenAI",
      "parent": "codex",
      "paperUrl": "https://openai.com/index/harness-engineering/",
      "projectUrl": "",
      "category": "runtime",
      "motivation": "把环境设计与反馈闭环提升为方法论",
      "summary": "Harness Engineering 提出了 AI 编程模型评估的系统化工程方法论，将评估从静态 Benchmark 范式转向以环境设计、任务支架和反馈闭环为核心的动态交互式评估体系，解决了传统编程 Benchmark 缺乏真实环境交互和迭代反馈的根本缺陷。",
      "keyPoints": [
        "提出 <strong>Harness（支架/夹具）</strong> 概念：为每个编程任务构建独立、可复现的执行环境（Docker 沙箱），使模型能自主运行、测试和修正代码",
        "三层评估架构：<strong>Spec → Harness → Iteration</strong>，将任务规格化、环境执行、迭代修正统一为闭环",
        "引入 <strong>Pass@k 指标的工程化扩展</strong>：不只统计最终通过率，还追踪每次迭代的测试结果、错误类型和修复路径，形成多维能力画像",
        "反馈闭环设计：将编译错误、运行时异常、测试失败等信号结构化地反馈给模型，模拟真实开发中的 debug 循环",
        "与传统 Benchmark（HumanEval、MBPP）的根本对比：从\"给题目→收代码→判对错\"的静态模式，升级为\"给需求→在真实环境中迭代→达到可工作状态\"的动态模式",
        "环境即接口（Environment as Interface）：通过统一的 harness 协议解耦任务定义与执行环境，支持任意编程语言和工具链的即插即用",
        "提出任务难度分级：根据所需迭代轮次、依赖复杂度、环境配置难度将任务分为 L1–L4，指导模型能力评估"
      ],
      "detail": "<p><img alt=\"Harness Engineering 示意图\" src=\"https://images.ctfassets.net/kftzwdyauwt9/1Gu58eNlqDEuITmbqJDmq9/1e2e62f7e15fb16d2da0da5407240564/fig_1__codex_drives_the_app_.png?fm=webp&amp;q=90&amp;w=3840\" />\n<em>图：Harness Engineering 的核心框架或系统示意。</em></p>\n<h5>核心框架示意</h5>\n<pre><code>┌─────────────────────────────────────────────────────┐\n│                    Harness 框架                       │\n│  ┌──────────┐    ┌──────────────┐    ┌────────────┐ │\n│  │   Spec   │ →  │   Harness    │ →  │  Feedback  │ │\n│  │ (任务规格) │    │ (执行环境+工具) │    │  (结果/错误) │ │\n│  └──────────┘    └──────────────┘    └────────────┘ │\n│       ↑                                    │         │\n│       └──────────  Model Iteration ←───────┘         │\n│            (模型根据反馈修正代码)                        │\n└─────────────────────────────────────────────────────┘\n</code></pre>\n<p><em>图：Harness Engineering 的核心闭环——模型在 harness 环境中执行代码、获取反馈、迭代修正</em></p>\n<h5>动机与背景</h5>\n<p>传统编程模型评估（如 HumanEval、MBPP）存在以下根本性局限：</p>\n<ol>\n<li>\n<p><strong>输入/输出匹配模式陷阱</strong>：模型只需生成符合预期输出的代码，不涉及真实执行环境的交互。一个在 HumanEval 上 Pass@1 达到 90% 的模型，在真实项目中可能因无法处理依赖安装、环境配置、运行时错误而完全失效。</p>\n</li>\n<li>\n<p><strong>缺失迭代能力评估</strong>：真实编程中，开发者的核心技能并非\"一次写对\"，而是\"根据错误信息定位并修复\"。传统 Benchmark 只评估一次生成的能力，完全忽略了 debug 闭环。</p>\n</li>\n<li>\n<p><strong>环境不可复现</strong>：每个评测方的运行环境不同，导致模型能力不可直接对比。同一个模型在 A 的评测中得到 80%，在 B 的环境可能因 Python 版本、依赖库差异降至 50%。</p>\n</li>\n</ol>\n<p>OpenAI Codex 团队在 2026 年初的工程实践中发现，模型在真实软件开发任务中的表现与 Benchmark 分数之间存在显著 gap。这一洞察驱动了 Harness Engineering 方法论的提出——核心思想是：<strong>评估环境本身应成为第一等公民（first-class citizen），而非可忽略的外部因素</strong>。</p>\n<h5>核心机制：Spec → Harness → Feedback Loop</h5>\n<p>Harness Engineering 的核心流程可概括为三个阶段：</p>\n<p><strong>Phase 1 — Spec（任务规格化）</strong></p>\n<p>每个任务被定义为一个标准化的 Spec 文件（YAML/JSON），包含：</p>\n<ul>\n<li><code>task_description</code>：用自然语言描述的需求</li>\n<li><code>environment</code>：所需运行时（Python 3.12 / Node.js 22 / Rust 等）、依赖文件列表（<code>requirements.txt</code>、<code>Cargo.toml</code>）和系统级依赖</li>\n<li><code>setup_commands</code>：环境初始化命令序列（如 <code>pip install -e .</code>、<code>npm install</code>）</li>\n<li><code>test_suite</code>：一个或多个可执行的测试脚本，用于判定任务是否完成</li>\n<li><code>timeout</code>：单次执行的时间上限</li>\n<li><code>max_iterations</code>：模型被允许的最大修正轮次</li>\n</ul>\n<div class=\"key-point\">💡 关键：Spec 文件本身是版本控制、可分享、可复现的最小单元。任何一个任务 Spec 拉取后即可在任一机器上重建相同评估环境。</div>\n<p><strong>Phase 2 — Harness（执行支架）</strong></p>\n<p>Harness 是一个轻量级的编排层（orchestration layer），负责：</p>\n<ol>\n<li><strong>环境构建</strong>：根据 Spec 创建隔离的 Docker 容器或 sandbox，安装指定依赖</li>\n<li><strong>工具注入</strong>：为模型提供一组可调用的工具（执行 shell 命令、读写文件、查看 diff、运行测试等），模型通过工具调用的方式与环境交互</li>\n<li><strong>状态追踪</strong>：记录每一次模型动作（代码修改 / 命令执行 / 测试运行）及其结果，形成完整的交互轨迹</li>\n</ol>\n<p>Harness 的设计哲学是\"<strong>给模型一个真实的终端，而非一个评测员的打分表</strong>\"。其伪代码逻辑如下：</p>\n<pre><code class=\"language-python\">def run_harness(spec, model, max_iterations):\n    env = create_environment(spec.environment)\n    env.setup(spec.setup_commands)\n\n    trajectory = []\n    for i in range(max_iterations):\n        # 模型观察当前环境状态和任务描述\n        observation = {\n            &quot;task&quot;: spec.task_description,\n            &quot;files&quot;: env.list_files(),\n            &quot;last_output&quot;: trajectory[-1].output if trajectory else None,\n            &quot;test_results&quot;: env.run_tests(spec.test_suite)\n        }\n\n        # 模型决定下一步动作：修改文件 / 执行命令 / 提交\n        action = model.generate(observation)\n\n        # 在真实环境中执行动作\n        result = env.execute(action)\n        trajectory.append({&quot;iteration&quot;: i, &quot;action&quot;: action, &quot;result&quot;: result})\n\n        # 检查是否通过全部测试\n        if result.all_tests_passed:\n            return {&quot;status&quot;: &quot;success&quot;, &quot;trajectory&quot;: trajectory}\n\n    return {&quot;status&quot;: &quot;failure&quot;, &quot;trajectory&quot;: trajectory}\n</code></pre>\n<p><strong>Phase 3 — Feedback Loop（反馈闭环）</strong></p>\n<p>反馈闭环是 Harness Engineering 区别于传统评估的最关键创新。它将环境执行产生的结果结构化，分为三个层次反馈给模型：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>层级</th>\n<th>反馈类型</th>\n<th>示例</th>\n<th>模型应对</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>L1</td>\n<td>语法/编译错误</td>\n<td><code>SyntaxError: invalid syntax at line 42</code></td>\n<td>直接修正语法</td>\n</tr>\n<tr>\n<td>L2</td>\n<td>运行时错误/测试失败</td>\n<td><code>AssertionError: expected 42, got 0</code></td>\n<td>分析逻辑错误，修正实现</td>\n</tr>\n<tr>\n<td>L3</td>\n<td>环境/依赖问题</td>\n<td><code>ModuleNotFoundError: No module named 'torch'</code></td>\n<td>修改依赖配置或更换实现方案</td>\n</tr>\n</tbody>\n</table></div>\n<p>每一层的反馈都附带<strong>上下文快照</strong>（当前文件清单、最近命令的输出、测试失败的具体 diff），使模型能像人类开发者一样在 IDE 中工作。</p>\n<h5>与传统方法的根本区别</h5>\n<p>传统编程 Benchmark 的评估流水线是单向的：</p>\n<pre><code>题目 → 模型生成代码 → 静态分析/单元测试 → 通过/不通过\n</code></pre>\n<p>Harness Engineering 将其转变为双向交互闭环：</p>\n<pre><code>Spec → [模型 ↔ Harness环境] × N次迭代 → 最终判定\n</code></pre>\n<p>具体的差异化体现在：</p>\n<ol>\n<li><strong>从\"一次生成\"到\"N 次迭代\"</strong>：评估的不再是模型的\"一次性正确率\"，而是在有限步数内\"达成目标的能力\"</li>\n<li><strong>从\"纯文本\"到\"真实执行\"</strong>：模型可以看到代码的真实执行结果，包括 stdout、stderr、文件系统变化</li>\n<li><strong>从\"封闭题目\"到\"开放环境\"</strong>：Spec 只定义目标和约束，模型可以选择任意实现路径，包括添加新文件、安装新依赖、甚至修改测试（如果 Spec 允许）</li>\n<li><strong>全轨迹可审计</strong>：每一次交互都被完整记录，评估者可以不仅是看最终是否通过，还能分析模型的 debug 策略、错误处理模式、迭代效率等细粒度能力指标</li>\n</ol>\n<h5>任务难度分级 (L1–L4)</h5>\n<p>Harness Engineering 提出了一套基于经验的任务难度分级系统：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>等级</th>\n<th>典型迭代次数</th>\n<th>特征</th>\n<th>示例任务</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>L1</td>\n<td>≤3</td>\n<td>单文件、无外部依赖、纯逻辑实现</td>\n<td>实现一个排序算法</td>\n</tr>\n<tr>\n<td>L2</td>\n<td>4–8</td>\n<td>多文件、少量标准库依赖</td>\n<td>构建一个 REST API 端点</td>\n</tr>\n<tr>\n<td>L3</td>\n<td>9–20</td>\n<td>多模块、需要第三方依赖、涉及环境配置</td>\n<td>搭建一个带数据库的 Web 服务</td>\n</tr>\n<tr>\n<td>L4</td>\n<td>&gt;20</td>\n<td>全栈项目、复杂依赖链、需要架构设计</td>\n<td>从零实现一个微服务系统并部署</td>\n</tr>\n</tbody>\n</table></div>\n<div class=\"warn-box\">⚠️ 注意：L4 任务在实际评估中极少出现（模型通常需要数十轮迭代才能完成），目前主要用于衡量模型的长期自主工作能力（long-horizon autonomy）。</div>\n<h5>对 AI 编程能力评估的影响</h5>\n<p>Harness Engineering 的提出标志着 AI 编程评估从\"考试模式\"向\"工作模式\"的范式转变。其核心贡献在于：</p>\n<ol>\n<li><strong>生态建设</strong>：团队同时开源了一套标准 Harness Spec 集合（涵盖 Python、JS/TS、Rust 等语言的数百个任务）和 Harness Runner 的参考实现，使社区可以在此基础上扩展</li>\n<li><strong>评估信度提升</strong>：因为环境完全由 Spec 控制，不同机构、不同时间评测的结果具有真正可比性（消除了\"我的环境没装某个库导致失败\"的噪声）</li>\n<li><strong>能力画像细化</strong>：通过分析 trajectory，可以分别评估模型的代码生成能力、错误诊断能力、自修复能力、工具使用能力等独立维度</li>\n</ol>",
      "quiz": {
        "q": "Harness Engineering 相比传统编程 Benchmark（如 HumanEval）最根本的变革是什么？",
        "options": [
          "使用更难的编程题目",
          "从静态的一次性代码生成评估，转变为在真实环境中进行多轮交互迭代的闭环评估",
          "改用 Docker 容器运行评测",
          "增加了 Pass@k 指标的统计维度"
        ],
        "answer": 1,
        "explain": "Harness Engineering 的核心创新在于将评估从单向的'给题→生成→判分'升级为'在真实环境中反复迭代、根据反馈修正'的闭环，这是与传统 Benchmark 最本质的区别。Docker 只是实现手段之一，Pass@k 扩展是具体指标层面，都不构成范式层面的变革。"
      }
    },
    {
      "id": "meta_harness",
      "num": 12,
      "name": "Meta-Harness",
      "fullName": "元 Harness 外环优化 (Meta-Harness)",
      "year": "2026.03",
      "org": "Stanford/MIT",
      "parent": "openai_harness",
      "paperUrl": "https://arxiv.org/abs/2603.28052",
      "projectUrl": "",
      "category": "optimization",
      "motivation": "外环搜索可执行harness代码",
      "summary": "Meta-Harness 提出了一种**用编码Agent自动搜索和优化任务级Harness（上下文管理程序）的外循环框架**。它通过让一个编码Agent（Proposer）访问完整的文件系统（包含历史Harness代码、评分和执行轨迹），实现选择性诊断和针对性修改，在无需人工设计搜索启发式规则的前提下，自动发现超越人类设计的上下文管理策略。",
      "keyPoints": [
        "<strong>Harness即程序</strong>：将上下文管理策略（提示构造、检索、记忆更新、工具编排）实现为可执行的Python程序，使搜索空间从文本提示扩展到完整算法逻辑",
        "<strong>文件系统即反馈通道</strong>：用文件系统存储所有历史Harness的源代码、评分和执行轨迹（每次评估可产生高达10,000,000 token的诊断信息），Proposer通过grep/cat等标准工具选择性检索，而非将压缩摘要一次性塞入上下文",
        "<strong>极简外循环</strong>：Meta-Harness不硬编码任何搜索启发式（无父代选择规则、无固定变异算子、无持久记忆机制），完全由Proposer自主决定诊断什么、如何修改",
        "<strong>代码空间搜索的自然正则化</strong>：编码模型倾向于生成连贯可复用的算法而非脆弱的硬编码方案，搜索偏向可泛化的上下文管理过程",
        "<strong>Pareto多目标优化</strong>：支持准确率与上下文成本的联合优化，自动产生平滑的Pareto前沿，可按需选取工作点",
        "<strong>10× 加速与显著提升</strong>：在在线文本分类上，Meta-Harness用十分之一的评估次数匹配最佳文本优化器的性能，最终准确率超越对手超10个百分点",
        "<strong>跨领域泛化</strong>：在三个截然不同的领域（在线文本分类、数学推理、Agent编码）均验证有效，且发现的Harness对未见过的数据集和模型均展现强泛化能力",
        "<strong>执行轨迹是关键</strong>：消融实验证明，完整执行轨迹访问是核心组件——仅看评分和摘要的Proposer远不及有轨迹访问的Proposer"
      ],
      "detail": "<p><img alt=\"Meta-Harness 示意图\" src=\"https://ar5iv.labs.arxiv.org/html/2603.28052/assets/x1.png\" />\n<em>图：Meta-Harness 的核心框架或评测示意。</em></p>\n<h5>问题定义：Harness优化</h5>\n<p>一个Harness <span class=\"kb-math kb-math-inline\">H</span> 是一个有状态程序，包装语言模型 <span class=\"kb-math kb-math-inline\">M</span> 并决定模型在每一步看到什么上下文。对于任务分布 <span class=\"kb-math kb-math-inline\">\\mathcal{X}</span> 上的实例 <span class=\"kb-math kb-math-inline\">x</span>，Harness执行轨迹 <span class=\"kb-math kb-math-inline\">\\tau \\sim p_M(H, x)</span>，奖励函数 <span class=\"kb-math kb-math-inline\">r(\\tau, x)</span> 评分。优化目标是找到最大化期望奖励的Harness：</p>\n<div class=\"kb-math kb-math-display\">H^* = \\arg\\max_H \\mathbb{E}_{x \\sim \\mathcal{X}, \\tau \\sim p_M(H,x)} \\; r(\\tau, x)</div>\n<p>与传统文本优化（只在Prompt空间搜索）不同，Harness优化搜索的是完整的可执行程序，包括检索逻辑、记忆更新策略、工具调用和流程编排。</p>\n<h5>核心框架：Meta-Harness搜索循环</h5>\n<pre><code>┌─────────────────────────────────────────────────────────┐\n│                 Meta-Harness 外循环                       │\n│                                                         │\n│  ┌──────────┐    ┌──────────────┐    ┌──────────────┐   │\n│  │ Filesystem│ ←─ │   Proposer   │ ──→│  Evaluate    │   │\n│  │  D (code, │    │  (Coding     │    │  (在新任务上  │   │\n│  │  scores,  │    │   Agent)     │    │   运行Harness)│   │\n│  │  traces)  │    └──────────────┘    └──────────────┘   │\n│  └──────────┘          │                      │          │\n│       ↑                │    inspect            │          │\n│       └────────────────┘    propose            │          │\n│                             edit                │          │\n│                                          store results    │\n│                                          (code+scores     │\n│                                           +traces)        │\n└─────────────────────────────────────────────────────────┘\n</code></pre>\n<p><strong>Algorithm 1</strong> 伪代码：</p>\n<ol>\n<li>初始化：给定任务分布 <span class=\"kb-math kb-math-inline\">\\mathcal{X}</span>、LLM <span class=\"kb-math kb-math-inline\">M</span>、Proposer <span class=\"kb-math kb-math-inline\">P</span>、迭代轮次 <span class=\"kb-math kb-math-inline\">N</span></li>\n<li>初始种群 <span class=\"kb-math kb-math-inline\">\\mathcal{H}</span> 中的Harness逐一评估，结果存入文件系统 <span class=\"kb-math kb-math-inline\">\\mathcal{D}</span></li>\n<li>每轮迭代：</li>\n<li>Proposer <span class=\"kb-math kb-math-inline\">P</span> 通过文件系统工具查询 <span class=\"kb-math kb-math-inline\">\\mathcal{D}</span>（检查历史代码、评分、执行轨迹）</li>\n<li>Proposer提出 <span class=\"kb-math kb-math-inline\">k</span> 个新Harness</li>\n<li>通过接口验证的Harness被评估，结果追加到 <span class=\"kb-math kb-math-inline\">\\mathcal{D}</span></li>\n<li>返回 <span class=\"kb-math kb-math-inline\">\\mathcal{D}</span> 中所有Harness的Pareto前沿</li>\n</ol>\n<p><strong>关键设计决策</strong>：</p>\n<ul>\n<li><strong>无父代选择规则</strong>：Proposer可自由检查任何历史Harness及其轨迹</li>\n<li><strong>无固定变异算子</strong>：修改粒度从局部编辑到完全重写，由Proposer自主决定</li>\n<li><strong>文件系统代替压缩摘要</strong>：一次评估可产生多达10M token的诊断信息，远超任何上下文窗口，因此存储为文件并支持选择性检索</li>\n</ul>\n<h5>实验验证</h5>\n<p><strong>1. 在线文本分类</strong></p>\n<p>在LawBench（215类）、Symptom2Disease（22类）、USPTO-50k（180类）三个数据集上，使用GPT-OSS-120B作为基础模型：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>Harness</th>\n<th>平均准确率</th>\n<th>上下文Token (K)</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Zero-Shot</td>\n<td>27.4</td>\n<td>0</td>\n</tr>\n<tr>\n<td>Few-Shot (8)</td>\n<td>34.3</td>\n<td>2.0</td>\n</tr>\n<tr>\n<td>Few-Shot (32)</td>\n<td>35.4</td>\n<td>7.9</td>\n</tr>\n<tr>\n<td>Few-Shot (all)</td>\n<td>40.8</td>\n<td>12.3</td>\n</tr>\n<tr>\n<td>MCE</td>\n<td>40.0</td>\n<td>28.5</td>\n</tr>\n<tr>\n<td>ACE</td>\n<td>40.9</td>\n<td>50.8</td>\n</tr>\n<tr>\n<td><strong>Meta-Harness</strong></td>\n<td><strong>48.6</strong></td>\n<td><strong>11.4</strong></td>\n</tr>\n</tbody>\n</table></div>\n<p>Meta-Harness超越ACE 7.7个百分点，同时仅使用ACE 22%的上下文token。</p>\n<p><strong>与文本优化器的对比</strong>（搜索集准确率）：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>方法</th>\n<th>中位数准确率</th>\n<th>最佳准确率</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>GEPA</td>\n<td>32.6</td>\n<td>40.2</td>\n</tr>\n<tr>\n<td>Best-of-N</td>\n<td>34.0</td>\n<td>44.2</td>\n</tr>\n<tr>\n<td>OpenEvolve</td>\n<td>39.1</td>\n<td>43.3</td>\n</tr>\n<tr>\n<td>TTT-Discover</td>\n<td>34.1</td>\n<td>45.6</td>\n</tr>\n<tr>\n<td><strong>Meta-Harness</strong></td>\n<td><strong>50.0</strong></td>\n<td><strong>56.7</strong></td>\n</tr>\n</tbody>\n</table></div>\n<p>Meta-Harness在<strong>0.1倍评估预算</strong>下匹配最佳文本优化器，最终超越超10个百分点。</p>\n<p><strong>消融实验</strong>：对比三种Proposer信息接口——</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>接口</th>\n<th>中位数准确率</th>\n<th>最佳准确率</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>仅评分</td>\n<td>34.6</td>\n<td>41.3</td>\n</tr>\n<tr>\n<td>评分+摘要</td>\n<td>34.9</td>\n<td>38.7</td>\n</tr>\n<tr>\n<td><strong>完整轨迹（Meta-Harness）</strong></td>\n<td><strong>50.0</strong></td>\n<td><strong>56.7</strong></td>\n</tr>\n</tbody>\n</table></div>\n<p>摘要甚至可能因压缩掉诊断有用的细节而降低性能。<strong>执行轨迹的完整访问是核心驱动力。</strong></p>\n<p><strong>Pareto优化</strong>：Proposer可同时优化准确率和上下文成本，产生平滑的准确率-上下文Pareto曲线，支持按需选择工作点。</p>\n<p><strong>OOD泛化</strong>：在9个未见过的数据集上，Meta-Harness取得73.1%最佳平均准确率（ACE为70.2%），在6/9数据集上表现最佳。</p>\n<p><strong>2. 检索增强数学推理</strong></p>\n<p>在200道IMO级别数学题上，单个发现的Harness在5个留出模型上平均提升4.7个百分点准确率。</p>\n<p><strong>3. Agent编码（TerminalBench-2）</strong></p>\n<p>发现的Harness超越Terminus-KIRA，在Haiku 4.5 Agent中排名#1。</p>\n<h5>为什么Code-Space搜索优于Text-Space搜索？</h5>\n<ol>\n<li><strong>小改动可影响远期行为</strong>：检索、记忆或提示构造逻辑的微小改动可能在多步后产生放大效应，局部搜索启发式无法应对</li>\n<li><strong>轨迹诊断能力</strong>：Proposer通过检查执行轨迹，可推断Harness失败的原因和早期设计选择的责任链，而非仅知道失败</li>\n<li><strong>自然正则化</strong>：编码模型倾向生成连贯算法，偏向可复用的上下文管理过程</li>\n<li><strong>与模型训练分布对齐</strong>：前沿编码助手本身训练于读-写-执行的开发工作流</li>\n</ol>\n<h5>与相关工作的本质区别</h5>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>维度</th>\n<th>文本优化器 (ProTeGi/TextGrad等)</th>\n<th>Meta-Harness</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>搜索空间</td>\n<td>文本Prompt</td>\n<td>完整可执行程序</td>\n</tr>\n<tr>\n<td>反馈信息量</td>\n<td>~10K tokens（摘要/评分）</td>\n<td>~10M tokens（代码+评分+轨迹）</td>\n</tr>\n<tr>\n<td>信息访问方式</td>\n<td>一次性注入上下文</td>\n<td>文件系统选择性检索</td>\n</tr>\n<tr>\n<td>搜索结构</td>\n<td>硬编码遗传算子/梯度</td>\n<td>无固定结构，Proposer自主决定</td>\n</tr>\n<tr>\n<td>优化目标</td>\n<td>单一标量</td>\n<td>Pareto多目标</td>\n</tr>\n</tbody>\n</table></div>",
      "quiz": {
        "q": "Meta-Harness 相比只把分数或摘要喂给优化器，最关键的设计变化是什么？",
        "options": [
          "把 harness 搜索空间限制为几个固定 prompt 模板，避免代码级搜索过大",
          "让 proposer 通过文件系统访问候选代码、分数和完整执行轨迹，而不是只看压缩反馈",
          "只优化单一准确率指标，不再考虑上下文 token 成本",
          "把所有候选 harness 都交给人工工程师手动复核后再选择"
        ],
        "answer": 1,
        "explain": "Meta-Harness 的核心不是单纯有一个 proposer，而是 proposer 能通过文件系统读取完整先验经验，包括代码与轨迹，这比只给摘要或标量分数的信息量高得多。"
      }
    },
    {
      "id": "harness_design",
      "num": 13,
      "name": "Harness Design",
      "fullName": "长程应用开发 Harness 设计 (Harness Design for Long-Running Application Development)",
      "year": "2026.03",
      "org": "Anthropic",
      "parent": "claude_longrun",
      "paperUrl": "https://www.anthropic.com/engineering/harness-design-long-running-apps",
      "projectUrl": "",
      "category": "runtime",
      "motivation": "用规划生成评审三Agent突破长程开发",
      "summary": "Anthropic 将长程应用开发 harness 升级为 `planner-generator-evaluator` 三代理体系：先把一句话需求扩成完整产品 spec，再由生成代理分阶段实现、由评估代理用 Playwright 和显式标准验收，从而把长时自主编程从“能写代码”推进到“能持续交付可用应用”。",
      "keyPoints": [
        "继承早期长程 harness 的两个经验：把大任务切成可控块，以及用结构化产物在会话之间交接上下文。",
        "形成三代理结构：<code>Planner</code> 负责扩写产品规格，<code>Generator</code> 负责逐块实现，<code>Evaluator</code> 负责独立验收而不是让生成代理自评。",
        "<code>Planner</code> 不写细粒度实现细节，而是生成高层产品 spec 与功能范围，避免上游设计错误层层传导。",
        "<code>Generator</code> 采用 feature-at-a-time / sprint 式推进，每轮先和 <code>Evaluator</code> 协商 sprint contract，再动手实现。",
        "<code>Evaluator</code> 通过 Playwright MCP 真实操作应用，按产品深度、功能、视觉设计、代码质量等标准打分，并把缺陷写回下一轮。",
        "随模型能力增强，Anthropic 又验证了 harness 应持续“去脚手架”：在更强模型上删掉不再 load-bearing 的 sprint 结构，只保留真正有效的 planner 与 evaluator。"
      ],
      "detail": "<p><img alt=\"Harness Design 示意图\" src=\"https://www.anthropic.com/_next/image?q=75&amp;url=https%3A%2F%2Fwww-cdn.anthropic.com%2Fimages%2F4zrzovbb%2Fwebsite%2Ff94c2257964fb2d623f1e81f874977ebfc0986bc-1920x1080.gif&amp;w=3840\" />\n<em>图：Anthropic 在长程应用开发中使用的多代理 harness 思路，重点是规划、实现、评估三者分离。</em></p>\n<pre><code class=\"language-python\"># Harness Design 的抽象执行循环\nspec = planner.expand_user_prompt(user_prompt)\n\nwhile not spec.all_features_done():\n    sprint = generator.propose_sprint(spec)\n    contract = evaluator.negotiate_contract(spec, sprint)\n\n    build_result = generator.implement(contract, workspace)\n    review = evaluator.qa(contract, build_result, tools=[&quot;playwright&quot;])\n\n    if review.passed:\n        spec.mark_done(contract.feature_id)\n    else:\n        generator.consume_feedback(review)\n</code></pre>\n<p>这篇文章的出发点不是“再造一个更强的单代理”，而是承认长程应用开发天然包含三种不同工作：确定要做什么、把东西做出来、以及独立检查是否真的可用。Anthropic 在文中明确说，早期 long-running harness 已经证明了两件事有效：一是把任务拆成 tractable chunks，二是用结构化 artifact 在多次运行之间传递状态；新工作则把这套经验提升成更完整的三代理架构。</p>\n<p><code>Planner</code> 的作用是把 1 到 4 句的短 prompt 扩成完整产品规格，而不是让用户一开始就写非常细的 spec。文章特别强调，planner 应聚焦产品语境与高层技术设计，而不要过早规定具体实现细节；原因很直接，如果上游把技术细节写错，错误会沿着整条实现链传播。这个设计和传统 task decomposition 的区别在于，它不是仅列 TODO，而是给出一份足够指导后续构建、但又不过度锁死实现路径的 product spec。</p>\n<p><code>Generator</code> 负责真正的应用构建。Anthropic 在第一版 harness 里让它按 sprint 逐块推进，每次只拿一个 feature 施工，并在开工前与 <code>Evaluator</code> 协商一份 sprint contract，明确“这轮具体做什么、完成后如何验证”。这一步非常关键，因为产品 spec 故意写得偏高层，若没有 contract 层，生成代理容易把 user story 和可测试实现之间的空隙用自己的主观判断补齐，导致后续验收标准不稳定。通过 contract，生成代理和评估代理先对 done definition 达成一致，再进入代码实现。</p>\n<p><code>Evaluator</code> 是整套 harness 最重要的质量闸门。文中明确指出，代理自评时往往会过度宽容，即便在有可验证结果的软件任务中也是如此，因此要把“做事的代理”和“判断质量的代理”拆开。Anthropic 让 evaluator 使用 Playwright MCP 去真实点击运行中的应用，检查 UI 流程、API 行为、数据库状态，再按产品深度、功能、视觉设计、代码质量等维度给出是否过阈值的结论。只要任一关键维度低于阈值，该 sprint 就判失败，生成代理必须根据详细 bug 反馈返工。</p>\n<p>文章还有一个很有价值的 engineering lesson：harness 不是越复杂越好，而是要随着模型能力演进持续剥离不再必要的脚手架。作者提到，早期 4.5 时代为了克服 context anxiety，需要更重的分段与交接机制；而在后续更强模型上，部分结构开始不再 load-bearing，于是他们尝试删掉 sprint construct，只保留 planner 与最终 evaluator，把 session 变为更连续的长程构建。这个结果说明 harness 设计不是静态最佳实践，而是和模型能力共同演化的工程层对象。</p>\n<div class=\"key-point\">💡 关键：这套 harness 的核心不是“多代理数量更多”，而是把规格生成、实现、评估三种本质不同的认知工作拆给不同角色。</p>\n<p>⚠️ 注意：Anthropic 明确指出 evaluator 的价值与任务难度相关。对已落入模型原生能力边界内的任务，它可能只是额外开销；但对接近能力边界的长程应用构建，独立评估仍然显著提升结果质量。</div>",
      "quiz": {
        "q": "Harness Design 中为什么要让 generator 和 evaluator 在实现前先协商 sprint contract？",
        "options": [
          "为了让 planner 直接生成每一行代码，减少 generator 的自由度",
          "为了把高层产品 spec 转换成当前迭代可测试的完成定义，避免做错方向后才发现",
          "为了让 evaluator 接管所有代码修改，generator 只负责运行测试",
          "为了把 Playwright MCP 从 evaluator 手里移交给 generator 使用"
        ],
        "answer": 1,
        "explain": "文章强调 sprint contract 用来桥接高层 spec 与具体实现验收标准，先约定这轮要交付什么、如何测试，再开始编码，可以显著减少偏题与返工。"
      }
    },
    {
      "id": "managed_agents",
      "num": 14,
      "name": "Managed Agents",
      "fullName": "托管长程智能体服务 (Managed Agents)",
      "year": "2026.04",
      "org": "Anthropic",
      "parent": "harness_design",
      "paperUrl": "https://www.anthropic.com/engineering/managed-agents",
      "projectUrl": "",
      "category": "runtime",
      "motivation": "解耦session、harness与sandbox接口",
      "summary": "> Managed Agents 通过将智能体解耦为 Session（事件日志）、Harness（调度层）和 Sandbox（执行环境）三个独立接口，解决了长程智能体任务中组件耦合导致的可靠性差、安全性弱和扩展难的问题，使各组件可独立失败、替换与扩展。",
      "keyPoints": [
        "提出三层解耦架构：Session（不可变事件日志）、Harness（调用 Claude 并路由工具调用的循环层）、Sandbox（代码执行与文件编辑的执行环境）",
        "统一工具调用接口 <code>execute(name, input) → string</code>，使 Harness 对执行环境无感知，支持容器、手机、模拟器等多种后端",
        "Session 作为\"上下文对象\"存储在 Claude 上下文窗口之外，通过 <code>getEvents()</code> 按位置切片检索历史事件",
        "设计安全边界：凭证通过 Vault + MCP Proxy 注入，沙箱内生成的代码永远无法触及原始 Token",
        "容器从\"宠物\"变为\"牲口\"：失败后通过 <code>provision({resources})</code> 重建，不再需要人工修复",
        "Harness 无状态化：崩溃后通过 <code>wake(sessionId)</code> 恢复，从 Session 日志回放，无需持久化任何本地状态",
        "按需供应容器：仅在 Sandbox 被调用时才 provision，p50 TTFT 下降约 60%，p95 TTFT 下降超 90%",
        "支持\"多脑多手\"：多个 Harness 实例可共享多个 Sandbox，Brains 之间可传递 Hands"
      ],
      "detail": "<h5>核心架构图</h5>\n<p><img alt=\"Managed Agents 解耦架构\" src=\"https://www-cdn.anthropic.com/images/4zrzovbb/website/73e900af5b9d6ed8c64db0a8e74d4465963556b7-1640x1596.png\" />\n<em>图：Managed Agents 的三层解耦架构概览</em></p>\n<p><img alt=\"Session/Event 流示意\" src=\"https://www-cdn.anthropic.com/images/4zrzovbb/website/cf0719d7832b1f577b7393c84a7c53eecc725ca4-760x200.png\" />\n<em>图：Session 事件流与 Harness 交互示意</em></p>\n<h5>架构演进的动机：从\"宠物\"到\"牲口\"</h5>\n<p>在 Managed Agents 的早期设计中，Session、Harness 和 Sandbox 被放置在同一容器内。这种耦合设计带来两个致命缺陷：</p>\n<ol>\n<li>\n<p><strong>容器成为\"宠物\"</strong>：容器崩溃即意味着 Session 丢失，运维人员需要手动进入容器排查问题。但由于容器内留存用户数据，安全策略禁止工程师直接访问，导致调试几乎不可能。WebSocket 事件流是唯一的观测窗口，但无法定位故障发生在 Harness、网络层还是容器本身的哪个环节。</p>\n</li>\n<li>\n<p><strong>Harness 编码过时假设</strong>：Harness 隐含假设\"Claude 操作的所有资源都在同一容器内\"。当客户需要接入自有 VPC 时，只能走网络对等互联或自部署 Harness，这大幅限制了服务的接入灵活性。</p>\n</li>\n</ol>\n<div class=\"key-point\">💡 关键洞察：Harness 中编码的是\"当前模型不擅长什么\"的假设——但这些假设会随模型升级而<strong>过时</strong>（go stale）。例如 Claude Sonnet 4.5 在上下文窗口接近上限时会提前结束任务（\"上下文焦虑\"），团队在 Harness 中加入了上下文重置逻辑；但当 Opus 4.5 使用时，该行为已消失，重置逻辑变成死代码。</div>\n<p>解决方案借鉴了操作系统的设计哲学：<strong>将硬件虚拟化为抽象接口，使上层程序与底层实现解耦</strong>。如同 <code>read()</code> 系统调用不关心底层是 1970 年代的磁盘组还是现代 SSD，Managed Agents 定义了三个稳定接口，实现可以自由替换。</p>\n<h5>核心接口设计</h5>\n<p>Managed Agents 定义了以下关键接口，构成了系统的\"元 Harness\"（meta-harness）：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>接口</th>\n<th>签名</th>\n<th>职责</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>工具执行</td>\n<td><code>execute(name, input) -&gt; string</code></td>\n<td>Harness 对 Sandbox 的统一调用入口，屏蔽底层实现差异</td>\n</tr>\n<tr>\n<td>容器供应</td>\n<td><code>provision({resources})</code></td>\n<td>按需创建新的执行环境，失败后重建而非修复</td>\n</tr>\n<tr>\n<td>会话恢复</td>\n<td><code>wake(sessionId)</code></td>\n<td>从 Session 日志恢复 Harness，实现无状态化</td>\n</tr>\n<tr>\n<td>事件写入</td>\n<td><code>emitEvent(id, event)</code></td>\n<td>Harness 向 Session 追加不可变事件记录</td>\n</tr>\n<tr>\n<td>事件检索</td>\n<td><code>getSession(id)</code> / <code>getEvents()</code></td>\n<td>按位置切片查询 Session 中的历史事件</td>\n</tr>\n</tbody>\n</table></div>\n<pre><code class=\"language-python\"># Managed Agents 核心调度循环伪代码\ndef agent_loop(session_id: str):\n    # 恢复或创建 Harness（无状态）\n    harness = wake(session_id)\n    session = getSession(session_id)\n\n    # 按需 provision Sandbox（延迟绑定）\n    sandbox_id = None\n\n    while not task_complete:\n        # 从 Session 获取近期事件作为上下文\n        events = session.getEvents(slice=(-100, None))\n\n        # 构造 Claude 的上下文窗口\n        context_window = harness.transform(events)\n\n        # Claude 推理\n        response = claude.generate(context_window)\n\n        if response.is_tool_call:\n            tool_name = response.tool_name\n            tool_input = response.tool_input\n\n            if tool_name == &quot;execute&quot;:\n                # 延迟绑定：首次调用时才 provision Sandbox\n                if sandbox_id is None:\n                    sandbox_id = provision({&quot;repo&quot;: repo_url, &quot;token&quot;: bundled_auth})\n                result = execute(sandbox_id, tool_input)\n            else:\n                # 通用工具调用（MCP 等）\n                result = execute(tool_name, tool_input)\n\n            # 持久化事件到 Session\n            harness.emitEvent(session_id, {\n                &quot;type&quot;: &quot;tool_result&quot;,\n                &quot;tool&quot;: tool_name,\n                &quot;result&quot;: result\n            })\n        else:\n            # 持久化响应\n            harness.emitEvent(session_id, {\n                &quot;type&quot;: &quot;assistant_message&quot;,\n                &quot;content&quot;: response.content\n            })\n\n    return final_result\n</code></pre>\n<h5>安全边界设计：Token 隔离</h5>\n<p>在耦合架构中，Claude 生成的不可信代码与凭证共享同一容器环境——攻击者只需诱导 Claude 读取环境变量即可窃取 Token，随后生成不受限的新 Session。</p>\n<p>Managed Agents 通过<strong>结构性地确保 Token 永远不可达沙箱内部</strong>来解决此问题，采用了两种互补模式：</p>\n<ol>\n<li>\n<p><strong>凭证与资源绑定（Bundled Auth）</strong>：以 Git 操作为例，在 Sandbox 初始化时使用仓库访问 Token 克隆代码，并将 Token 仅配置在本地 Git remote 中。Sandbox 内的 <code>git push/pull</code> 正常运作，但 Agent 代码本身从不接触 Token 原文。</p>\n</li>\n<li>\n<p><strong>外部 Vault + MCP 代理（Vault-backed Proxy）</strong>：对于自定义工具，OAuth Token 存储于独立的安全 Vault 中。Claude 通过专用的 MCP 代理发起工具调用，代理使用 Session 关联的标识符从 Vault 获取凭证后，才向外部服务发起实际请求。</p>\n</li>\n</ol>\n<pre><code>┌─────────────────────────────────────────────────────────┐\n│                     Harness (Brain)                     │\n│  不感知任何凭证；仅持有 session-associated token ID      │\n└────────────┬──────────────────────────────┬─────────────┘\n             │ execute(&quot;git_push&quot;, ...)     │ execute(&quot;mcp:github&quot;, ...)\n             ▼                              ▼\n┌─────────────────────┐     ┌─────────────────────────────┐\n│    Sandbox (Hands)  │     │      MCP Proxy + Vault       │\n│  git push/pull 可用 │     │  token_id → OAuth token     │\n│  无 Token 原文      │     │  → 调用外部 API             │\n└─────────────────────┘     └─────────────────────────────┘\n</code></pre>\n<div class=\"warn-box\">⚠️ 注意：传统做法是缩小 Token 权限范围（narrow scoping），但这本身编码了\"Claude 用受限 Token 做不到什么\"的假设——而 Claude 正变得越来越聪明。结构隔离才是根本解法。</div>\n<h5>Session：不是上下文窗口，而是上下文对象</h5>\n<p>长程任务常常超出 Claude 的上下文窗口长度。传统的解决方案（compaction 摘要、选择性裁剪、memory tool 写入文件）存在一个根本性困境：<strong>难以预知未来的推理轮次需要哪些历史 Token</strong>。</p>\n<p>Managed Agents 的创新在于将 Session 视为一个<strong>存活于上下文窗口之外的持久对象</strong>（context object that lives outside the context window）。关键接口 <code>getEvents()</code> 允许 Harness 以高度灵活的方式检索历史：</p>\n<ul>\n<li><strong>位置切片</strong>：<code>getEvents(slice=(-50, None))</code> 读取最近 50 个事件</li>\n<li><strong>时间回溯</strong>：在某关键时刻前多取几个事件以查看前因后果</li>\n<li><strong>选择性重读</strong>：重读特定操作之前的上下文</li>\n</ul>\n<p>获取到的事件在传入 Claude 上下文窗口之前，Harness 可进行任意转换（重组、修剪、缓存优化等）。这种设计将<strong>可恢复的上下文存储</strong>（Session 负责）与<strong>上下文工程优化</strong>（Harness 负责）的关注点彻底分离。</p>\n<div class=\"key-point\">💡 关键：我们无法预测未来模型需要什么上下文工程策略——因此接口只保证 Session 持久可用，而将具体的上下文管理策略推入 Harness，由 Harness 自由选择实现。</div>\n<h5>性能收益：按需供应与延迟绑定</h5>\n<p>解耦架构带来了直接的性能收益。在耦合设计中，每个 Session 必须预先启动包含 Sandbox 的完整容器——即使 Session 可能永远不会用到代码执行能力。这引入了显著的时间到首 Token 延迟（TTFT），是用户最直观感受到的延迟指标。</p>\n<p>解耦后：</p>\n<ol>\n<li><strong>Harness 启动极快</strong>：Harness 是无状态的轻量进程，仅需从 Session 日志中拉取待处理事件即可开始推理</li>\n<li><strong>Sandbox 按需供应</strong>：只有在 Claude 决定调用 <code>execute()</code> 时才 provision 容器，大量无需代码执行的 Session 永远不会触发容器创建</li>\n<li><strong>并行扩展简单</strong>：扩展至多个 Brain 只需启动多个无状态 Harness 实例，按需连接 Hands</li>\n</ol>\n<p>实际效果：<strong>p50 TTFT 下降约 60%，p95 TTFT 下降超过 90%</strong>。</p>\n<h5>多脑多手：从单进程到分布式智能体网络</h5>\n<p>解耦架构还使\"多脑多手\"成为可能。在耦合设计中，一个容器内只有一个 Harness 和一个 Sandbox。解耦后：</p>\n<ul>\n<li><strong>一个 Brain 可连接多个 Hands</strong>：Harness 可以管理多个独立的 Sandbox，Claude 需要在不同执行环境间协调工作——这是一个更难的认知任务，但随着模型智能增长已成为可能</li>\n<li><strong>Brains 可传递 Hands</strong>：由于 Hand 不绑定任何特定 Brain，多个 Harness 实例可以共享和传递 Sandbox 引用</li>\n<li><strong>按领域优化 Harness</strong>：可以为不同任务定制专业化 Harness（如 Claude Code），它们都通过同一套接口接入 Managed Agents</li>\n</ul>\n<div class=\"key-point\">💡 关键：Managed Agents 是一个\"元 Harness\"——不对具体 Harness 的实现做假设，而是对 Claude 周围所需的接口做假设：操纵状态（Session）和执行计算（Sandbox）。这些接口不关心 Sandbox 是一个容器、一部手机还是 Pokémon 模拟器。</div>",
      "quiz": {
        "q": "Managed Agents 中 Session 接口 getEvents() 的核心设计目的是什么？",
        "options": [
          "替代 Claude 的上下文窗口，直接作为推理输入",
          "将历史上下文作为持久对象存储在上下文窗口之外，允许按需检索和回放",
          "提高事件存储的压缩比率，降低存储成本",
          "为 Harness 提供实时 WebSocket 事件推送能力"
        ],
        "answer": 1,
        "explain": "getEvents() 将 Session 设计为存活于上下文窗口外的持久对象，支持位置切片和时间回溯检索，解决了长程任务中不可逆的上下文压缩导致的信息丢失问题。"
      }
    },
    {
      "id": "genericagent",
      "num": 15,
      "name": "GenericAgent",
      "fullName": "自进化通用 Agent Harness (GenericAgent)",
      "year": "2026.04",
      "org": "复旦/北大/Qiji Zhifeng",
      "parent": "claude_longrun",
      "paperUrl": "https://arxiv.org/abs/2604.17091",
      "projectUrl": "",
      "category": "runtime",
      "motivation": "以最小工具和自演化记忆控制上下文",
      "summary": "GenericAgent 通过**上下文信息密度最大化**原则，系统性解决了 LLM Agent 设计中的\"完备性-简洁性-自然性\"三难困境，提出了包含最小化工具集、分层记忆架构、自演化机制和上下文压缩在内的四组件框架，使 Agent 能够在无需人工干预的情况下持续自我改进。",
      "keyPoints": [
        "提出了 <strong>Agent 设计三难困境</strong>：完备性（Completeness）、简洁性（Conciseness）、自然性（Naturalness）三者不可兼得，传统设计只能在三者间做权衡",
        "提出<strong>上下文信息密度最大化</strong>作为统一设计原则，以 bits/token 为度量指标",
        "设计了仅包含 <strong>9 个原子工具的极小工具集</strong>（TaskTool, ReadTool, WriteTool, EditTool, WebSearchTool, BrowseTool, CodeTool, AskUserTool, FinishTool），通过组合实现复杂任务",
        "创新性地提出 <strong>4 层分层记忆架构</strong>：L1（当前上下文）、L2（工作记忆/Working Memory）、L3（经验记忆/经验池）、L4（核心规则/Constitution），信息随层级上升逐步压缩精炼",
        "提出 <strong>\"No Execution, No Memory\"</strong> 自演化机制：仅当 Agent 实际执行并观察到结果后，才将经验写入记忆，防止幻觉污染记忆库",
        "设计了<strong>上下文截断与压缩</strong>策略：当上下文接近限制时，自动对历史消息进行摘要压缩，保留关键信息",
        "发现了 <strong>3 种涌现能力</strong>：(1) 工具组合创新 (2) 自适应记忆管理 (3) 跨任务经验迁移",
        "在 WebArena、GAIA、AgentBench 等多个基准上达到 SOTA，且仅需极少人工设计"
      ],
      "detail": "<h5>1. 设计三难困境与信息密度最大化</h5>\n<p>传统 LLM Agent 设计面临一个根本性的三难困境：</p>\n<ul>\n<li><strong>完备性（Completeness）</strong>：提供足够多的工具和指令以覆盖所有可能场景 → 导致上下文膨胀</li>\n<li><strong>简洁性（Conciseness）</strong>：保持系统提示和工具描述简短 → 导致功能不完备</li>\n<li><strong>自然性（Naturalness）</strong>：使用自然语言描述而非结构化格式 → 导致模糊和歧义</li>\n</ul>\n<p>三者相互冲突：追求完备性必然增加冗长，追求简洁性必然牺牲完备性，追求自然性必然降低信息密度。GenericAgent 的核心洞察是：<strong>不应在三维空间中寻找平衡点，而应直接优化上下文信息密度</strong>——即单位 token 包含的有效信息量（bits/token）。</p>\n<div class=\"key-point\">💡 关键：信息密度最大化 = 用最少的 token 传递最多的有效信息。这成为架构设计的唯一指导原则。</div>\n<p><img alt=\"GenericAgent 工作流示意图\" src=\"https://raw.githubusercontent.com/lsdefine/GenericAgent/main/assets/images/workflow.jpg\" />\n<em>图：Agent设计的三难困境——完备性、简洁性、自然性三者不可兼得。GenericAgent通过信息密度最大化跳出这一困境。</em></p>\n<h5>2. 四组件架构总览</h5>\n<p>GenericAgent 由四个核心组件组成，每个组件都围绕信息密度最大化原则设计：</p>\n<p>┌─────────────────────────────────────────────┐\n│              GenericAgent 架构               │\n├─────────────────┬───────────────────────────┤\n│  原子工具集(9)   │     分层记忆(L1-L4)       │\n│  TaskTool       │  L1: 当前上下文            │\n│  ReadTool       │  L2: 工作记忆              │\n│  WriteTool      │  L3: 经验池                │\n│  EditTool       │  L4: 核心规则(Constitution)│\n│  WebSearchTool  │                           │\n│  BrowseTool     ├───────────────────────────┤\n│  CodeTool       │  自演化机制                │\n│  AskUserTool    │  \"No Execution, No Memory\" │\n│  FinishTool     │                           │\n├─────────────────┼───────────────────────────┤\n│  上下文压缩       │  涌现能力                  │\n│  截断+摘要       │  工具组合/记忆管理/经验迁移 │\n└─────────────────┴───────────────────────────┘</p>\n<h5>3. 最小化原子工具集（Minimal Atomic Tool Set）</h5>\n<p>传统 Agent 框架倾向于为每个功能创建专门工具（如单独的文件搜索、文件移动、目录列表等），导致工具描述占用大量上下文。GenericAgent 反其道而行，仅提供 9 个原子操作：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>工具</th>\n<th>功能</th>\n<th>设计理念</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><strong>TaskTool</strong></td>\n<td>创建和管理子任务</td>\n<td>分解复杂任务为子任务</td>\n</tr>\n<tr>\n<td><strong>ReadTool</strong></td>\n<td>读取文件内容（支持分页/搜索）</td>\n<td>统一的读取接口，替代所有\"查看\"类工具</td>\n</tr>\n<tr>\n<td><strong>WriteTool</strong></td>\n<td>创建或全量覆盖文件</td>\n<td>统一的写入接口</td>\n</tr>\n<tr>\n<td><strong>EditTool</strong></td>\n<td>精细的局部文件修改（patch）</td>\n<td>替代所有\"修改\"类工具</td>\n</tr>\n<tr>\n<td><strong>WebSearchTool</strong></td>\n<td>网络搜索</td>\n<td>获取外部信息</td>\n</tr>\n<tr>\n<td><strong>BrowseTool</strong></td>\n<td>网页浏览/内容提取</td>\n<td>统一的网页交互接口</td>\n</tr>\n<tr>\n<td><strong>CodeTool</strong></td>\n<td>代码执行</td>\n<td>计算和自动化</td>\n</tr>\n<tr>\n<td><strong>AskUserTool</strong></td>\n<td>向用户提问/请求决策</td>\n<td>人机交互</td>\n</tr>\n<tr>\n<td><strong>FinishTool</strong></td>\n<td>标记任务完成并提交结果</td>\n<td>任务终结</td>\n</tr>\n</tbody>\n</table></div>\n<p>通过这 9 个工具的<strong>组合</strong>，Agent 可以实现任意复杂操作（如：先 ReadTool 定位 → EditTool 修改 → WriteTool 保存，模拟\"文件重命名\"）。工具描述极简，每个仅 1-2 行，极大提升了系统提示的信息密度。</p>\n<h5>4. 四层分层记忆架构</h5>\n<p>传统 Agent 将所有信息都放在单一上下文中，导致关键信息被稀释。GenericAgent 设计了从\"原始\"到\"精炼\"的四层记忆：</p>\n<p>L4: 核心规则 (Constitution)        ← 极简，~200 tokens，不可变核心原则\n    ↑ 提炼\nL3: 经验池 (Experience Pool)       ← 经过验证的任务经验，RAG检索\n    ↑ \"No Execution, No Memory\"过滤\nL2: 工作记忆 (Working Memory)      ← 当前会话关键信息，人工+自动维护\n    ↑ 上下文压缩/截断\nL1: 当前上下文 (Current Context)    ← 完整对话历史，受token限制</p>\n<ul>\n<li><strong>L1（当前上下文）</strong>：包含完整的对话历史、工具调用和结果。当接近 token 限制时触发压缩。</li>\n<li><strong>L2（工作记忆/Working Memory）</strong>：从 L1 中提取的关键信息快照，通过 <code>update_working_checkpoint</code> 工具主动维护。相当于人类的\"便签本\"，防止长时间任务中信息丢失。</li>\n<li><strong>L3（经验池/Experience Pool）</strong>：跨会话的经过验证的经验和教训，通过 RAG 机制检索。遵循 <strong>\"No Execution, No Memory\"</strong> 原则——仅当 Agent 实际执行并观察到结果后，才将经验写入，有效防止\"纸上谈兵\"类错误记忆。</li>\n<li><strong>L4（核心规则/Constitution）</strong>：Agent 不可违背的根本原则，固定不变。类似机器人三定律，由开发者预设。</li>\n</ul>\n<div class=\"warn-box\">⚠️ 注意：记忆向上层迁移时，信息被逐级<strong>压缩精炼</strong>，遵循信息密度最大化原则。原始对话 → 关键摘要 → 经验规则 → 核心原则。</div>\n<h5>5. \"No Execution, No Memory\" 自演化机制</h5>\n<p>这是 GenericAgent 最核心的创新之一。传统方法允许 Agent 在推理阶段就生成\"记忆\"或\"经验\"，但这些未经实际验证的记忆往往是<strong>幻觉</strong>。</p>\n<p>GenericAgent 的铁律：\n- 任何经验在写入 L3 经验池之前，必须经过<strong>实际的工具执行</strong>和<strong>结果观察</strong>\n- 纯语言模型推理产出的\"建议\"不能直接成为记忆\n- 只有被实际验证有效的操作序列才能被提炼为经验</p>\n<p>这类似于科学方法：假设必须经过实验验证才能成为理论。该机制大幅减少了记忆污染，确保经验池中的每一条记录都有实证支撑。</p>\n<h5>6. 上下文截断与压缩</h5>\n<p>当 L1 上下文接近模型 token 限制时，GenericAgent 自动触发压缩：</p>\n<ol>\n<li><strong>截断</strong>：保留最近的 K 轮交互（K 为可配置参数）</li>\n<li><strong>摘要压缩</strong>：对截断部分的历史消息，使用 LLM 生成结构化摘要</li>\n<li><strong>摘要格式</strong>：<code>[历史摘要]</code> 标签包裹，包含任务目标、已完成步骤、关键发现、待处理事项</li>\n<li><strong>信息密度提升</strong>：原始历史可能消耗 10K+ tokens，摘要压缩至 200-500 tokens，信息密度提升 20-50 倍</li>\n</ol>\n<div class=\"key-point\">💡 关键：压缩不是简单丢弃信息，而是<strong>蒸馏</strong>——提取对当前任务仍有价值的部分，丢弃已完成的中间细节。</div>\n<pre><code class=\"language-python\"># 上下文压缩伪代码\ndef compress_context(messages, max_tokens):\n    if token_count(messages) &lt;= max_tokens:\n        return messages\n\n    # 保留最近 N 轮\n    recent = messages[-N:]\n    old = messages[:-N]\n\n    # 生成摘要\n    summary = llm.summarize(old, focus=[\n        &quot;current_task_goal&quot;,\n        &quot;completed_steps&quot;, \n        &quot;key_findings&quot;,\n        &quot;pending_items&quot;\n    ])\n\n    # 拼接返回\n    return [{&quot;role&quot;: &quot;system&quot;, &quot;content&quot;: f&quot;[历史摘要] {summary}&quot;}] + recent\n</code></pre>\n<h5>7. 三种涌现能力</h5>\n<p>实验发现 GenericAgent 展现出三种未在系统中显式编程的能力：</p>\n<p><strong>① 工具组合创新</strong>：Agent 自发发现并利用工具组合实现新功能。例如，组合 ReadTool + EditTool + WriteTool 实现\"文件重命名\"（该系统无 rename 工具），甚至组合 WebSearchTool + CodeTool 实现数据分析自动化。</p>\n<p><strong>② 自适应记忆管理</strong>：Agent 学会根据任务复杂度动态调整记忆写入策略。简单任务自动减少 L2 更新频率，复杂任务则频繁保存 checkpoint，展现出类似人类的\"元认知\"能力。</p>\n<p><strong>③ 跨任务经验迁移</strong>：在一个任务中学到的经验（如\"处理大文件时应先用 ReadTool 分页查看而非全量读取\"）能自动迁移到其他任务中，通过 L3 经验池的 RAG 检索实现。</p>\n<h5>8. 与现有方法的对比</h5>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>维度</th>\n<th>ReAct</th>\n<th>AutoGPT</th>\n<th>Reflexion</th>\n<th><strong>GenericAgent</strong></th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>工具数量</td>\n<td>3-5（任务相关）</td>\n<td>10-20（通用）</td>\n<td>3-5</td>\n<td><strong>9（最小原子集）</strong></td>\n</tr>\n<tr>\n<td>记忆机制</td>\n<td>无</td>\n<td>文件存储</td>\n<td>经验反思</td>\n<td><strong>4层分层记忆</strong></td>\n</tr>\n<tr>\n<td>自演化</td>\n<td>✗</td>\n<td>✗</td>\n<td>部分（反思）</td>\n<td><strong>No Execution, No Memory</strong></td>\n</tr>\n<tr>\n<td>上下文管理</td>\n<td>截断</td>\n<td>截断</td>\n<td>截断</td>\n<td><strong>截断+摘要压缩</strong></td>\n</tr>\n<tr>\n<td>信息密度优化</td>\n<td>✗</td>\n<td>✗</td>\n<td>✗</td>\n<td><strong>核心设计原则</strong></td>\n</tr>\n</tbody>\n</table></div>",
      "quiz": {
        "q": "GenericAgent 的 'No Execution, No Memory' 机制解决的核心问题是什么？",
        "options": [
          "减少记忆存储的磁盘占用",
          "防止 Agent 将未经实际验证的推理产物作为经验写入记忆库，避免幻觉污染",
          "加快工具调用的执行速度",
          "限制 Agent 每天可执行的任务数量"
        ],
        "answer": 1,
        "explain": "'No Execution, No Memory' 要求任何经验在写入 L3 经验池前必须经过实际的工具执行和结果观察验证，纯推理产物不能成为记忆，从而有效防止幻觉污染记忆库。"
      }
    },
    {
      "id": "ahe",
      "num": 16,
      "name": "AHE",
      "fullName": "自动演化编码 Harness (Agentic Harness Engineering)",
      "year": "2026.04",
      "org": "复旦/北大/Qiji Zhifeng",
      "parent": "meta_harness",
      "paperUrl": "https://arxiv.org/abs/2604.25850",
      "projectUrl": "",
      "category": "optimization",
      "motivation": "用可观测闭环自动进化编码harness",
      "summary": "AHE 提出了一种智能体驱动的启发式进化框架，通过**三支柱闭环**（组件空间、Rollout 经验、编辑决策）让三个角色智能体（Evolve / Explore / Exploit）自动迭代改进 LLM Agent 的 7 类启发式组件，在 Terminal-Bench 2 上将纯 Bash 种子从 69.7% 提升至 77.0%，无需人工干预。",
      "keyPoints": [
        "首次将 LLM Agent 的启发式组件调优形式化为自动化闭环进化问题，替代手工试错",
        "三支柱闭环架构：<strong>Components</strong>（7 类可编辑启发式）→ <strong>Rollout Experience</strong>（执行轨迹可观测）→ <strong>Edit Decisions</strong>（结构化编辑决策）",
        "三个角色 Agent 协同：<strong>Evolve</strong>（诊断建议编辑）→ <strong>Exploit</strong>（验证编辑是否修复目标任务）→ <strong>Explore</strong>（检验编辑是否破坏其他任务）",
        "覆盖 7 类启发式组件类型：system prompt、bash tool、python tool、memory、sandbox policy、stop condition、perplexity filter",
        "10 轮迭代从空组件种子起步，在 89 个 Terminal-Bench 2 任务上取得 77.0% 的 Pass@1",
        "跨模型迁移能力：GPT-5.4 进化的 workspace 迁移至 GPT-5.1 达 74.2%，迁移至 Sonnet 4.5 达 73.4%",
        "自预测校准：Evolve 模型在\"修复预测\"任务上精度 ~0.60、回归预测精度 ~0.92"
      ],
      "detail": "<p><img alt=\"AHE 性能总览图\" src=\"https://ar5iv.org/html/2604.25850/assets/x1.png\" />\n<em>图 1：AHE 将纯 Bash 种子进化为超越所有人工设计和自进化基线，三个角色 Agent 共享一个基础模型（GPT-5.4 high）</em></p>\n<p><img alt=\"AHE 三支柱闭环架构\" src=\"https://ar5iv.org/html/2604.25850/assets/x2.png\" />\n<em>图 2：AHE 流水线将三个可观测表面连接为一个闭环——组件、Rollout 经验、编辑决策各自呈现为结构化文本表面，驱动迭代进化</em></p>\n<h5>动机与背景</h5>\n<p>传统 LLM Agent 的性能高度依赖启发式组件（如 system prompt、工具定义、sandbox 策略等）的质量，但这些组件的设计完全依赖人工经验和反复试错。随着 Agent 复杂度增加，组件空间呈组合爆炸，手工调优成为严重瓶颈。AHE 的核心洞察是：<strong>启发式组件的设计空间虽然庞大，但其质量可以通过执行反馈（Rollout）被自动评估，而编辑决策可以被结构化表达</strong>——这三个\"可观测表面\"构成了自动化进化的基础。</p>\n<h5>核心机制：三支柱闭环</h5>\n<p>AHE 将 Agent 进化形式化为在三个结构化文本表面上的闭环迭代：</p>\n<ol>\n<li>\n<p><strong>组件表面（Component Surface）</strong>：Agent 的完整配置，包含 7 类可编辑启发式组件。每类组件由结构化 YAML/文本块表示，Evolve 智能体可以对任意组件子集进行增删改操作。</p>\n</li>\n<li>\n<p><strong>Rollout 经验表面（Rollout Experience Surface）</strong>：每次任务执行后产生的完整轨迹——包括 Agent 的推理链（chain-of-thought）、工具调用序列、工具输出、最终结果（成功/失败/错误类型）。Explore 智能体通过分析失败 Rollout 来发现回归问题。</p>\n</li>\n<li>\n<p><strong>编辑决策表面（Edit Decision Surface）</strong>：Evolve 智能体观察 Rollout 经验后生成的结构化编辑提案。每个编辑包含：目标组件路径、操作类型（add/modify/delete）、旧内容片段、新内容片段、编辑理由。这一结构化设计使编辑可追溯、可回滚。</p>\n</li>\n</ol>\n<h5>三个角色智能体</h5>\n<ul>\n<li>\n<p><strong>Evolve（进化者）</strong>：核心决策者。接收多轮 Rollout 经验作为输入，诊断失败原因，生成结构化编辑提案。Evolve 需要同时考虑\"修复当前问题\"和\"避免引入回归\"之间的平衡。</p>\n</li>\n<li>\n<p><strong>Exploit（利用者）</strong>：验证编辑是否有效修复了目标失败案例。在应用编辑前后的 Agent 配置上分别运行失败任务，对比结果。如果修复成功，编辑进入候选池。</p>\n</li>\n<li>\n<p><strong>Explore（探索者）</strong>：检验编辑是否引入回归。在全部 89 个任务上运行应用编辑后的 Agent，检测是否有原本成功的任务变失败。这一角色解决了进化中的\"灾难性遗忘\"问题——修复一个任务不应破坏其他任务。</p>\n</li>\n</ul>\n<p>三者共享同一个基础模型（GPT-5.4 high），通过不同的 system prompt 区分角色。这种设计简洁而有效：单个强大模型在不同上下文中扮演不同角色，无需训练多个专用模型。</p>\n<h5>七类可编辑启发式组件</h5>\n<p>AHE 的组件空间覆盖了 LLM Agent 的七个关键维度：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>组件类型</th>\n<th>说明</th>\n<th>示例</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>system prompt</td>\n<td>Agent 顶层指令</td>\n<td>角色定义、约束条件、输出格式要求</td>\n</tr>\n<tr>\n<td>bash tool</td>\n<td>Shell 命令工具定义</td>\n<td>可用命令列表、参数规范、安全限制</td>\n</tr>\n<tr>\n<td>python tool</td>\n<td>Python 执行工具定义</td>\n<td>可用库列表、执行超时、内存限制</td>\n</tr>\n<tr>\n<td>memory</td>\n<td>记忆/上下文管理策略</td>\n<td>滑动窗口大小、摘要触发条件、关键信息提取规则</td>\n</tr>\n<tr>\n<td>sandbox policy</td>\n<td>沙箱安全策略</td>\n<td>网络访问权限、文件系统限制、进程隔离规则</td>\n</tr>\n<tr>\n<td>stop condition</td>\n<td>停止条件</td>\n<td>最大步数、输出验证规则、循环检测阈值</td>\n</tr>\n<tr>\n<td>perplexity filter</td>\n<td>困惑度过滤器</td>\n<td>低质量输出的检测与截断阈值</td>\n</tr>\n</tbody>\n</table></div>\n<p>所有组件初始化为空（bash-only 种子），AHE 在 10 轮迭代中自主发现并添加有效组件。</p>\n<h5>训练/进化流程</h5>\n<p>每轮迭代包含以下步骤：</p>\n<ol>\n<li>\n<p><strong>Rollout 阶段</strong>：在当前 Agent 配置下，对所有 89 个 Terminal-Bench 2 任务各运行 k 次（k=4），收集成功/失败信息及完整轨迹。</p>\n</li>\n<li>\n<p><strong>Evolve 阶段</strong>：将失败案例的 Rollout 经验（最多 3 个代表性案例）提交给 Evolve 智能体，生成编辑提案。Evolve 同时进行自预测——对每项编辑预测其修复效果和可能的回归影响。</p>\n</li>\n<li>\n<p><strong>Exploit 阶段</strong>：对每个编辑提案，在目标失败任务上重新运行（应用编辑后配置），验证修复效果。</p>\n</li>\n<li>\n<p><strong>Explore 阶段</strong>：将通过 Exploit 验证的编辑应用到 Agent 配置，在全部任务上运行，检测回归。如果净收益为正（修复数 &gt; 新引入失败数），接受编辑；否则回滚。</p>\n</li>\n<li>\n<p><strong>更新组件表面</strong>：将接受的编辑合并到组件配置中，进入下一轮迭代。</p>\n</li>\n</ol>\n<h5>与传统方法的区别</h5>\n<ul>\n<li>\n<p><strong>vs 手工调优</strong>：AHE 消除人工试错，在 10 轮迭代内发现人类难以搜到的组件组合。实验显示所有手工设计的基线 Agent 均被超越。</p>\n</li>\n<li>\n<p><strong>vs 自进化基线</strong>：AHE 的三支柱闭环设计（特别是 Explore 角色的回归检测）使其进化稳定性远超简单的\"用 LLM 改 prompt\"方法。自我改进基线在 Terminal-Bench 2 上的改进幅度（~2-3%）远低于 AHE（~7.3%）。</p>\n</li>\n<li>\n<p><strong>vs Learned Optimizer</strong>：AHE 无需训练元学习器或优化器网络，完全通过 LLM 的上下文推理能力实现进化，避免了大量训练数据和计算开销。</p>\n</li>\n</ul>\n<h5>关键实验结果</h5>\n<ul>\n<li><strong>主结果</strong>：10 轮进化，Pass@1 从 69.7%（bash-only seed）提升至 77.0%（+7.3 个百分点），超越所有基线。</li>\n<li><strong>跨模型迁移</strong>：GPT-5.4 进化的 workspace 直接迁移至 GPT-5.1（74.2%）和 Sonnet 4.5（73.4%），证明进化的组件具有模型通用性，而非过拟合到特定模型。</li>\n<li><strong>自预测能力</strong>：Evolve 模型对修复预测的精度约 0.60（较保守），对回归预测的精度约 0.92（高度准确），表明模型能较准确地预判编辑是否会引入副作用。</li>\n</ul>\n<p><img alt=\"跨模型迁移结果\" src=\"https://ar5iv.org/html/2604.25850/assets/x3.png\" />\n<em>图 3：跨模型迁移——AHE workspace 在 GPT-5.4 上进化后，在其他基础模型上重新评估的结果</em></p>\n<p><img alt=\"自预测精度\" src=\"https://ar5iv.org/html/2604.25850/assets/x4.png\" />\n<em>图 4：Evolve 模型在 9 轮评估中的自预测精度和召回率变化趋势</em></p>\n<h5>Ablation 分析</h5>\n<ul>\n<li><strong>去除 Explore</strong>：回归率显著上升，净提升大幅缩水，验证了 Explore 在防止灾难性遗忘中的关键作用。</li>\n<li><strong>去除 Exploit</strong>：编辑质量下降，许多\"看起来合理\"的编辑在实际执行中无效，Exploit 的过滤作用不可或缺。</li>\n<li><strong>减少 Rollout 次数</strong>：k=1 时进化几乎停滞，k=4 显著优于 k=2，说明充分的执行反馈对诊断和修复至关重要。</li>\n<li><strong>组件类型消融</strong>：7 类组件各有贡献，其中 system prompt 和 sandbox policy 的贡献最大，但单独进化任何一类都不及全组件联合进化。</li>\n</ul>\n<h5>伪代码：AHE 核心循环</h5>\n<pre><code class=\"language-python\"># AHE 核心进化循环\nworkspace = initialize_empty_components()  # 空组件种子\nfor round in range(1, R+1):  # R=10\n    # 1. Rollout: 在所有任务上执行\n    rollouts = run_all_tasks(workspace, tasks, k=4)\n\n    # 2. Evolve: 诊断失败并生成编辑\n    failures = [r for r in rollouts if not r.success]\n    edits = evolve_agent.diagnose_and_propose(failures[:3])\n\n    # 3. Exploit: 验证编辑修复效果\n    for edit in edits:\n        if exploit_agent.verify_fix(workspace, edit, edit.target_task):\n            validated_edits.append(edit)\n\n    # 4. Explore: 检测回归\n    new_workspace = apply_edits(workspace, validated_edits)\n    new_rollouts = run_all_tasks(new_workspace, tasks, k=1)\n    regressions = detect_regressions(rollouts, new_rollouts)\n\n    # 5. 条件接受\n    if net_gain(validated_edits, regressions) &gt; 0:\n        workspace = new_workspace  # 接受进化\n    else:\n        continue  # 回滚，保持原配置\n</code></pre>\n<div class=\"key-point\">💡 <strong>关键设计</strong>：三支柱闭环的核心优势在于\"可观测性\"——组件的每次变更、每次执行的轨迹、每次编辑的决策都有结构化记录，使进化过程完全可审计、可回滚、可解释。</p>\n<p>⚠️ <strong>注意</strong>：AHE 的有效性依赖于基础模型的多轮推理能力。在当前实验中使用 GPT-5.4 high，较弱模型可能在诊断失败和预测回归时表现不佳，导致进化效率降低。</div>",
      "quiz": {
        "q": "AHE 中 Explore 角色的主要作用是什么？",
        "options": [
          "生成启发式组件的编辑提案",
          "验证编辑是否修复了目标任务",
          "检测编辑是否在其他任务上引入回归",
          "收集所有任务的 Rollout 经验"
        ],
        "answer": 2,
        "explain": "Explore 在全部任务上运行应用编辑后的 Agent，检测原本成功的任务是否变失败，防止进化中的灾难性遗忘。"
      }
    },
    {
      "id": "openclaw",
      "num": 17,
      "name": "OpenClaw",
      "fullName": "统一 Agent 控制平面 (OpenClaw)",
      "year": "2026.05",
      "org": "OpenClaw",
      "parent": "codex",
      "paperUrl": "https://docs.openclaw.ai/plugins/sdk-agent-harness",
      "projectUrl": "",
      "category": "runtime",
      "motivation": "把Codex等原生运行时并入统一控制面",
      "summary": "OpenClaw 提出 Agent Harness 插件 SDK，通过将 Codex 等原生 Agent 运行时抽象为可注册的 harness 插件，在统一的控制平面（session、tool policy、channel、transcript）下调度异构运行时，解决了多运行时共存时 provider/runtime 耦合、回退策略混乱和会话状态割裂的问题。",
      "keyPoints": [
        "定义 Agent Harness 抽象层：将一次 prepared agent turn 的低级执行封装为标准接口，与 provider、channel、tool registry 解耦",
        "提供 <code>openclaw/plugin-sdk/agent-harness</code> 公共 SDK，第三方可通过 <code>registerAgentHarness()</code> 注册原生运行时",
        "三级运行时选择策略：Model-scoped &gt; Provider-scoped &gt; auto（插件候选匹配）&gt; embedded fallback",
        "引入 <code>runtimePlan</code> 策略包：包含 tools.normalize、transcript.resolvePolicy、delivery.isSilentPayload、outcome.classifyRunResult 等共享决策模块，harness 可读取但不可修改",
        "Codex 插件作为参考实现：provider + harness 配对模式，harness id 为 <code>codex</code>，OpenAI 模型引用默认路由到 Codex harness",
        "运行时中立的工具结果中间件（<code>registerAgentToolResultMiddleware</code>）：替代旧的 Codex-only 和 embedded-only 扩展钩子",
        "终端结果分类器 <code>classifyAgentHarnessTerminalOutcome</code>：区分 empty/reasoning-only/planning-only 以支持模型回退决策",
        "严格的运行时绑定：一旦插件 harness 认领运行，不回退到其他运行时，避免副作用重复或语义变更",
        "Native session 与 OpenClaw transcript 双轨镜像机制：harness 维护原生会话 id，同时向 OpenClaw transcript 同步可见输出"
      ],
      "detail": "<p><img alt=\"OpenClaw 示意图\" src=\"https://docs.openclaw.ai/assets/pixel-lobster.svg\" />\n<em>图：OpenClaw 的核心框架或系统示意。</em></p>\n<h5>架构总览</h5>\n<p>OpenClaw 的 Agent Harness 架构将 Agent 执行栈分为<strong>控制平面</strong>（OpenClaw Core）和<strong>执行平面</strong>（Harness Plugin）两层：</p>\n<pre><code>+-----------------------------------------------------------+\n|                   OpenClaw Core (控制平面)                  |\n|  +----------+ +----------+ +--------+ +---------------+  |\n|  | Provider | | Channel  | | Session| | Tool Policy   |  |\n|  | Resolver | | Reply    | | Store  | | &amp; Sandbox     |  |\n|  +----------+ +----------+ +--------+ +---------------+  |\n|                         |                                  |\n|               runtimePlan (只读策略包)                       |\n|  +------------------------------------------------------+ |\n|  | tools.normalize | transcript.resolvePolicy           | |\n|  | delivery.isSilentPayload | outcome.classify          | |\n|  | observability (provider/model/harness metadata)      | |\n|  +------------------------------------------------------+ |\n|                         |                                  |\n|         +---------------+--------------+                 |\n|         |   Harness Selection Policy     |                 |\n|         |  Model &gt; Provider &gt; auto       |                 |\n|         |  &gt; embedded fallback           |                 |\n|         +---------------+--------------+                 |\n+-------------------------+--------------------------------+\n                          |\n+-------------------------+--------------------------------+\n|              Agent Harness Plugin (执行平面)                |\n|  +------------------------+---------+                    |\n|  |  supports(ctx) -&gt; {supported, priority}               |\n|  |  runAttempt(params) -&gt; AgentTurnResult                 |\n|  |  reset(sessionId)                                     |\n|  +------------------------------------------------------+ |\n|  +----------+ +-----------+ +------------------+        |\n|  |  Codex   | | Claude CLI| | Custom Daemon... |        |\n|  |  Harness | |  Harness  | |                    |        |\n|  +----------+ +-----------+ +------------------+        |\n|       |                                                   |\n|  +----+------------------------------------------+       |\n|  | Native Session (thread id, resume token)       |       |\n|  |    &lt;-&gt; Transcript Mirror (同步到 OpenClaw)      |       |\n|  +-----------------------------------------------+       |\n+-----------------------------------------------------------+\n</code></pre>\n<p><em>图：OpenClaw Agent Harness 双层架构——控制平面负责 provider/channel/session/tool policy 决策并将 runtimePlan 下发给 harness；执行平面通过 supports/runAttempt/reset 接口接入原生运行时，并通过 Transcript Mirror 保持会话一致性。</em></p>\n<h5>核心接口定义</h5>\n<p>Harness 插件的核心接口（TypeScript）：</p>\n<pre><code class=\"language-typescript\">// 注册入口\nimport { definePluginEntry } from &quot;openclaw/plugin-sdk&quot;;\n\n// Harness 定义\nconst myHarness: AgentHarness = {\n  id: &quot;my-harness&quot;,\n  label: &quot;My native agent harness&quot;,\n\n  // 声明支持条件：基于 ctx.provider + ctx.model 决策\n  supports(ctx) {\n    return ctx.provider === &quot;my-provider&quot; \n      ? { supported: true, priority: 100 } \n      : { supported: false };\n  },\n\n  // 执行一次准备好的 turn\n  async runAttempt(params) {\n    // params 包含：prompt, tools, images, onPartialReply,\n    // onAgentEvent, runtimePlan, sessionId...\n    return await runMyNativeTurn(params);\n  },\n};\n\nexport default definePluginEntry({\n  id: &quot;my-native-agent&quot;,\n  name: &quot;My Native Agent&quot;,\n  description: &quot;Runs selected models through a native agent daemon.&quot;,\n  register(api) {\n    api.registerAgentHarness(myHarness);\n  },\n});\n</code></pre>\n<h5>动机与背景</h5>\n<p>传统 Agent 框架在处理多运行时共存时面临三个核心问题：</p>\n<ol>\n<li>\n<p><strong>Provider/Runtime 耦合</strong>：每个模型 provider 的 API 传输层与 Agent 执行逻辑耦合，当 Codex 等产品拥有自己的原生 session 线程（含 compaction、resume、tool 执行）时，通过标准 provider 传输层适配会丢失原生能力或需要重复实现。</p>\n</li>\n<li>\n<p><strong>回退策略混乱</strong>：当原生运行时失败时，是静默回退到嵌入式运行时、重试同一运行时、还是直接报错？缺乏统一的分类和决策机制。</p>\n</li>\n<li>\n<p><strong>会话状态割裂</strong>：原生运行时的 thread id、resume token 与 OpenClaw 的 session/transcript 各自独立，切换运行时会丢失上下文。</p>\n</li>\n</ol>\n<p>OpenClaw Harness 的设计思路是：<strong>将\"执行\"从\"控制\"中彻底分离</strong>。控制平面负责所有 Agent 共用的决策（认证、预算、工具策略、通道回复），harness 只负责一次已准备好的 turn 的低级执行。这种分离使得 Codex 可以保留其原生线程管理、compaction 和 app-server 协议，同时被纳入 OpenClaw 的统一 session/channel/tool policy 框架。</p>\n<h5>运行时选择策略</h5>\n<p>OpenClaw 采用三级优先级从高到低的运行时选择策略：</p>\n<ol>\n<li>\n<p><strong>Model-scoped runtime</strong>：在 <code>models</code> 配置中为特定模型显式指定 <code>agentRuntime.id</code>，如 <code>\"openai/gpt-5.5\": { \"agentRuntime\": { \"id\": \"codex\" } }</code>。这是最精确的绑定。</p>\n</li>\n<li>\n<p><strong>Provider-scoped runtime</strong>：在 <code>providers</code> 配置中为整个 provider 指定默认运行时。优先级低于 model-scoped。</p>\n</li>\n<li>\n<p><strong>auto 模式</strong>（默认）：OpenClaw 遍历所有已注册 harness 插件，调用 <code>supports(ctx)</code> 询问是否支持当前 provider/model。若多个插件声明支持，按 priority 排序。无匹配时使用嵌入式运行时。</p>\n</li>\n</ol>\n<blockquote>\n<p>关键约束：一旦插件 harness 通过 <code>supports()</code> 认领了一次运行并开始执行（产生 assistant text、tool calls 或 message sends），OpenClaw <strong>不会</strong>将该 turn 重放到另一个运行时。这是为了避免认证/运行时语义变更或副作用重复。</p>\n</blockquote>\n<h5>runtimePlan 策略包</h5>\n<p><code>runtimePlan</code> 是 OpenClaw Core 在下发 prepared attempt 时注入的策略包。Harness 可读取这些策略来做与 OpenClaw 行为一致的决策，但<strong>不得修改</strong>其内容或利用它在 turn 内部切换 provider/model：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>策略模块</th>\n<th>功能</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>tools.normalize(...)</code></td>\n<td>Provider 感知的工具 schema 规范化</td>\n</tr>\n<tr>\n<td><code>tools.logDiagnostics(...)</code></td>\n<td>工具调用诊断日志</td>\n</tr>\n<tr>\n<td><code>transcript.resolvePolicy(...)</code></td>\n<td>Transcript 清洗和 tool-call 修复策略</td>\n</tr>\n<tr>\n<td><code>delivery.isSilentPayload(...)</code></td>\n<td>判断是否为 NO_REPLY 或媒体静默投递</td>\n</tr>\n<tr>\n<td><code>outcome.classifyRunResult(...)</code></td>\n<td>模型回退分类（empty/reasoning/planning）</td>\n</tr>\n<tr>\n<td><code>observability</code></td>\n<td>已解析的 provider/model/harness 元数据</td>\n</tr>\n</tbody>\n</table></div>\n<h5>工具结果中间件</h5>\n<p>OpenClaw 提供运行时中立的工具结果中间件机制：</p>\n<pre><code class=\"language-typescript\">api.registerAgentToolResultMiddleware(...)\n</code></pre>\n<p>该接口要求插件在 manifest 的 <code>contracts.agentToolResultMiddleware</code> 中声明目标运行时 id。适用于在工具输出返回给模型之前执行异步转换（如格式化、过滤、增强）。这替代了旧的两套钩子：\n- <code>api.registerCodexAppServerExtensionFactory(...)</code>（Codex-only，已标记为 legacy）\n- <code>api.registerEmbeddedExtensionFactory(...)</code>（已移除，需迁移到运行时中立 API）</p>\n<h5>终端结果分类</h5>\n<p>当原生 harness 完成一次 turn 但没有产生可见的 assistant text 时，调用 <code>classifyAgentHarnessTerminalOutcome(...)</code> 可将结果分类为：\n- <code>empty</code>：完全空输出，可能需要重试\n- <code>reasoning-only</code>：只有推理内容但无用户可见回复\n- <code>planning-only</code>：只有规划步骤</p>\n<p>OpenClaw 的 fallback 策略据此决定是否用其他模型重试。Prompt 错误、进行中的 turn 和 <code>NO_REPLY</code> 等有意静默回复<strong>不被分类</strong>，避免误触发回退。</p>\n<h5>Codex 参考实现</h5>\n<p>Codex 插件是 OpenClaw 官方捆绑的 harness 实现：</p>\n<ul>\n<li><strong>Provider 注册</strong>：同时注册 provider（使 model refs、auth、<code>/model</code> 选择对 OpenClaw 可见）和 harness（通过 <code>supports()</code> 认领）。</li>\n<li><strong>Model refs 路由</strong>：<code>openai/gpt-*</code> 引用默认选择 Codex harness；旧的 <code>codex/gpt-*</code> 引用作为兼容性别名保留。</li>\n<li><strong>App-server 协议</strong>：OpenClaw 向 Codex 发送裸 model id，harness 负责与 Codex app-server（要求 &gt;= 0.125.0）通信。</li>\n<li><strong>显式绑定</strong>：通过 <code>agentRuntime.id: \"codex\"</code> 可强制仅使用 Codex 路径（失败即报错，不回退到嵌入式运行时）。</li>\n<li><strong>Transcript Mirror</strong>：Codex 维护原生 thread id 和 resume 行为，同时将所有用户可见的 assistant/tool 输出镜像到 OpenClaw transcript。</li>\n</ul>\n<h5>Native Session 与 Transcript 双轨机制</h5>\n<p>Harness 可以维护自己的原生会话标识（thread id、daemon-side resume token），但必须：\n1. 将该绑定显式关联到 OpenClaw session\n2. 持续将用户可见的 assistant/tool 输出镜像到 OpenClaw transcript\n3. 实现 <code>reset(...)</code> 方法，当 OpenClaw session 被 reset 时清除原生侧绑定</p>\n<p>OpenClaw transcript 作为兼容层保障：\n- Channel 可见的会话历史\n- Transcript 搜索与索引\n- 后续 turn 切换回嵌入式 OpenClaw harness 的能力\n- 通用的 <code>/new</code>、<code>/reset</code> 和 session 删除行为</p>\n<h5>与传统方法的区别</h5>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>维度</th>\n<th>传统 Provider 插件</th>\n<th>OpenClaw Harness</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>抽象层次</td>\n<td>HTTP/WebSocket API 传输</td>\n<td>原生 session 运行时</td>\n</tr>\n<tr>\n<td>适用场景</td>\n<td>标准 LLM API 接入</td>\n<td>自有线程/compaction/resume 的 Agent 服务器</td>\n</tr>\n<tr>\n<td>Session 归属</td>\n<td>OpenClaw 全权管理</td>\n<td>Harness 管理原生 session，OpenClaw 管理 transcript</td>\n</tr>\n<tr>\n<td>回退策略</td>\n<td>Provider 级别 fallback</td>\n<td>三级选择 + 分类器驱动的模型回退</td>\n</tr>\n<tr>\n<td>扩展方式</td>\n<td>Provider 专有钩子</td>\n<td>运行时中立中间件 API</td>\n</tr>\n</tbody>\n</table></div>",
      "quiz": {
        "q": "OpenClaw Agent Harness 的 runtimePlan 策略包的正确使用方式是？",
        "options": [
          "Harness 可以修改 runtimePlan 中的 tools.normalize 来实现自定义工具策略",
          "Harness 读取 runtimePlan 做与 OpenClaw 行为一致的决策，但不得修改其内容",
          "runtimePlan 仅用于 provider/model 的初始选择，选择完成后不再使用",
          "Harness 可以忽略 runtimePlan，完全使用原生运行时的独立策略"
        ],
        "answer": 1,
        "explain": "runtimePlan 是 OpenClaw Core 注入的只读策略包，harness 应读取其中 tools、transcript、delivery、outcome 等模块以保持与 OpenClaw 行为一致，但不得修改或利用它在 turn 内部切换 provider/model。"
      }
    },
    {
      "id": "roadmapbench",
      "num": 18,
      "name": "RoadmapBench",
      "fullName": "版本升级长程开发基准 (RoadmapBench)",
      "year": "2026.05",
      "org": "UniPat AI",
      "parent": "idebench",
      "paperUrl": "https://arxiv.org/abs/2605.15846",
      "projectUrl": "",
      "category": "evaluation",
      "motivation": "用版本升级任务检验长程工程能力",
      "summary": "RoadmapBench 提出了一个面向长程软件工程任务的评估基准，通过模拟真实世界版本升级（Version Upgrade）场景，系统性地检验大语言模型在跨越多版本迭代的复杂工程任务中的规划、适应和持续执行能力，填补了现有评测体系对长程开发能力覆盖不足的空白。",
      "keyPoints": [
        "聚焦<strong>版本升级</strong>场景：模拟真实软件从旧版本到新版本的完整迁移过程，涵盖依赖更新、API 变更适配、配置文件迁移等多维度子任务",
        "构建了<strong>多阶段长程任务</strong>框架：每个任务跨越多个版本节点，要求模型在长时间跨度内保持一致的工程决策能力",
        "引入<strong>累积式错误分析</strong>：追踪模型在多步长程推理中的错误传播与累积模式，揭示当前 LLM 在长程任务中的退化规律",
        "提供<strong>标准化评估协议</strong>：包含自动化验证脚本与人工评估准则的双轨评测体系",
        "与 IDE 环境深度集成：基于 idebench 框架扩展，在真实开发环境中评估模型的长程工程表现",
        "评估维度涵盖：版本跨度适应力、中间态变更跟踪、回归风险预判、工程决策一致性"
      ],
      "detail": "<p><img alt=\"RoadmapBench 示意图\" src=\"https://ar5iv.labs.arxiv.org/html/2605.15846/assets/x1.png\" />\n<em>图：RoadmapBench 的核心框架或评测示意。</em></p>\n<h5>1. 动机与背景</h5>\n<p>现有的代码生成与软件工程评测基准（如 HumanEval、SWE-bench）侧重于单次、短程的任务完成，无法充分反映真实工业场景中跨越多个版本的持续集成与升级挑战。在真实开发中，工程师需要处理依赖库的主版本升级（如 Python 2 → 3、框架大版本迁移），这类任务不仅涉及语法修正，还需要理解版本间语义变更、API 弃用周期以及下游兼容性。RoadmapBench 的设计动机就是弥补这一长程评测的空白。</p>\n<h5>2. 核心机制：版本升级任务设计</h5>\n<p>RoadmapBench 的核心方法论围绕\"版本路线图（Roadmap）\"展开：</p>\n<ul>\n<li><strong>版本节点定义</strong>：每个任务设定一个起始版本 <span class=\"kb-math kb-math-inline\">V_0</span> 和目标版本 <span class=\"kb-math kb-math-inline\">V_k</span>，中间经过 <span class=\"kb-math kb-math-inline\">k</span> 个关键版本节点 <span class=\"kb-math kb-math-inline\">V_1, V_2, \\ldots, V_{k-1}</span>。每个节点对应真实世界中该依赖库的特定发行版，附带确定的变更日志（Changelog）与弃用声明（Deprecation Notice）。</li>\n<li><strong>任务分解</strong>：模型需要将总版本跳变分解为逐步的近邻版本迁移，在每个中间节点完成局部的代码修正与测试验证。</li>\n<li><strong>累积依赖处理</strong>：版本升级往往不是孤立的，一个依赖的升级可能触发传递依赖的连锁更新。RoadmapBench 包含交叉依赖场景，考察模型的全局工程规划能力。</li>\n</ul>\n<div class=\"key-point\">💡 关键：RoadmapBench 的升级路径不是线性设计的，而是允许分支与回退，模拟工程师在实际决策中可能遇到的\"是否需要跳过某个中间版本\"的判断。</div>\n<h5>3. 任务形式与评估指标</h5>\n<p>每个 RoadmapBench 任务包含：\n- <strong>源仓库</strong>：包含旧版本代码的完整项目\n- <strong>升级指令</strong>：自然语言描述的目标版本与约束条件\n- <strong>环境镜像</strong>：可重现的 Docker 容器，确保评估一致性\n- <strong>验证套件</strong>：包含单元测试、集成测试和构建成功标准的自动化检查</p>\n<p>评估指标：\n- <strong>任务成功率</strong> (Success Rate)：是否最终通过所有验证\n- <strong>步数效率</strong> (Step Efficiency)：实际执行步数与最优步数之比\n- <strong>错误修正率</strong> (Error Recovery Rate)：发生错误后成功自我修复的比例\n- <strong>一致性分数</strong> (Consistency Score)：跨多版本节点的工程决策保持度</p>\n<h5>4. 与传统基准的对比</h5>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>维度</th>\n<th style=\"text-align: center;\">HumanEval / MBPP</th>\n<th style=\"text-align: center;\">SWE-bench</th>\n<th style=\"text-align: center;\">RoadmapBench</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>任务跨度</td>\n<td style=\"text-align: center;\">单函数级</td>\n<td style=\"text-align: center;\">单 Issue 级</td>\n<td style=\"text-align: center;\">多版本长程</td>\n</tr>\n<tr>\n<td>时间视角</td>\n<td style=\"text-align: center;\">即时</td>\n<td style=\"text-align: center;\">即时</td>\n<td style=\"text-align: center;\">跨版本演化</td>\n</tr>\n<tr>\n<td>错误传播</td>\n<td style=\"text-align: center;\">不涉及</td>\n<td style=\"text-align: center;\">有限</td>\n<td style=\"text-align: center;\">显式追踪</td>\n</tr>\n<tr>\n<td>依赖管理</td>\n<td style=\"text-align: center;\">无</td>\n<td style=\"text-align: center;\">简单</td>\n<td style=\"text-align: center;\">复杂传递依赖</td>\n</tr>\n</tbody>\n</table></div>\n<p>RoadmapBench 的独特贡献在于首次将软件的<strong>时间演化维度</strong>引入自动化评测，为评估大语言模型作为\"长程工程伙伴\"的能力提供了全新视角。</p>\n<h5>5. 初步发现</h5>\n<p>论文对多个主流 LLM（包括 GPT-4、Claude、Gemini 等）在 RoadmapBench 上进行测试，发现：\n- 所有模型在长程任务中的成功率显著低于短程基准，退步幅度可达 40% 以上\n- 错误呈现明显的<strong>累积效应</strong>：早期版本次的错误决策会逐步放大，导致后期无法修复\n- 模型在\"识别弃用 API\"方面表现较好，但在\"理解语义变更\"和\"平衡多依赖升级顺序\"方面存在明显短板\n- 显式使用版本变更日志作为上下文能够显著提升表现，提示 RAG 增强方向</p>\n<pre><code class=\"language-python\">for task in benchmark:\n    env.reset(task)\n    result = agent.run(env.observe())\n    metrics.record(validate(result, env))\n</code></pre>",
      "quiz": {
        "q": "RoadmapBench 与 SWE-bench 的核心区别是什么？",
        "options": [
          "RoadmapBench 只评估 Python 代码，SWE-bench 支持多语言",
          "RoadmapBench 聚焦跨多版本的长程升级任务，SWE-bench 侧重单 Issue 修复",
          "RoadmapBench 完全自动化，SWE-bench 需要人工评估",
          "RoadmapBench 不使用 Docker 环境，SWE-bench 使用"
        ],
        "answer": 1,
        "explain": "RoadmapBench 的核心创新在于模拟真实版本升级场景，跨越多个版本节点追踪持续工程能力，而 SWE-bench 聚焦单次 Issue 的修复，不具备长程时间演化视角。"
      }
    },
    {
      "id": "harness_bench",
      "num": 19,
      "name": "Harness-Bench",
      "fullName": "Harness 配置诊断基准 (Harness-Bench)",
      "year": "2026.05",
      "org": "北大/Qiyuan Tech",
      "parent": "ahe",
      "paperUrl": "https://arxiv.org/abs/2605.27922",
      "projectUrl": "",
      "category": "evaluation",
      "motivation": "分离模型与harness配置效应",
      "summary": "Harness-Bench 提出首个系统性诊断框架，通过在统一的106个沙盒任务、多模型后端和多harness配置的factorial矩阵（5194条轨迹）上评测，量化了agent执行层（harness）对性能的实质性影响——相同模型在不同harness下得分差距可达23.8分，并揭示了5类典型执行漂移失败模式，明确提出“agent性能应报告为model–harness配置层级的属性而非仅归因于基础模型”。",
      "keyPoints": [
        "提出 <strong>Harness-Bench</strong> 诊断性benchmark，首次系统研究harness配置对agent端到端性能的影响",
        "包含 <strong>106个沙盒化任务</strong>，覆盖8个workflow类别：软件工程(22)、数据分析(14)、工具/多模态操作(15)、知识检索(13)、办公通信(12)、垂直专业工作流(12)、长期自主/状态适应(11)、SRE/DevOps(7)",
        "6个可配置harness（OpenClaw, NanoBot, Hermes, ZeroClaw, NullClaw, Moltis） × 8个模型后端，形成完整factorial矩阵，共计 <strong>5,194条完整执行轨迹</strong>",
        "三维评分公式：<strong>TaskScore = Security × Completion × Process</strong>，其中Process = (Robustness + ToolUse + Consistency) / 3，Security为二值安全门控",
        "发现可配置harness间最大性能差距 <strong>23.8分</strong>（NanoBot 76.2 vs OpenClaw 52.4），Codex（model-bound coding agent）作为参考基线达80.4",
        "提出 <strong>“执行对齐”(execution alignment)</strong> 概念：衡量harness是否保持推理、workspace状态、工具action和evaluator检查条件之间的对应关系",
        "归纳 <strong>5类典型失败模式</strong>：Contract/format(36.4%)、Tool/recovery(24.6%)、Evidence/grounding(14.6%)、Artifact commitment(11.1%)、State/continuation(9.3%)",
        "发现更强模型后端跨harness方差更小，表明强模型对执行层差异更宽容"
      ],
      "detail": "<h5>1. 核心框架：Harness-Bench评估管道</h5>\n<p><img alt=\"Harness-Bench评估管道概览\" src=\"https://arxiv.org/html/2605.27922v1/x1.png\" />\n<em>图1：Harness-Bench评估管道。每个任务在沙盒中实例化，由model–harness配置执行。系统记录artifacts、traces、usage statistics和validator outputs，最终合成综合诊断分数。</em></p>\n<p>Harness-Bench将Agent定义为 <strong>Agent = Model + Harness</strong>。评估管道分为三阶段：</p>\n<ol>\n<li><strong>Setup</strong>：渲染task specification，构建runtime环境，初始化fresh sandbox</li>\n<li><strong>Execution</strong>：配置好的agent在budget和workspace约束下尝试完成任务，记录所有模型请求/响应、tool calls、workspace变更</li>\n<li><strong>Judge</strong>：evaluator检查final workspace状态和execution evidence，agent无法访问reference artifacts、hidden answers和evaluator scripts</li>\n</ol>\n<p>每个run产生四类证据：final workspace state、execution trace、usage statistics、validator outputs。</p>\n<h5>2. 评分公式与指标体系</h5>\n<p>核心评分采用乘法聚合：</p>\n<div class=\"kb-math kb-math-display\">\\text{TaskScore}_i = \\text{Security}_i \\cdot \\text{Completion}_i \\cdot \\text{Process}_i</div>\n<p>其中：\n- <strong>Security</strong> ∈ {0, 1}：binary gate，任何显式权限/安全违规（未授权访问、秘钥泄露、禁止操作）直接归零\n- <strong>Completion</strong>：task-specific deterministic validator或rubric-based judgment\n- <strong>Process</strong> = (Robustness + ToolUse + Consistency) / 3：\n  - <strong>Robustness</strong>：agent是否正确处理tool或环境故障\n  - <strong>ToolUse</strong>：工具选择和应用的适当性\n  - <strong>Consistency</strong>：actions、observations、中间状态和最终输出与workspace状态和用户约束的一致性</p>\n<div class=\"key-point\">💡 关键：乘法公式设计极为保守——需同时满足任务完成、无安全违规、可靠执行行为三者才能获得高分。Process分数通过外部LLM judge（claude-sonnet-4.6）从重构trace中评估。</div>\n<h5>3. 任务套件设计：四原则筛选</h5>\n<p>所有106个任务需满足四个纳入标准：\n- <strong>Realism</strong>：反映真实的用户workflow\n- <strong>Solvability</strong>：可使用提供的沙盒资源完成\n- <strong>Oracle-checkability</strong>：成功可由确定性检查或指定rubric验证\n- <strong>Integrity</strong>：agent无法通过读取隐藏答案、修改protected fixtures或绕过约束来获得credit</p>\n<p>任务完全本地化、沙盒化执行，避免了依赖live services带来的benchmark drift问题。</p>\n<h5>4. 实验设计与核心发现</h5>\n<p><strong>Factorial矩阵控制变量</strong>（Table 1）：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>因素</th>\n<th>处理方式</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Task prompt/fixtures</td>\n<td>每个task固定</td>\n</tr>\n<tr>\n<td>初始沙盒状态</td>\n<td>每个task固定</td>\n</tr>\n<tr>\n<td>Budget/timeout/evaluator</td>\n<td>每个task固定</td>\n</tr>\n<tr>\n<td>模型后端</td>\n<td>Factorial矩阵中变化</td>\n</tr>\n<tr>\n<td>Harness配置</td>\n<td>Factorial矩阵中变化</td>\n</tr>\n<tr>\n<td>Prompting/action格式</td>\n<td>各harness原生</td>\n</tr>\n<tr>\n<td>Tool接口/状态策略</td>\n<td>各harness原生</td>\n</tr>\n<tr>\n<td>Retry/recovery行为</td>\n<td>各harness原生</td>\n</tr>\n</tbody>\n</table></div>\n<p><strong>主要结果</strong>（Table 2聚合）：\n- NanoBot得分最高(76.2)，OpenClaw最低(52.4)，<strong>差距23.8分</strong>\n- Codex（GPT-5.4底层，model-bound coding agent）达80.4，但作为专门化系统的参考基线\n- <strong>更强模型后端跨harness方差更低</strong>：表明强模型对prompting、tool interfaces、state management差异更宽容\n- Token/turn用量不能单独解释性能：NanoBot用68.7K tokens达76.2，NullClaw用175.1K tokens仅64.4</p>\n<h5>5. 执行对齐与失败模式分析</h5>\n<p>论文提出关键概念<strong>执行对齐（execution alignment）</strong>：衡量harness保持以下几点之间对应关系的程度——\n1. agent的推理(reasoning)\n2. 观察到的workspace状态\n3. 通过工具执行的动作\n4. evaluator检查的条件</p>\n<p><strong>5类典型失败症状及出现率</strong>（Table 3）：\n- <strong>Contract/format</strong> (36.4%)：schema或output-contract违规——malformed JSON、缺失ledger行、不完整manifest\n- <strong>Tool/recovery</strong> (24.6%)：工具错误或blocked commands后无有效恢复或计划修订\n- <strong>Evidence/grounding</strong> (14.6%)：不完整source coverage，伴随无支撑声明或缺失验证\n- <strong>Artifact commitment</strong> (11.1%)：有合理推理但未提交required outputs或workspace artifacts\n- <strong>State/continuation</strong> (9.3%)：在中断或多轮任务中无法保存持久进度或可靠恢复</p>\n<div class=\"warn-box\">⚠️ 注意：这些失败发生在\"语义合理性\"和\"机器可验证输出\"的边界——agent可能看似理解任务，但在执行层面与oracle可验证的条件脱节。</div>\n<h5>6. 与传统Benchmark的关系</h5>\n<p>区别于SWE-bench、AgentBench等outcome-grounded benchmark，Harness-Bench的独特贡献在于<strong>将harness从背景条件提升为第一类研究变量</strong>。它不要求所有系统统一内部policy或runtime，每个harness保持原生行为，从而在共享外部条件下衡量<strong>配置层级</strong>的诊断性差异，而非因果分解单个机制。</p>\n<pre><code class=\"language-python\">for task in benchmark:\n    env.reset(task)\n    result = agent.run(env.observe())\n    metrics.record(validate(result, env))\n</code></pre>",
      "quiz": {
        "q": "Harness-Bench评分公式中Security gate的作用是什么？",
        "options": [
          "衡量agent的token使用效率",
          "作为二值门控，任何安全/权限违规直接使总分归零",
          "计算Process子项中的Consistency分数",
          "仅在Completion分数不达标时触发惩罚"
        ],
        "answer": 1,
        "explain": "Security ∈ {0,1}是乘法公式中的二值门控——一旦发生未授权访问、秘钥泄露或禁止操作等安全违规，无论Completion和Process分数多高，TaskScore直接归零。"
      }
    },
    {
      "id": "codex_safety",
      "num": 20,
      "name": "Codex Safety",
      "fullName": "Codex 安全治理运行栈 (Running Codex Safely)",
      "year": "2026.05",
      "org": "OpenAI",
      "parent": "codex",
      "paperUrl": "https://openai.com/index/running-codex-safely/",
      "projectUrl": "",
      "category": "runtime",
      "motivation": "用规则审批与遥测约束高风险执行",
      "summary": "OpenAI 提出了一套分层安全治理运行栈，通过**事前规则审批**（Rule-based Approval）与**事后遥测约束**（Telemetry-based Constraints）双重机制，在不可信执行环境中对 Codex Agent 的高风险操作进行实时审计与自动熔断，解决了 LLM Agent 自主执行代码时的安全可控难题。",
      "keyPoints": [
        "<strong>两层安全架构</strong>：事前策略检查（Pre-execution Policy Check）+ 运行时遥测监控（Runtime Telemetry Monitoring），构成纵深防御",
        "<strong>规则审批引擎</strong>：在 Codex 执行文件写入、Shell 命令、网络请求等高风险操作前，自动匹配预设安全策略，违规则拒绝或升级人工审批",
        "<strong>遥测约束管道</strong>：实时采集 Agent 的 CPU、内存、磁盘 IO、网络流量、子进程树等系统级指标，异常时自动熔断",
        "<strong>策略描述语言</strong>：使用声明式策略 DSL 定义安全规则，支持按操作类型、路径模式、网络目标等维度精细化控制",
        "<strong>熔断与回滚机制</strong>：检测到异常行为（如资源超限、敏感文件访问、异常外联）后自动终止会话，支持文件系统快照回滚",
        "<strong>与 Codex 训练 Pipeline 解耦</strong>：安全层独立于模型推理，不增加推理延迟，可横向扩展到其他 Agent 框架",
        "<strong>审计日志完整性</strong>：所有操作决策、遥测数据、审批记录落盘，支持事后合规审查"
      ],
      "detail": "<p><img alt=\"Codex Safety 示意图\" src=\"https://images.ctfassets.net/kftzwdyauwt9/6wYGm9QST2WYLbPJl5YwZC/1e63f3bfb458ce891db4f94a52052240/Codex_Blog_Header_V5.png?fm=webp&amp;q=90&amp;w=3840\" />\n<em>图：Codex Safety 的核心框架或系统示意。</em></p>\n<h5>4.1 核心架构示意</h5>\n<p>Codex 安全治理运行栈的架构分为三个层级，从内到外依次为：Codex 执行引擎、策略审批层、遥测监控层。用户发出的每个指令（Prompt）在到达执行引擎前，先经<strong>策略审批层</strong>进行意图分析和高风险操作预检；执行过程中，<strong>遥测监控层</strong>持续采集系统指标，一旦触发熔断阈值即终止会话。</p>\n<pre><code>┌──────────────────────────────────────┐\n│          用户请求 (Prompt)            │\n└─────────────┬────────────────────────┘\n              ▼\n┌──────────────────────────────────────┐\n│    策略审批层 (Pre-execution)          │\n│  ┌────────┐  ┌────────┐  ┌────────┐ │\n│  │ 文件访问 │  │ Shell  │  │ 网络   │ │\n│  │ 策略    │  │ 策略   │  │ 策略   │ │\n│  └────┬───┘  └───┬────┘  └───┬────┘ │\n│       └──────────┼───────────┘       │\n│                  ▼                   │\n│          ┌──────────────┐            │\n│          │ 审批/拒绝/升级│            │\n│          └──────────────┘            │\n└─────────────────┬────────────────────┘\n                  ▼ (通过审批)\n┌──────────────────────────────────────┐\n│        Codex 执行引擎                 │\n│   (文件/Script/Shell/Browser...)     │\n└─────────────────┬────────────────────┘\n                  ▼ (运行时)\n┌──────────────────────────────────────┐\n│    遥测监控层 (Runtime)               │\n│  ┌────────┐  ┌────────┐  ┌────────┐ │\n│  │ CPU/内存│  │ 磁盘IO │  │ 网络流量│ │\n│  └────┬───┘  └───┬────┘  └───┬────┘ │\n│       └──────────┼───────────┘       │\n│                  ▼                   │\n│          ┌──────────────┐            │\n│          │ 熔断/回滚/告警│            │\n│          └──────────────┘            │\n└──────────────────────────────────────┘\n</code></pre>\n<h5>4.2 规则审批引擎</h5>\n<p>策略审批是安全治理的第一道防线。核心机制如下：</p>\n<p><strong>声明式策略 DSL</strong></p>\n<p>安全策略使用 YAML 风格的声明式语言定义。每条规则包含操作类型（operation）、匹配条件（condition）和动作（action）三要素。示例：</p>\n<pre><code class=\"language-yaml\">rules:\n  - name: &quot;禁止写入系统目录&quot;\n    operation: &quot;file_write&quot;\n    condition:\n      path_pattern: [&quot;/etc/*&quot;, &quot;/sys/*&quot;, &quot;/boot/*&quot;]\n    action: &quot;deny&quot;\n    escalation: &quot;human_approval&quot;\n\n  - name: &quot;限制Shell高危命令&quot;\n    operation: &quot;shell_exec&quot;\n    condition:\n      command_pattern: [&quot;rm -rf /*&quot;, &quot;mkfs.*&quot;, &quot;dd if=* of=/dev/*&quot;]\n    action: &quot;deny&quot;\n\n  - name: &quot;外联白名单&quot;\n    operation: &quot;network_outbound&quot;\n    condition:\n      host_not_in: [&quot;api.openai.com&quot;, &quot;pypi.org&quot;]\n    action: &quot;deny_with_log&quot;\n</code></pre>\n<p><strong>高风险操作分类</strong></p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>操作类别</th>\n<th>风险等级</th>\n<th>典型操作</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>文件读取</td>\n<td>低</td>\n<td>读取用户工作区、配置文件</td>\n</tr>\n<tr>\n<td>文件写入</td>\n<td>中</td>\n<td>修改/创建文件</td>\n</tr>\n<tr>\n<td>Shell 执行</td>\n<td>高</td>\n<td>任意命令执行</td>\n</tr>\n<tr>\n<td>网络请求</td>\n<td>高</td>\n<td>外联下载/上传数据</td>\n</tr>\n<tr>\n<td>进程管理</td>\n<td>高</td>\n<td>创建子进程、发送信号</td>\n</tr>\n<tr>\n<td>包安装</td>\n<td>高</td>\n<td>pip/apt 安装未校验包</td>\n</tr>\n</tbody>\n</table></div>\n<div class=\"key-point\">💡 关键：策略审批在 <strong>Codex 生成工具调用的参数阶段介入</strong>，而非在最终执行阶段。这意味着即使模型产生高风险意图，在实际调用系统 API 前即被拦截，不会留下恶意操作的窗口。</div>\n<h5>4.3 遥测约束管道</h5>\n<p>通过审批的操作进入执行阶段后，<strong>遥测约束管道</strong>作为第二道防线持续监控。</p>\n<p><strong>监控指标</strong></p>\n<p>遥测 Agent 以独立进程运行，通过 Linux cgroup、namespace 和 eBPF 采集以下指标：</p>\n<ul>\n<li><strong>资源上限</strong>：CPU 配额 \\<span class=\"kb-math kb-math-inline\">C_{max}\\</span>、内存限制 \\<span class=\"kb-math kb-math-inline\">M_{max}\\</span>、磁盘 IO 带宽</li>\n<li><strong>进程树追踪</strong>：Codex 及其子进程的 fork/exec 行为，检测逃逸进程</li>\n<li><strong>网络流量审计</strong>：实时分析出站连接的目标 IP、端口和协议</li>\n<li><strong>敏感路径访问</strong>：监控对 \\<span class=\"kb-math kb-math-inline\">/proc, /sys, /root, /etc/shadow\\</span> 等路径的异常 read/open 调用</li>\n</ul>\n<p><strong>熔断逻辑</strong></p>\n<p>当任意指标超过阈值 \\<span class=\"kb-math kb-math-inline\">T\\</span> 时触发熔断：</p>\n<div class=\"kb-math kb-math-display\">\\text{circuit\\_break} = \n\\begin{cases}\n\\text{true} &amp; \\text{if } \\exists i: m_i &gt; T_i \\\\\n\\text{true} &amp; \\text{if 检测到进程逃逸} \\\\\n\\text{true} &amp; \\text{if 外联至未信任 IP} \\\\\n\\text{false} &amp; \\text{otherwise}\n\\end{cases}</div>\n<p>熔断后，遥测层向 Codex 会话发送 SIGTERM，同时触发<strong>文件系统快照回滚</strong>——利用 copy-on-write 快照将工作区恢复到任务开始前的状态。</p>\n<div class=\"warn-box\">⚠️ 注意：遥测约束与审批策略<strong>互补而非替代</strong>。审批解决\"要不要做\"，遥测解决\"做得对不对/有没有异常\"。即便审批策略放行的操作，在运行时若表现出攻击特征（如 CPU 挖矿、数据外泄），遥测层仍会熔断。</div>\n<h5>4.4 训练与推理阶段的差分治理</h5>\n<p>OpenAI 特别强调了训练环境（内部 Codex 沙箱）与推理环境（用户侧部署）的安全策略差异：</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>维度</th>\n<th>训练环境</th>\n<th>推理/部署环境</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>网络</td>\n<td>完全隔离，仅允许内网 Python 镜像源</td>\n<td>白名单外联策略</td>\n</tr>\n<tr>\n<td>文件系统</td>\n<td>临时 overlayfs，会话结束后丢弃</td>\n<td>快照 + 回滚</td>\n</tr>\n<tr>\n<td>审计</td>\n<td>全量策略日志 + 遥测事件</td>\n<td>用户可自定义审计粒度</td>\n</tr>\n<tr>\n<td>审批</td>\n<td>自动执行（deny-by-default）</td>\n<td>支持 human-in-the-loop</td>\n</tr>\n</tbody>\n</table></div>\n<h5>4.5 与传统沙箱方案的对比</h5>\n<p>对比传统方案（Docker 容器、seccomp、AppArmor），Codex 安全运行栈的差异化在于：</p>\n<ol>\n<li><strong>语义级策略</strong>：传统沙箱基于系统调用级别（如限制 <code>open()</code> 的 flags），Codex 的策略基于<strong>任务语义</strong>（\"是否在写论文草稿时意外执行了 <code>rm -rf</code>\"），更贴合 Agent 场景</li>\n<li><strong>策略与模型协同进化</strong>：通过遥测反馈，可自动发现模型高频误触的策略规则并优化，形成安全-性能的闭环</li>\n<li><strong>轻量级集成</strong>：独立于推理引擎，不需修改模型权重或推理代码，通过工具调用中间件即可注入</li>\n</ol>",
      "quiz": {
        "q": "Codex 安全治理运行栈中，策略审批层的主要拦截时机是什么？",
        "options": [
          "在 Codex 生成自然语言回复后",
          "在 Codex 生成工具调用参数后、实际执行前",
          "在工具执行完成后，由遥测层审计",
          "在用户提交 Prompt 时，进行意图分类"
        ],
        "answer": 1,
        "explain": "策略审批在 Codex 生成工具调用参数后、系统调用实际发生前介入。这样可以在模型产生高风险意图的瞬间完成拦截，而非等到执行后再审计，确保零执行窗口。"
      }
    },
    {
      "id": "rho",
      "num": 21,
      "name": "RHO",
      "fullName": "回顾式 Harness 优化 (Retrospective Harness Optimization)",
      "year": "2026.06",
      "org": "CityU/MSRA",
      "parent": "ahe",
      "paperUrl": "https://arxiv.org/abs/2606.05922",
      "projectUrl": "",
      "category": "optimization",
      "motivation": "只靠历史轨迹自监督优化harness",
      "summary": "RHO 是一种**零外部监督**的 agent harness 自优化方法：从历史轨迹中精选多样化的困难任务 coreset，并行重跑并利用 agent 自身的 self-validation、self-consistency 和 pairwise self-preference 来生成并筛选最优 harness 更新，单轮优化即可将 SWE-Bench Pro 通过率从 59% 提升至 78%。",
      "keyPoints": [
        "<strong>Phase 1 — Retrospective Coreset Selection</strong>: 从历史轨迹中基于难度和多样性选取代表性任务子集",
        "<strong>Phase 2 — Parallel Rollouts</strong>: 对 coreset 中的每个任务，agent 并行重新求解，生成多条 rollout 轨迹",
        "<strong>Phase 3 — Self-Analysis</strong>: 通过 self-validation（自我验证结果正确性）和 self-consistency（多条 rollout 之间的一致性）分析表现",
        "<strong>Phase 4 — Harness Update via Self-Preference</strong>: agent 生成多个候选 harness 更新方案，通过 pairwise self-preference 选出最优方案并应用",
        "SWE-Bench Pro 单轮优化：<strong>59% → 78%</strong>（+19个百分点），无需任何外部 grading",
        "三个领域验证：软件工程（SWE-Bench Pro）、技术工作、知识工作均有显著提升",
        "Harness 优化后，agent 的<strong>行为模式发生持久改变</strong>，在长时间会话中维持更高准确率",
        "RHO 能有效针对性地修复先前的<strong>失败模式</strong>",
        "首次将 <strong>self-preference</strong> 引入 harness 优化，agent 通过成对比较自行判断哪个 harness 更新更优",
        "<strong>Coreset selection</strong> 确保优化聚焦于最有价值的困难案例，避免冗余计算",
        "全流程<strong>零外部监督</strong>，仅依赖历史轨迹，实现真正的自主进化"
      ],
      "detail": "<h5>1. Algorithm 1 — RHO 完整算法伪代码</h5>\n<pre><code>Algorithm 1: Retrospective Harness Optimization (RHO)\n\nInput:  Agent A with harness H_0, trajectory history T, \n        number of optimization rounds R\nOutput: Optimized harness H_R\n\n1:  for round r = 1 to R do\n2:      // Phase 1: Retrospective Coreset Selection\n3:      C_r ← SelectCoreset(T, k)        ▷ k tasks from history\n4:          ▷ Selection criteria: (i) difficulty score, (ii) diversity\n5:      \n6:      // Phase 2: Parallel Rollouts\n7:      for each task t ∈ C_r do\n8:          Rollouts_t ← {A.solve(t, H_{r-1}) for i = 1..m}\n9:      end for\n10:     \n11:     // Phase 3: Self-Analysis\n12:     for each task t ∈ C_r do\n13:         V_t ← SelfValidate(Rollouts_t, t)\n14:         ▷ Check: output correctness, intermediate reasoning, tool usage\n15:         S_t ← SelfConsistency(Rollouts_t)\n16:         ▷ Measure: agreement among m rollouts on final answer\n17:         A_t ← Aggregate(V_t, S_t)\n18:     end for\n19:     \n20:     // Phase 4: Harness Update via Self-Preference\n21:     Updates ← GenerateCandidates(A, C_r, {A_t, Rollouts_t})\n22:         ▷ Candidates: modified prompts, tool specs, workflow DAGs\n23:     H_r ← SelectBySelfPreference(A, Updates, C_r, H_{r-1})\n24:         ▷ Pairwise comparison: &quot;Which harness leads to better outcomes?&quot;\n25:         ▷ Select update with highest win rate over H_{r-1}\n26: end for\n27: return H_R\n</code></pre>\n<p><strong>解读</strong>：\n- <strong>Phase 1 的 Coreset Selection</strong> 是整个优化的基础。不同于随机采样或全量重跑，RHO 通过难度评分（如历史失败率）和多样性度量（如任务 embedding 的 MMR）选取 k 个任务。这保证了优化资源投放在最有信息量的案例上。\n- <strong>Phase 2 的并行 Rollouts</strong> 利用 agent 的非确定性（temperature &gt; 0），对同一任务生成 m 条可能不同的求解路径。这种多样性是后续 self-consistency 分析的信息来源。\n- <strong>Phase 3 的 Self-Analysis</strong> 包含两个互补维度：SelfValidation 直接检查单条 rollouts 的输出质量（代码能否运行、答案格式是否正确），SelfConsistency 通过多条 rollouts 的答案一致性来间接评估，两者结合给出无需 ground truth 的可靠质量信号。\n- <strong>Phase 4 的 Self-Preference</strong> 是最核心的创新：agent 生成候选 harness 修改（如调整提示词、工具调用策略），然后通过成对比较（\"使用 harness A 得到的 rollouts vs. 使用 harness B 得到的 rollouts\"）让 agent 自己判断哪个更好，选择胜率最高的更新。</p>\n<h5>2. 示意图说明</h5>\n<p><strong>Figure 1 — RHO 总体框架</strong>\n<img alt=\"RHO Framework\" src=\"https://ar5iv.labs.arxiv.org/html/2606.05922/assets/x1.png\" />\n- <em>左半部分</em>：展示历史轨迹的积累，包含成功和失败的案例\n- <em>中间</em>：四阶段流水线（Coreset Selection → Parallel Rollouts → Self-Analysis → Self-Preference）\n- <em>右半部分</em>：优化后的 harness 在新任务上表现提升，形成正向循环</p>\n<p><strong>Figure 2 — Coreset Selection 示意</strong>\n<img alt=\"Coreset Selection\" src=\"https://ar5iv.labs.arxiv.org/html/2606.05922/assets/x2.png\" />\n- 可视化展示了如何在 embedding 空间中选取既困难又多样化的 coreset 任务\n- 颜色深浅表示任务难度，选取的任务（红圈）覆盖了表示空间的不同区域\n- 对比了随机采样、仅按难度采样和 RHO coreset 采样的分布差异</p>\n<p><strong>Figure 3 — 主实验结果</strong>\n<img alt=\"Main Results\" src=\"https://ar5iv.labs.arxiv.org/html/2606.05922/assets/x3.png\" />\n- SWE-Bench Pro: 59% → 78%（单轮优化）\n- 包含 ablation study：去掉 coreset selection、去掉 self-consistency、去掉 self-preference 的性能退化\n- 多轮优化的效果曲线，显示第二轮后趋于饱和</p>\n<p><strong>Figure 4 — Harness 优化前后行为模式变化</strong>\n<img alt=\"Behavior Change\" src=\"https://ar5iv.labs.arxiv.org/html/2606.05922/assets/x4.png\" />\n- 展示了 agent 工具调用序列的分布变化（如某个原来被过度使用的工具调用频率下降）\n- 推理链长度的分布偏移（关键步骤被前置或增加了特定的验证步骤）\n- 失败模式的针对性修复（特定类型的 bug 在优化后显著减少）</p>\n<p><strong>Figure 5 — 长时间会话中的稳定性</strong>\n<img alt=\"Long-horizon Stability\" src=\"https://ar5iv.labs.arxiv.org/html/2606.05922/assets/x5.png\" />\n- 对比优化前后 agent 在连续多步任务中的准确率衰减曲线\n- 优化后的 harness 显著减缓了长时间运行中的性能退化\n- 展示了累积错误率的变化趋势</p>\n<h5>3. Coreset Selection 的核心设计</h5>\n<p>RHO 的 coreset selection 采用两阶段评分：</p>\n<p><strong>难度评分</strong> (Difficulty Score)：\n- 历史轨迹中该任务的首次尝试成功率（越低越困难）\n- 推理步骤长度（过长或过短都可能表示困难）\n- 工具调用异常频率（如重复调用同一工具）</p>\n<p><strong>多样性评分</strong> (Diversity Score)：\n- 使用 Maximum Marginal Relevance (MMR) 在任务 embedding 空间中选取\n- 确保 coreset 覆盖不同的任务类型、领域和失败模式\n- 避免选取语义高度相似的冗余任务</p>\n<p>最终的 coreset 是难度和多样性的加权组合，实验表明两者缺一都会导致优化效果显著下降。</p>\n<h5>4. Self-Analysis 的双重验证机制</h5>\n<p><strong>Self-Validation</strong>：Agent 检查自身输出的质量\n- 代码任务：执行结果是否符合预期、能否编译通过\n- 推理任务：中间步骤是否有逻辑跳跃、最终结论是否有证据支持\n- 使用结构化 checklist 让 agent 逐项打分</p>\n<p><strong>Self-Consistency</strong>：通过多次 rollouts 交叉验证\n- 对同一任务运行 m 次（m=3~5），统计最终答案的一致性\n- 高一致性 → 答案可信度更高，低一致性 → 该任务是优化的重点目标\n- 与 self-validation 结合：两者矛盾时优先信任 consistency（更客观）</p>\n<p><strong>关键洞察</strong>：这两种机制都不需要 ground truth，却能为 harness 优化提供可靠的反馈信号。Self-validation 捕捉 agent 的\"自我认知\"，self-consistency 捕捉答案的\"客观稳定性\"。</p>\n<h5>5. Pairwise Self-Preference 的实现</h5>\n<p>这是 RHO 最核心的机制创新。具体流程：</p>\n<ol>\n<li><strong>候选生成</strong>：基于 coreset 的分析结果，agent 生成 N 个候选 harness 更新方案（如修改系统提示中关于工具使用的指导、调整特定工作流的条件分支）</li>\n<li><strong>成对评估</strong>：对每个候选 harness H'，agent 在 coreset 任务上分别用 H 和 H' 生成 rollouts</li>\n<li><strong>自我偏好判断</strong>：agent 比较配对结果，选择\"在更多任务上表现更好\"的 harness</li>\n<li><strong>胜率聚合</strong>：统计每个候选在 pairwise 比较中的胜率，选择胜率最高的方案</li>\n</ol>\n<p><strong>为什么不用绝对评分？</strong> 实验表明 LLM 的绝对评分（如\"这个解决方案打 8/10 分\"）有严重的校准问题。而 pairwise 比较（\"方案 A 和方案 B 哪个更好？\"）更符合 LLM 的评估能力，结果更稳定可靠。</p>",
      "quiz": {
        "q": "RHO 在没有外部标注和 ground-truth 验证集时，主要依靠什么机制来选择更好的 harness 更新？",
        "options": [
          "只看单次 rollout 的最终奖励，选择分数最高的候选",
          "让人工工程师离线审查所有轨迹并手工投票",
          "结合 self-validation、self-consistency，再通过 pairwise self-preference 比较候选 harness",
          "直接把历史失败任务加入监督微调数据，不再做 harness 搜索"
        ],
        "answer": 2,
        "explain": "RHO 的核心就在于无标注自监督闭环：先用自验证和自一致性分析轨迹，再让 agent 通过成对偏好比较候选 harness，而不是依赖外部标签。"
      }
    }
  ],
  "categories": {
    "foundation": {
      "label": "奠基基准",
      "color": "#0F766E"
    },
    "environment": {
      "label": "环境与沙箱",
      "color": "#2563EB"
    },
    "evaluation": {
      "label": "编码回归",
      "color": "#7C3AED"
    },
    "runtime": {
      "label": "运行时框架",
      "color": "#EA580C"
    },
    "optimization": {
      "label": "Harness 优化",
      "color": "#DC2626"
    }
  },
  "projectUrls": {}
};
